# 螺旋输送机（screwConveyor）风格对齐 · 分步优化计划

> 目标文件：`templates/screwConveyor.js`（经 grep 确认 `editor.js` / `preview.html` 无 `screwConveyor` 外挂分支，预计无需联动改动）
> 风格基准：`templates/bucketElevator.js`（斗式提升机）、`templates/reactor.js`（反应釜）
> 参照样板：`templates/screwConveyorLite.js`（螺旋输送机·简化版，已按同一套步骤改造完成，本轮主要实现参照）
> 本文档用于记录每一步的进度，异常中断后从第一个未勾选项继续。

## 0. 风格基准清单（必须照搬）

| 项 | 取值 |
| --- | --- |
| 金属渐变 | `#7d849e → #d9dded → #767d97`（局部 `<defs>`，id 带 uid 后缀，不用外部全局 `gEquip`） |
| 深色内腔 | `#12162b`，描边 `#6a7192` |
| 法兰 / 接管 | `#2a2f45` 填充 + `#3f445c` 描边 |
| 件体（金属件明细） | `#9aa2bc` + `#5b6280` |
| 门板 / 铰链 / 把手 | `#1a1f33` / `#3a4060` / `#8e96b6` |
| 定尺封顶 | `H(hr, wr) = Math.min(h*hr, w*wr)`；或逐量 `Math.max(下限, Math.min(上限, 比例量))` |
| 动画 | SMIL `<animate>` / `<animateTransform>`（编辑态由 `editor.js stripSMIL()` 剥离） |
| 接管三件套 | 金属渐变管壁 + 深色管腔（`#12162b`/`#6a7192`，内缩约 0.9）+ 端面法兰（`#2a2f45`/`#3f445c`） |

## 1. 现状问题清单（读码定位结果）

- [x] P1 依赖外部全局渐变：L99 槽体 `fill="url(#gEquip)"`（`gEquip` 定义在 `editor.html` L287）→ 步骤 1 改局部 `<defs>` + uid 解决
- [x] P2 颜色无兜底：全程直用 `p.color`（`p` 为 `null` 直接抛错）→ 步骤 1 加 `const c = (p && p.color) || '#9C99FF';`
- [x] P3 配色不符基准：`#161534` / `#1c1b40` / `#0f0e22` / `#0a0a1a` → 步骤 1~3 全面改基准配色（步骤 1 已覆盖槽体/内腔/法兰）
- [x] P4 无 `<defs>`、无 uid 防冲突（同页多实例只能共用全局 `gEquip`）→ 步骤 1 新增双渐变 + uid 后缀
- [x] P5 无 `clipPath`：螺旋叶片（`scale` 动画）与物料粒子（`cx` 动画）无裁剪，缩放后飘到槽体外 → 步骤 5 加内腔裁剪解决
- [ ] P6 定尺硬编码：`endW = 24`（固定）、`boltCount = max(3, floor(bodyW/22))`、`bladeCount = 9`、`pCount = 5`、`bladeRx/bladeRy` 等不随尺寸自适应；极小尺寸（如 30×30）下 `bodyW = w-endW*2-6` 会变成**负值** → 步骤 4 逐量 clamp + 负值守卫（步骤 1 已消除 `endW` 硬编码与负值，剩余项步骤 4 收敛）
- [x] P7 无接管三件套：进料口是 `w*0.3` 处的硬编码小方块（L108-110，无管腔/法兰、无斗体），出料是左下角小三角（L112-113）→ 步骤 3 改为「进料斗（流入式）+ 出料接管三件套」
- [x] P8 **出料口与端口不对齐**（结构性缺陷）：`outlet` 端口定义在 `x=1, y=0.75`（右缘），模板却把出料画在**左下角**（`endW+4`）；`inlet` 端口在 `0.3w` 顶面，模板小方块中心也是 `w*0.3` 但无收口结构 → 步骤 3 按端口坐标重画，使画法中心线与 `ports` 零偏差
- [x] P9 动画几何写死、无裁剪：`dur="2.2s"` 叶片 `scale` 动画 + `dur="3s"` 粒子 `cx` 动画，`begin` 相位写死；叶片 `rx/ry` 与粒子 `cy±bodyH*0.18` 不随尺寸收敛 → 步骤 5 改为「槽内平移螺旋牙 + 裁剪内粒子」，几何全部由尺寸推导
- [x] P10 首尾轴心与左右端口不同轴：`cy = bodyY+bodyH/2 = 0.54h`，而 `left`/`right` 端口在 `y = 0.5h`，接轴管线会错位 → 步骤 1 令槽体竖向居中（`bodyY = h*0.5 - bodyH/2`）使轴心严格 `0.5h`

## 2. 必须保持的对外契约（不得改动）

- 模板 key：`TEMPLATES.screwConveyor`
- `name: '螺旋输送机'`、`category: '设备'`、`defaultSize: { w: 200, h: 90 }`
- `ports`（原样保留，四个）：
  - `inlet (0.3, 0, up)` 顶部进料 → 画法：进料斗斗口中心 `x = 0.3w`，斗口顶沿 `y = 0`
  - `outlet (1, 0.75, right)` 右端出料 → 画法：水平出料接管中心线 `y = 0.75h`，管口贴 `x = w`
  - `left (0, 0.5, left)` 尾端轴头 → 画法：螺旋轴心线 `y = 0.5h`，端面贴 `x = 0`
  - `right (1, 0.5, right)` 驱动端轴头 → 画法：螺旋轴心线 `y = 0.5h`，端面贴 `x = w`
- 动画标签形式：`<animate>` / `<animateTransform>`（编辑态由 `editor.js stripSMIL()` 剥离，故动画元素须写成闭合标签）
- 本组件**不接** `runTag` / `_run`（`editor.js refreshScrewConveyorRun()` 只针对 `screwConveyorLite`，本轮不改 `editor.js`），动画恒运行、编辑态被 `stripSMIL()` 剥离

## 3. 分步执行

### 步骤 1：槽体主体对齐风格（含轴心对齐端口）— 已完成
- [x] 1.1 新增局部 `<defs>`：`metalId`（竖向，给横向槽体）、`metalVId`（横向，给端板/进料斗/出料管），id 带 uid 后缀（`scw_metal_*` / `scw_metalv_*`）
- [x] 1.2 槽体改金属渐变 + `#3f445c` 描边，内腔 `#12162b` + `#6a7192`；删除 `url(#gEquip)` 依赖
- [x] 1.3 颜色兜底 `const c = (p && p.color) || '#9C99FF';`
- [x] 1.4 槽体竖向居中：`bodyY = h*0.5 - bodyH/2`，使螺旋轴心 `cy = 0.5h`，与 `left`/`right` 端口同轴
- [x] 1.5 螺旋轴 + 螺旋牙静态重画：轴 `#6a7192`，牙 `#9aa2bc`，螺距按槽高/槽长自适应（`pitch` clamp）；牙道数 = `max(2, round(bodyW/pitch))`，7 档实测 4~5 道
- [x] 1.6 槽体上下法兰条（`#2a2f45` + `#3f445c`）+ 标准节把合法兰（`nSec = max(3, round(bodyW/(bodyH*1.9)))` 自动增多）+ 法兰螺栓（上下双排 `#2a2f45` + `#3f445c`，半径 clamp）
- [x] 1.7 校验：`node --check` 退出码 0；7 档冒烟 `ALL OK`；浏览器 12 格核对页通过
- [x] 1.8 **提前落地**（原属步骤 2）：左右端板（`url(#metalVId)` + `#3f445c`），端面贴 `x=0` / `x=w`；板上带座轴承 `#12162b`/`#6a7192` + 轴头 `${c}`，`hubR` 受 `endW*0.5-0.5` 约束（防越出组件边界）

### 步骤 2：驱动端链传动 — 已完成
- [x] 2.1 驱动端（右）链传动：双链轮 + 环形链条（`stroke-dasharray` 链节）+ 轮辐，整体收在端板高度内，全部随 `bodyH` 定尺
- [x] 2.2 校验：`node --check` 退出码 0；冒烟 `NaN=0`、链轮不越出包围盒；浏览器目视

> 注：原计划的「左右端板 / 尾端带座轴承」已提前在步骤 1 落地（见 1.8），步骤 2 只保留驱动端链传动。

### 步骤 3：进料斗与出料接管（端口同轴）— 已完成
- [x] 3.1 进料斗（流入式喂料）：梯形金属斗体 + 深色内腔 + 斗口口沿；斗口中心严格 `0.3w`、顶沿 `y=0`、收口落在槽顶法兰面；删除旧的 `w*0.3` 硬编码小方块
- [x] 3.2 右端出料接管三件套：金属管壁 + `#12162b` 管腔 + `#2a2f45` 端面法兰；中心线严格 `y = 0.75h`、管口贴 `x = w`；删除旧的**左下角出料小三角**（与 `outlet` 端口矛盾，见 P8）
- [x] 3.3 校验：`node --check` 退出码 0；结构化断言 `dInlet = dOutlet = 0.00`（斗口中心 / 出料管中心线与端口坐标零偏差）；`ports` 原样未变；浏览器目视

### 步骤 4：定尺封顶与极小尺寸守卫 — 已完成
- [x] 4.1 逐量 clamp：槽壁厚 `wall`、法兰厚 `flH`、端板宽 `endW`、轴径 `shaftW`、牙宽 `flightW`、螺栓半径、轴承座半径、链轮半径、进料斗口宽/斗壁厚、出料管径/管壁厚
- [x] 4.2 负值守卫：`bodyW = Math.max(6, w - bodyX*2)`、斗口宽/收口宽下限、出料管长 `Math.max(4, w - root)`、端板与槽体净空 `gap` 的 `Math.max(0, …)` 让位约束
- [x] 4.3 数量自适应：螺旋牙道数、把合法兰数量、螺栓数量按槽长/槽高推导（有上下限）；把合法兰新增「净空上限」防止窄高时挤成一团
- [x] 4.4 校验：`node --check` 退出码 0；**12 档**冒烟（实机 200×90 / 55×54 / 30×30 / 60×300 / 300×120 / 600×600 / 110×60 / 20×20 / 20×200 / 200×20 / 24×20 / 20×90）无 `NaN` / 无负值 / 无越界 / 两条结构断言通过；浏览器 16 格核对页 `getBBox()` 0 越界

> 注：本步骤前查证真实尺寸下限 —— `editor.js` L1224 `const MIN = 20;`（拖拽缩放手柄）+ L1611-1612 属性面板 `Math.max(20, …)`，故组件最小可达 **20×20**，冒烟档位由原计划 7 档扩到 12 档以覆盖下限边界。

### 步骤 5：clipPath 防溢出与 SMIL 动画 — 已完成
- [x] 5.1 `clipPath`：槽体内腔边界（`bodyX+wall ~ bodyX+bodyW-wall`，`innerTop ~ innerBot`），约束螺旋牙/螺旋轴/物料粒子
- [x] 5.2 螺旋牙 SMIL：`<animateTransform type="translate">` 沿轴平移一个螺距（`from 0 0 → to ±pitch 0`），`reverse` 时反向；牙左右各冗余一道，滑动时始终铺满槽体
- [x] 5.3 物料粒子 SMIL：数量按槽长自适应，`cx` 沿轴流动 + `opacity` 淡入淡出 + 轻微 `cy` 摆动，负 `begin` 错相；全部包在裁剪组内
- [x] 5.4 校验：`node --check` 退出码 0；冒烟 `anim` 计数稳定、`stripSMIL()` 后 `<animate` 计数为 0、剪裁 id 定义与引用成对；浏览器目视运行/编辑两态

### 步骤 6：整体校验与产物清理 — 已完成
- [x] 6.1 `node --check templates/screwConveyor.js` 退出码 0；`GetDiagnostics` 无告警
- [x] 6.2 浏览器目视：默认 200×90、极小 20×20（真实下限）、细高 60×300、扁平 200×20、自定义色兜底
- [x] 6.3 编辑态 `stripSMIL` 生效（`animate` 计数 0）；`editor.html` 实机载入项目目视
- [x] 6.4 镜像 `mirrored` / 旋转 `rotation` 正常；四个 `ports` 命中对应结构
- [x] 6.5 清理临时核对页与冒烟脚本

## 4. 进度记录

| 步骤 | 状态 | 备注 |
| --- | --- | --- |
| 步骤 1 | 已完成 | 槽体主体 + 端板/带座轴承（1.8 提前落地）；`check=0`、7 档冒烟 `ALL OK`、浏览器核对页通过 |
| 步骤 2 | 已完成 | 驱动端链传动（双链轮 + 闭合链条）；`check=0`、7 档冒烟 `ALL OK`、浏览器 22 组 `rotate` 计数正确、包围盒 0 越界 |
| 步骤 3 | 已完成 | 进料斗（流入式）+ 出料接管三件套；`check=0`、7 档冒烟 `ALL OK`（`hopper=1`/`outlet=1`/`dInlet=dOutlet=0.00`）、浏览器 12 格端口命中 |
| 步骤 4 | 已完成 | 定尺封顶补齐（`gap` 净空让位 + `nSec` 净空上限）；`check=0`、**12 档**冒烟 `ALL OK`、结构断言（`gapRT≥1.00`、`minJd≥2.5×法兰宽`）全过、浏览器 16 格 `getBBox()` 0 越界 |
| 步骤 5 | 已完成 | `clipPath` 内腔裁剪 + 螺旋牙平移（冗余牙铺满）+ 物料粒子错相流动，牙/粒子同组裁剪；`check=0`、12 档冒烟 `ALL OK`、浏览器 16 格 `anim/clipG/partInClip/clipIn` 全过、双帧采样确认牙与粒子同步位移且 `reverse` 反向 |
| 步骤 6 | 已完成 | 整体校验与产物清理；`check=0`、12 档冒烟 `ALL OK`、`preview.html?doc=` 8 组件可见几何零越界 + 四端口 `0.00` + 镜像/旋转/`reverse` 变换正确、`editor.html` 编辑态 `anim=0`，临时产物已删 |

## 5. 校验记录

### 步骤 1（槽体主体对齐风格）

- `node --check templates/screwConveyor.js` → `check=0`
- `node _scw_smoke.js`（7 档：200×90 / 55×54 / 30×30 / 60×300 / 300×120 / 600×600 / 110×60）→ **ALL OK**
  - `dAxis=0.00`（轴心与 `left`/`right` 端口零偏差）、`neg=0`、`over=0`
  - 每档 `plates=2`、`teeth=4~5`、`joints=2`、`bolts=4~48`；`gEquip=0`、`NaN`/`undefined` 均为 0
  - 自定义色 `#FF6600` 生效（轴头圆）；`scw_metal_*` / `scw_metalv_*` 均被引用
- 浏览器核对页 `_scw_check.html`（12 格，带 `?t=` 防缓存）→ 通过
  - 每格 `<animate>` 计数 0（本步尚无动画）、`gEquip` 未出现；全页 26 组渐变 id **无重复**
  - `getBBox()` 包围盒检查：12 格中仅旋转 90° 格（`rotate(90,100,45)`，设计使然）越界，其余 0 越界
- **修正记录**：首轮包围盒检查发现 55×54 / 60×300 两档**右端轴承圆越出组件右边界 0.25~0.3**（`hubR` 只受槽高约束）→ 改为 `hubR = Math.max(1.2, Math.min(4.5, bodyH*0.20, endW*0.5 - 0.5))`，复检 0 越界。
  （冒烟脚本的 `over` 检查带 1px 描边余量，未捕获该 0.3 越界；浏览器 `getBBox()` 为几何包围盒、无余量，故由浏览器检查捕获。）
- 结论：**P1 / P2 / P3（槽体部分）/ P4 / P10 已解决**；剩余 P3 细节、P5~P9 由后续步骤收敛。

### 步骤 2（驱动端链传动）

- `node --check templates/screwConveyor.js` → `check=0`
- `node _scw_smoke.js` 7 档 → **ALL OK**；`anim=2`（两个链轮各一个 `animateTransform`）、`neg=0`、`over=0`、`dAxis=0.00`
  - 链轮与链环全部随 `bodyH` 定尺：`spR1 = max(2, min(bodyH*0.30, endW*0.32))`、`spR2 = max(1.4, spR1*0.62)`；端板过扁时按 `kSp = upRoom/needH` 整体收缩（`needH = R1 + 2*R2`）
  - 极小尺寸守卫 `R1 < 1.6 || R2 < 1` 时整段省略（7 档实测未触发，最小档 30×30 仍为 `R1=2.0 / R2=1.4`）
- 浏览器核对页 12 格 → 通过
  - `rot = 22`（11 个非编辑态格 × 2 链轮），编辑态格 `rot = 0`（`stripSMIL` 生效）；`NaN=0`、`undefined=0`
  - `getBBox()` 包围盒：12 格中仅旋转 90° 格越界（设计使然），其余 `L/T/R/B` 均在 `viewBox` 内、0 越界
- **修正记录（闭合链条底弧 sweep 方向）**：初版底弧写成 `sweep-flag=1`，链条路径走向为**逆时针**（右上行 → 顶弧向左 → 左下行 → 底弧向右），底弧须与顶弧同为 `sweep-flag=0`；写成 `1` 会让底弧向内拱起、看起来链条没有绕过从动链轮。
  - 判据：链环 `getBBox()` 高度 —— 正确写法 `h = 2R1 + 2R2 ≈ 22.0`（实测 21.52，默认 200×90）；若底弧反向只有 `h = R1 + 2R2 ≈ 15.2`，数值可明确区分。
  - 已把该走向与 sweep 取值写进代码注释，防止后续误改。
- 结构断言（浏览器取 `d` / `circle`）：默认 200×90 链环 `M 196.18 45 L 193.60 34.01 A 4.21 → 185.19 34.01 L 182.61 45 A 6.79 Z`，从动链轮 `(189.40, 45.00, r6.79)` 与 `right` 端口（`x=w, y=0.5h`）同轴，主动链轮 `(189.40, 34.01, r4.21)` 正上方；尾端轴承座 `(10.60, 45.00, r4.50)` 与 `left` 端口同轴
- 与基准一致性：渲染次序（链环先画、链轮后画）、链轮配色（`#12162b` 轮盘 + `#6a7192` 描边 + `#5b6280` 六辐条 + `#6a7192` 轮毂 `r*0.26` `opacity 0.85`）、链节 `stroke-dasharray="2.6 1.4"`、同径链轮同向旋转，均与 `bucketElevator.js` L48-51 / L94-97 / L237-241 逐项对齐
- 结论：步骤 2 完成，**P6 中「链轮半径硬编码」一项已由本步收敛**。

### 步骤 3（进料斗与出料接管）

- `node --check templates/screwConveyor.js` → `check=0`
- `node _scw_smoke.js` 7 档 → **ALL OK**；每档 `hopper=1`、`outlet=1`、`dInlet=0.00`、`dOutlet=0.00`、`neg=0`、`over=0`
  - `dInlet` 检测：取 `polygon[fill=url(#scw_metalv_*)]`（斗体唯一）的 `getBBox()` 中心 `x` vs `0.3w`
  - `dOutlet` 检测：取 `rect[fill=url(#scw_metal_*)]` 且右缘贴 `x=w` 的（即出料管管壁）中心 `y` vs `0.75h`
- 浏览器核对页 12 格（`?t=` 防缓存）→ 全页 `svg=13`（含 1 个 16×16 辅助 svg）、`hopper=12`、`anim=22`、`NaN=0`、`undefined=0`
  - 逐格 `getBBox()` 端口命中（12 格全中，偏差 `0.00`）：
    - 进料斗斗口中心 `x` = `0.3w`，实测 `60.00/60.00`（200×90）、`9.00/9.00`（30×30）、`18.00/18.00`（60×300）、`180.00/180.00`（600×600）
    - 进料斗斗口顶沿 `y` = `0`，12 格全为 `0.00`
    - 出料管右缘 `x` = `w`，实测 `200.00/200`、`30.00/30`、`60.00/60`、`600.00/600`
    - 出料管中心线 `y` = `0.75h`，实测 `67.50/67.50`（200×90）、`22.50/22.50`（30×30）、`225.00/225.00`（60×300）、`450.00/450.00`（600×600）
  - 竖直长圆环式的**左端尾轴承座**（`polygon` 斗体 + 收口）与右端**出料管**在 12 格中均单独成对出现（`n=1`，即仅出料管管壁满足「右缘贴 `x=w`」，无误判）
- 画法要点（照搬 `screwConveyorLite.js` L78-127）：
  - 进料斗：`inCX = w*0.3`、`mouthY = 0`、`mouthW = max(6, min(w*0.16, bodyH*1.5))`、`throatW = mouthW*0.42`、`throatY = bodyY - flH*0.5`（收口落槽顶法兰面）、`inWall = max(0.8, min(1.6, bodyH*0.09))`、`opW = throatW*0.55`
    - 四件：槽顶进料口（`#0c1020`/`#6a7192`）→ 斗体梯形（`url(#metalVId)`/`#3f445c`）→ 斗内腔（`#12162b`/`#6a7192`，四边内缩 `inWall`）→ 斗口口沿（`#2a2f45`/`#3f445c`，高 `flH`）
  - 出料接管：`outCY = h*0.75`、`outH = max(3.2, min(bodyH*0.55, h*0.14))`、`outXL = bodyX + bodyW`（根部贴右端板）、`outLen = max(4, w - outXL)`、`outWall = max(0.8, min(1.6, outH*0.18))`、`outTop = outCY - outH*0.5 - 1.2`、`outHH = outH + 2.4`
    - 三件：管壁（`url(#metalId)`/`#3f445c`）→ 管腔（`#12162b`/`#6a7192`，内缩 `outWall`，**右端不单独收口**，端口面即法兰面）→ 端面法兰（`#2a2f45`/`#3f445c`，宽 2.2 贴 `x=w`）
  - 渲染次序：`defs → 槽体 → 内腔 → endPlates（尾轴承座 + 链传动 + 右端板）→ 螺旋牙/轴 → 上下法兰条 → 把合法兰 → 螺栓 → 进料斗 → 出料管`
  - 物料流向自洽：斗口在槽顶敞开（流入式喂料，非管道进料）→ 沿斗壁下滑收口 → 从槽顶进料口喂入 → 螺旋轴推送 → 右端下部出料管排出
- 结论：**P7 / P8 已解决**；四端口画法中心线与 `ports` 坐标零偏差（`dAxis` / `dInlet` / `dOutlet` 均为 `0.00`）。

### 步骤 4（定尺封顶与极小尺寸守卫）

- 前置查证：`editor.js` L1224 `const MIN = 20;`（拖拽缩放手柄下限）、L1611-1612 属性面板 `comp.w = Math.max(20, …)` → 组件真实可达下限为 **20×20**（原计划按 30 假定，档位据此扩到 12 档）。
- 退化探针 `_scw_probe.js`（把模板定尺公式复制一份逐档打印 + 退化判据）扫 9 档（`20×20 / 20×30 / 24×20 / 30×20 / 20×200 / 200×20 / 40×20 / 20×40 / 200×90`），定位到 **2 处真实退化**（其余 clamp 项在 20×20 档均未触发判据）：
  1. **`w≈20` 时槽体压住右端板**：`endW` 下限 6 与 `gap` 下限 1.5 双重下限叠加，使 `bodyX + bodyW > w - endW`。
     - 修正：`gap = Math.min(Math.max(1.5, Math.min(3, bodyH*0.09)), Math.max(0, (w - endW*2 - 6)/2));`
     - 验算：`20×20` → `endW=6, gap=1, bodyX=7, bodyW=6`，槽体右缘 13 < 右端板左缘 14。
  2. **`20×200` 时把合法兰挤成一团**：`bodyW=6`、`bodyH=76`，`nSec = max(3, round(6/144.4)=0) = 3`，两条法兰塞进 6 宽的槽体。
     - 修正：`jointW = Math.max(1.4, Math.min(3.2, bodyH*0.15));` 前置，`nSec = Math.min(Math.max(3, Math.round(bodyW/(bodyH*1.9))), Math.max(1, Math.floor(bodyW/(jointW*2.5))));`（窄高时压到 1 节，即不出中间法兰）。
- 上述两条几何关系**固化为冒烟断言**（从渲染出的 SVG 属性反查，不重算模板公式，避免与实现同源出错）：
  - ① 取 `fill` 含 `scw_metal_*` 的第一个 `rect` 为槽体、取 `fill` 含 `scw_metalv_*` 的最大 `x` 为右端板左缘，断言 `右端板左缘 − 槽体右缘 ≥ 0`；
  - ② 取 `fill=#2a2f45` 且 `height > 0.2h` 的 `rect` 为把合法兰，断言相邻法兰中心节距 `≥ 2.5 × 法兰宽`。
- `node --check templates/screwConveyor.js` → `check=0`
- `node _scw_smoke.js` **12 档** → **ALL OK**，逐档 `neg=0`、`over=0`、`nan=0`、`dAxis=0.00`、`dInlet=0.00`、`dOutlet=0.00`、`hopper=1`、`outlet=1`、`plates=2`
  - 极小/下限档实测：`20×20`（`teeth=4 joints=1 bolts=4 anim=0`）、`20×200`（`joints=0` 即按净空上限压到 1 节）、`20×90`（`joints=0`）、`24×20`（`anim=0`）、`200×20`（`teeth=16 joints=13 bolts=60`，扁长档法兰按 `1.9` 槽径节距铺满）
  - `anim=0` 表示该档驱动端链传动按极小守卫（`R1 < 1.6 || R2 < 1`）整段省略 —— 设计内行为
- 浏览器核对页扩到 **16 格**（新增 `20×20 / 20×200 / 200×20 / 20×90`）→ 全页 `nan=false und=false`
  - 逐格「槽体右缘 vs 右端板左缘」净空实测：`3.01`（200×90）、`1.50`（30×30）、`1.84`（55×54）、`2.05`（110×60）、`3.00`（60×300 / 300×120 / 600×600）、**`1.00`（20×20 / 20×200 / 20×90）**、`1.50`（200×20）—— 全档 `> 0`，无压盖
  - 逐格 `getBBox()` 联合包围盒：`ovL = ovR = ovT = 0.00`（精确贴边、无外溢），`ovB` 全部为负（未触底边，保留尾部余量）
  - 结构件计数随尺寸自适应：`rect` 数 `13`（常规档）/ `11`（`20×20`、`20×200`、`20×90`，省略链传动与把合法兰）/ `23`（`200×20`）
- 结论：步骤 4 完成；**P6 中「`endW` 硬编码与 `bodyW` 负值」已在步骤 1 消除，「数量硬编码」剩余项（螺旋牙道数）已在步骤 1.5 收敛**，本步补齐端板净空与把合法兰密度两处极小尺寸守卫。

### 步骤 5（clipPath 防溢出与 SMIL 动画）

- 实现（照搬 `screwConveyorLite.js` 的动画范式，裁剪范围改用内腔边界）：
  - 新增 `clipId = 'scw_clip_'+uid`；`<defs>` 内 `clipPath` 取 **槽体内腔**：`x = bodyX+wall`、`y = innerTop`、`w = bodyW−2·wall`、`h = innerBot−innerTop`（本组件端板在槽体之外，故不能用简化版的 `bodyX+epW`）
  - 螺旋牙改为**冗余铺满**：`pitch = max(bodyW/16, min(bodyW/4, bodyH*1.05))`、`skew = pitch*0.38`、`toothStart = bodyX−pitch`、`toothEnd = bodyX+bodyW+pitch`、`flightW = max(0.9, min(1.8, bodyH*0.10))`；左右各多出一道冗余牙，平移一整个周期内槽体始终被牙铺满、看不出接缝（冗余段由裁剪组裁掉）
  - 螺旋推进：`<animateTransform type="translate">` `from="0 0"` → `to="±pitch 0"`（`moveTo = reversed ? -pitch : pitch`）、`dur = spDur = 1.6s`（与链轮同周期、同向）
  - 物料粒子：`pR = max(0.55, min(1.5, bodyH*0.08))`、`pCount = max(2, min(6, round(bodyW/pitch)))`、`pY = cy + bodyH*0.13`；每粒三组 `animate`（`cx` 平移一个螺距 + `cy` 轻微摆动 + `opacity` `0.25→0.55→0.25` 呼吸），`begin = -(spDur*i/pCount)` 负错相形成连续料流
  - **牙与粒子同处一个 `<g clip-path="url(#clipId)">` 组**，平移过程中都不会露到槽体外
- `node --check templates/screwConveyor.js` → `check=0`
- `node _scw_smoke.js` **12 档** → **ALL OK**，逐档 `neg=0`、`over=0`、`dAxis/dInlet/dOutlet=0.00`、`hopper=1`、`outlet=1`、`plates=2`、`clip=true/true`、`stripSMIL` 后残留 `left0`
  - `anim` 计数：常规档 `15`（1 牙平移 + 2 链轮旋转 + 12 粒子属性动画）、`300×120` `18`（5 粒→15）、`600×600` `15`、`200×20` `19`（6 粒→18 + 1）、`20×20`/`24×20` `13`（链传动被极小守卫省略 → 1 + 12）
  - 冗余牙后牙数上调（步骤 4 记录中的 `teeth` 为冗余前数值）：`teeth=7`（常规档）、`8`（`300×120`）、`19`（`200×20`）
- 冒烟新增断言（同样**从渲染结果反查**，不重算模板）：
  - ③ `clipPath` 定义存在且**被引用**，且裁剪矩形完整落在槽体矩形内（`clipIn`）；
  - ④ 螺旋牙左右两端冗余各 `≥0.4 × 螺距`（由牙中心线序列估螺距，再比对槽体左右缘）；
  - ⑤ 物料粒子 `≥2` 个；⑥ 粒子必须落在裁剪组内（`<g clip-path=…>…values="0.25;0.55;0.25"…</circle></g>`）；⑦ 裁剪组引用数 `=1`
  - 包围盒越界检查**跳过螺旋牙**（`line` 且 `stroke=#9aa2bc`）：冗余段静态坐标本就落在槽体外，由裁剪组裁掉，其越界由断言 ③④ 覆盖，避免误报
- 浏览器核对页 **16 格**（`?t=` 防缓存）逐格实测：
  - `clipG=1`、`clipIn=true` 全 16 格；`partInClip`（裁剪组内粒子数）= `4`（常规档）、`5`（`300×120`）、`6`（`200×20`）
  - `anim` = `15` 全常规档、`18`（`300×120`）、`19`（`200×20`）、`13`（`20×20`）、**`0`（编辑态 `stripSMIL` 格）** → 编辑态剥离干净
  - 双帧采样（间隔 1s，默认 `200×90`）：牙平移 `helixE 16.519 → 11.281`、粒子 `p1cx 59.669 → 54.431`，**两者位移完全一致（Δ = 5.238）** → 牙与物料严格同步
  - `reverse` 对比：默认档 `helixE` 落在 `0 ~ +pitch`（`21.383` 采样）、反向档落在 `0 ~ −pitch`（`−25.493` 采样）→ 平移方向确实相反
- 过程中发现并修正的问题：
  - 粒子最初与裁剪组**并列**（`${helix}` 与 `${parts}` 同级），注释却写「包在裁剪内」——与 5.3 要求不符；已改为牙与粒子共用同一裁剪组。
  - 修正时一度出现 `${parts}` **重复插入**（`anim` 由 `15` 涨到 `27`），浏览器 `partInClip` 采样暴露；已删除并加断言 ⑥⑦ 固化该约束。
- 结论：步骤 5 完成；**P5（无 `clipPath`、溢出）与 P9（动画几何写死）已解决** —— 动画几何全部由尺寸推导，牙/粒子均受内腔裁剪约束。

### 步骤 6（整体校验与产物清理）

- `node --check templates/screwConveyor.js` → `check=0`；`GetDiagnostics` → 无告警
- `node _scw_smoke.js` **12 档** → **ALL OK**（`clip=true/true`、`neg=0`、`over=0`、`dAxis/dInlet/dOutlet=0.00`）
- 实机几何校验：确认 `projects/1#.json` 中螺旋输送机**全部为 `screwConveyorLite`（7 处）**、无 `screwConveyor` 实例，故改用两条实机路径：
  - `preview.html?doc=`（自造 8 组件 doc，`?t=` 防缓存）：`scA 200×90` / `scB 20×20`（真实下限）/ `scC 60×300` / `scD 200×20` / `scE reverse` / `scF mirrored` / `scG rotation 90` / `scH 自定义色 #FF7A45`
    - **可见几何（排除裁剪组内冗余牙）零越界**：8 档逐档 `box` 均落在 `[0,0,W,H]` 内 → 全 `IN`（`scA n=52`、`scB n=22`、`scC n=44`、`scD n=90`…）
    - **四端口全部命中**：8 档统一 `dAxis=0.00`（轴心线）、`dInletX=0.00 / inletY=0.00`（进料斗顶沿）、`dOutletY=0.00 / outletRight=0.00`（出料接管中心与右缘）
    - **镜像 / 旋转变换正确**：`scF` 组内 `inner=translate(200,0) scale(-1,1)`，其余档 `inner=-`；`scG outer=rotate(90 100 45)`
    - **`reverse` 反向确认**：正向 `helixTo=35.91 / spokeTo=360`，反向 `helixTo=-35.91 / spokeTo=-360` → 螺旋牙平移与链轮旋转同时反向
  - `editor.html` 编辑态实机：`TEMPLATES.screwConveyor` 存在、`stripSMIL` 为 function；`render(200,90)` → `runAnim=15 / editAnim=0`，`render(20,20)` → `runAnim_min=13 / editAnim_min=0`，`animateTransform` `3 → 0`，自定义色 `#FF7A45` 命中；再以 `doc.components.push(...) + renderAll()` 注入实测 → 生成组 `anim=0 / animT=0`（**编辑态动画剥离干净**），随后已移除注入组件
- 过程中发现并修正的问题：
  - 首轮包围盒判定「全 OVER」——`getBBox()` 把**裁剪组内被裁掉的冗余螺旋牙**（静态坐标本就在槽体外）计入包围盒，且当时未取到 `window.doc` 的 `w/h`；改为**排除 `clip-path` 祖先链内元素**后再求联合包围盒，并以组内最后一个子元素（透明命中框 `rect`）的 `width/height` 作为 W/H → 8 档全 `IN`。
- 澄清（承接上一轮疑问）：`editor.html` 一次性 evaluate 出现 `hasTpl:false` 系**时序问题**——该页经 `document.write` 动态注入 `templates.js`/`editor.js`，注入完成前查询必然取不到；等待后重测 `T=object / hasSC=1 / strip=function`，模板装载与 `stripSMIL` 均正常。
- 产物清理：删除临时文件 `_scw_smoke.js`、`_scw_check.html`、`_scw_probe.js`。
- 结论：**步骤 6 完成，螺旋输送机 `screwConveyor` 优化整体收口**；至此 P1~P10 全部解决（P1/P2/P3/P4/P10 见步骤 1、P5/P9 见步骤 5、P6/P7/P8 见步骤 3）。

