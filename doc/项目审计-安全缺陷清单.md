# 项目审计 · 安全缺陷清单（大厂标准）

> 审计日期：2026-09-22
> 审计维度：**维度一 · 安全审计**（对应《[项目审计-待补维度占位说明.md](./项目审计-待补维度占位说明.md)》维度一）
> 审计范围：`server.js`、`editor.js`、`editor.html`、`preview.html`、`embed-demo.html`、`Dockerfile`、`docker-compose.yml`、`.github/workflows/`
> 审计方式：**只读审计 + 本地实例 PoC 验证**（在 18091/18092/18093 端口起独立实例复现，未触碰 192.168.1.78 部署实例）
> 严重级别：**P0 严重**（数据丢失/崩溃/可利用安全漏洞/必然劣化）· **P1 高** · **P2 中** · **P3 低**
> 交叉引用：与已完成《[项目审计-架构与代码质量缺陷清单.md](./项目审计-架构与代码质量缺陷清单.md)》重复的条目只做引用与安全面补充，不重复展开

---

## 0. 结论摘要

**发现 4 个 P0 级可利用漏洞，其中 2 个已用 PoC 实证可在一条未认证请求内触发：**

| PoC | 请求 | 实测结果 | 危害 |
|---|---|---|---|
| 任意文件读取 | `GET /%2e%2e%2fscada_layout_editor.html` | **200 / 13914 字节**，读到 web 根目录**之外**的文件 | 读取服务器任意可读文件（源码、配置、密钥、项目库） |
| 绕过 `/projects` 403 | `GET /../RS/projects/1%23.json` | **200 / 34820 字节**（同文件直连 `/projects/` 返回 403） | 项目库隔离形同虚设 |
| 远程 DoS | `GET /%` | `URIError: URI malformed` → **进程退出**，后续请求 `http=000` | 一条请求打死服务，可反复触发 |
| 无鉴权 | 任意 `GET/DELETE /pfd-api/projects/xxx.json` | 无需任何凭证即可列目录、读、写、删 | 全项目库任意读写删除 |

**根因集中在 `server.js` 静态文件服务与项目库接口两处**：`decodeURIComponent(req.url)` 的结果既没做异常保护，也没做"解析后路径必须落在 web 根内"的收敛校验。

---

## 1. 任意文件读取 / 路径穿越

| # | 位置 | 缺陷描述 | 大厂标准 | 级别 |
|---|---|---|---|---|
| 1.1 | `server.js` L206 `decodeURIComponent(req.url.split('?')[0])` + L231–247 `path.join(ROOT, urlPath)` → `sendFile()` | **路径穿越（任意文件读取）**：`urlPath` 直接来自 URL 且已先做 `decodeURIComponent`，随后 `path.join(ROOT, urlPath)` 会归一化 `..` 并**逃出 ROOT**，全程没有任何"结果必须位于 ROOT 之内"的校验 | OWASP A01 Broken Access Control / CWE-22；静态服务必须先 `path.resolve` 再用 `resolved.startsWith(ROOT + path.sep)` 收敛，越界一律 403 | **P0** |
| 1.2 | 同上，PoC | **实测**：`curl --path-as-is 'http://127.0.0.1:18092/%2e%2e%2fscada_layout_editor.html'` → `http=200 size=13914`，返回内容为项目根目录**同级目录**下的 HTML；`/%2e%2e%2fdigital_twin_system_spec.md` → `200 / 28978 字节` | 安全测试应覆盖编码变体（`%2e%2e%2f`、`..%2f`、双重编码） | **P0** |
| 1.3 | `server.js` L225–229（`/projects` 显式 403）vs L231+ 静态分支 | **防护可绕过**：`/projects/*` 被显式 403，但用 `/../RS/projects/x.json` 走静态分支即可读到同一文件（`reqPath` 不以 `/projects` 开头 → 绕过判断 → `path.join` 归一化后又指回 `ROOT/projects/x.json`）。**实测 `http=200 size=34820`** | 防护必须作用在**解析后的真实路径**上，而非原始 URL 字符串前缀 | **P0** |
| 1.4 | `server.js` L139–150 `sendFile()` | 404 响应体回显完整文件系统路径：`404 Not Found: D:\Java\GY\RS\projects\1`，泄漏服务器目录结构与部署位置 | 错误响应不得回显内部路径（信息泄漏），只回资源名与请求 ID | P2 |

## 2. 未捕获异常导致远程 DoS

| # | 位置 | 缺陷描述 | 大厂标准 | 级别 |
|---|---|---|---|---|
| 2.1 | `server.js` L205–206 `http.createServer((req,res)=>{ const reqPath = decodeURIComponent(...) })` | **畸形 URI 直接崩进程**：`decodeURIComponent` 对非法百分号编码抛 `URIError`，该调用位于请求处理器顶层、**无 try/catch**，Node 默认行为是打印堆栈并退出 | CWE-248 未捕获异常 / OWASP "fail safe"；所有来自请求的解析必须包裹并返回 400，且进程需有 `uncaughtException` 兜底与守护重启 | **P0** |
| 2.2 | 同上，PoC | **实测**：`curl --path-as-is 'http://127.0.0.1:18091/%'` → 进程退出，日志 `URIError: URI malformed at decodeURIComponent ... at Server.<anonymous> (server.js:206:19)`；随后同一实例 `/editor` 返回 `http=000`（连接被拒） | 崩溃必须可观测 + 自愈；关键路径应有存活探测 | **P0** |
| 2.3 | `server.js` L64–80 `readBody()` 超限分支 | 超限时 `reject()` + `req.destroy()`，调用方 catch 里再 `sendJSON(res,400)` 时 socket 已销毁，**客户端收到连接重置而非 413**，无法区分"太大"与"网络故障" | 超限应返回 413 + `Connection: close`，并保证响应先于连接关闭 | P3 |

## 3. 鉴权 / 授权缺失

| # | 位置 | 缺陷描述 | 大厂标准 | 级别 |
|---|---|---|---|---|
| 3.1 | `server.js` L82–137 `handleProjectsApi()` 全分支 | **项目库接口零鉴权**：GET 列目录/读文件、POST 写文件、DELETE 删文件，全程不校验任何身份（全仓无 `Authorization`/`token`/`cookie` 相关代码） | OWASP A01/A07；状态变更接口必须鉴权 + 授权，至少要求 Bearer Token 或同源校验 | **P0** |
| 3.2 | `server.js` L250 `server.listen(PORT, ...)` + `docker-compose.yml` L10 `"8090:8090"` | **默认绑定 0.0.0.0**：未指定 host，容器端口也映射到所有网卡 → 无鉴权接口直接暴露给整个局域网（与 1.1 叠加 = 局域网任意文件读取） | 默认只监听 `127.0.0.1`；对外暴露须显式声明并置于认证网关之后 | **P0** |
| 3.3 | `server.js` L113–121 POST 分支 | 写入**无并发/冲突控制**（后写覆盖先写）、无写入者标识、无审计日志（仅 `console.log` 一行） | 状态变更需审计留痕（谁/何时/改了什么），并做乐观锁或 ETag 防覆盖 | P2 |

## 4. 反向代理安全

| # | 位置 | 缺陷描述 | 大厂标准 | 级别 |
|---|---|---|---|---|
| 4.1 | `server.js` L154 `const targetUrl = API_TARGET + req.url`；L215 `req.url.startsWith('/api/')` | **`/api/` 前缀可被逃逸**：`new URL()` 会归一化 `..`，`/api/../editor` 最终请求后端 `/editor`，绕过后端任何基于前缀的访问控制。**实测**：`GET /api/../editor` 返回 404（后端自身的 `/editor` 路径响应），证明请求已离开 `/api` 前缀 | 代理应只放行白名单路径，且归一化后再次校验前缀 | P1 |
| 4.2 | `server.js` L176–178、L196–200 | **CORS 全开放**：代理响应与预检一律 `Access-Control-Allow-Origin: *` → 任意站点可借用户浏览器读取传感器数据，代理成为开放只读中继 | 明确 origin 白名单；凭据场景禁止 `*` | P1 |
| 4.3 | `server.js` L165–171 | **透传全部客户端请求头**（含 `cookie`、`authorization`、`x-forwarded-for`）：客户端凭据被转发给后端；`X-Forwarded-For` 可伪造，污染后端审计 | 请求头白名单（仅转发必要头），`X-Forwarded-*` 由代理侧重写 | P1 |
| 4.4 | `server.js` L173–192 | **无超时、无响应大小限制**：未设置 `proxyReq.setTimeout`/`server.timeout`，上游挂起即长期占用连接（慢速耗尽型 DoS）；`/api/*` 也无请求体大小限制 | 显式连接/读取超时 + 请求体上限 + 连接数限制 | P1 |
| 4.5 | `server.js` L175 `headers = {...proxyRes.headers}` | 原样回传上游全部响应头（含 `connection`/`transfer-encoding`），未做 hop-by-hop 头剥离 | 按 RFC 7230 剥离 hop-by-hop 头再回写 | P3 |

## 5. 前端 XSS 面 / 数据注入

| # | 位置 | 缺陷描述 | 大厂标准 | 级别 |
|---|---|---|---|---|
| 5.1 | `editor.js` L2348 `esc()` 只转义 `"` 与 `<`（缺 `&` `>` `'`）；`preview.html` L1531 版本完整 —— **交叉引用 §4.9** | 本次补充**可利用性评估**：全仓检索未发现"单引号属性 + 插值"组合，组件名称/位号标签走 `editor.js` L615/L625 的 `textContent` 赋值（天然安全），模板层未发现直接插值 `props.tag/name` 的位置 → **当前无可直接利用链**，但转义不完备属纵深防御缺口 | 统一转义工具覆盖 `& < > " '`，单一来源；禁止按上下文"够用就行" | P1（原文档评 P0，本次细化为 P1） |
| 5.2 | `templates/instrument.js` L13 `<text ...>${(p.tag||'??').slice(0,2)}</text>` | 仪表标签**未转义直接插入 SVG 文本**；当前被 `.slice(0,2)` 截断到 2 字符而无法构造完整 payload，属**潜在**注入点 | 所有进入标记的属性/文本一律转义，不依赖长度截断兜底 | P3 |
| 5.3 | `preview.html` L1586–1590 `window.addEventListener('message', e=>{ const m=e.data; if(!m\|\|m.source!=='pfd-host') return; ... PFDEmbed.load(m.doc) })` | **postMessage 不校验 `e.origin`**：任何页面只要 iframe 嵌了预览页，post `{source:'pfd-host',type:'load',doc}` 即可注入任意文档并触发渲染；`source` 字符串是公开常量，不构成防护 | 收发双向都必须校验 origin（白名单），并校验消息 schema | **P1** |
| 5.4 | `preview.html` L1559 `window.parent.postMessage({source:'pfd-embed',...}, '*')`；`embed-demo.html` L91/L96/L103/L106 同 | 发送侧 `targetOrigin='*'`：消息（含被选组件名）会发给任意父窗口 | 发送侧指定确切 targetOrigin | P2 |
| 5.5 | `preview.html` L670–676 `const qp = new URLSearchParams(...).get('doc'); if(qp) doc = JSON.parse(qp)` | **`?doc=` 无校验直接成为文档源**：构造一个含恶意 props 的链接即可注入（与 5.3 同源风险面）；无 schema 校验、无字段白名单（交叉引用 §10.5/§10.6） | 外部数据一律 parse→validate→normalize 后才可渲染 | **P1** |
| 5.6 | `editor.js` L50–52 `const _apiParam = new URLSearchParams(location.search).get('api'); const SENSOR_API_BASE = _apiParam \|\| ...` | **`?api=` 可改写数据源**：`editor.html?api=https://evil.example` 会让编辑器把传感器请求发往任意主机（浏览器侧请求伪造 / 内网探测 / 伪造数据源） | 可配置端点需白名单或同源约束，禁止 URL 参数任意改写 | P2 |
| 5.7 | `editor.js` L1261 `nm.innerHTML = '<div class="pa-name">' + t.name + '</div>'` | 组件库名称走 `innerHTML` 拼接；当前 `t.name` 来自模板常量（非用户输入）故不可利用，但同文件其余 40+ 处均用 `esc()`，此处不一致易被后续改动放大 | 统一 `textContent` 或强制 `esc()` | P3 |

## 6. HTTP 安全响应头

| # | 位置 | 缺陷描述 | 大厂标准 | 级别 |
|---|---|---|---|---|
| 6.1 | `server.js` L51–54 `sendJSON`、L139–150 `sendFile` | **全部安全响应头缺失**：无 `Content-Security-Policy`、`X-Frame-Options`、`X-Content-Type-Options`、`Strict-Transport-Security`、`Referrer-Policy`、`Permissions-Policy`（全仓检索为 0 命中） | OWASP Secure Headers；至少 `X-Content-Type-Options: nosniff` + `X-Frame-Options: SAMEORIGIN` + 基础 CSP | P1 |
| 6.2 | 同上（`X-Frame-Options` 缺失 + 5.3） | **组合风险**：无框架保护 → 预览页可被任意站点 iframe，叠加 postMessage 无 origin 校验 → 形成完整的跨站注入链 | 防御纵深：框架保护与消息来源校验必须同时具备 | P1 |
| 6.3 | `server.js` L21–31 `MIME` 映射 + L146 | 未知扩展名回落 `application/octet-stream` 尚可，但 `.svg` 以 `image/svg+xml` 同源提供 → 若站内存在用户可控 SVG，可直接执行脚本 | 用户内容不得以同源可执行类型提供；下载类强制 `Content-Disposition: attachment` | P2 |

## 7. 敏感信息与配置

| # | 位置 | 缺陷描述 | 大厂标准 | 级别 |
|---|---|---|---|---|
| 7.1 | `editor.js` L52、`server.js` L7/L19、`Dockerfile` L24、`docker-compose.yml` L14（共 5 处） | 内网后端地址 `http://192.168.1.78` 硬编码进源码与镜像（原说明文档记为"四处"，实际为 5 处），泄漏内网拓扑；换环境必须改源码或重新构建镜像 | 配置外部化（环境变量 + `.env.example`），代码中只留无敏感默认值 | P2 |
| 7.2 | 仓库根目录 `pfd-server*.log`、`pfd-py.log`、`pfd-editor-ubuntu24.tar.gz`、`doc/` 审计文档 | 运行日志与构建产物入库：日志含请求路径（`[PROXY] GET /api/...`）与部署路径，泄漏运行信息；tar.gz 增大仓库体积 | 日志与产物不入库（`.gitignore`），运行日志轮转与脱敏 | P2 |
| 7.3 | `server.js` L94 列表接口返回 `dir: PROJECT_DIR` | 接口回显服务器绝对路径 | 对外响应不暴露内部路径 | P3 |

## 8. 容器与供应链

| # | 位置 | 缺陷描述 | 大厂标准 | 级别 |
|---|---|---|---|---|
| 8.1 | `Dockerfile` L11 `FROM node:20-alpine` | 基础镜像未固定 digest（可变 tag），构建结果不可复现，上游 tag 被移动即引入未知变更 | 固定到 `node:20-alpine@sha256:...`；定期用 Renovate/Dependabot 升级 | P1 |
| 8.2 | `Dockerfile` 全文 | **缺 `HEALTHCHECK`**；无 `--init`（PID 1 信号处理）、无只读根文件系统、无 capability 收敛（`--cap-drop=ALL`） | 容器须有健康探针；最小权限：非 root + 只读根 + 能力裁剪 | P2 |
| 8.3 | `Dockerfile` L24 `ENV API_TARGET=http://192.168.1.78` | 环境地址烘焙进镜像层，镜像跨环境不可用且泄漏内网地址 | 镜像只留占位默认值，运行时注入 | P2 |
| 8.4 | `docker-compose.yml` L10 `"8090:8090"`、L7 `image: pfd-editor:latest`、无 `deploy.resources` | 端口映射到所有网卡（放大 3.2）；可变 `latest` 标签；无 CPU/内存限制（资源耗尽无隔离） | 显式 `127.0.0.1:8090:8090`；镜像用 digest/SHA；设置资源上限 | P1 |
| 8.5 | `Dockerfile` L15 `COPY . .` | 依赖 `.dockerignore`（已存在，覆盖 `.git`/日志/tar.gz）——**但未排除 `doc/`（含审计文档）与 `projects/`**，构建上下文仍携带非运行必需内容 | 白名单式 COPY（只复制运行必需文件） | P3 |
| 8.6 | `.github/workflows/ci.yml` L26/L29、`cd.yml`（`actions/checkout@v4`、`setup-node@v4`、`docker/setup-qemu-action@v3`、`setup-buildx-action@v3`、`login-action@v3`、`build-push-action@v6`） | **Actions 全部引用可变大版本标签**，未固定到 commit SHA → 上游被投毒即可在 CI 中执行任意代码（可读取 `secrets.GITHUB_TOKEN`、推送镜像） | 第三方 Action 固定到完整 SHA + 开启 Dependabot；发布流程加漏洞扫描（Trivy/Grype） | P2 |
| 8.7 | `cd.yml` L25–26 `permissions: contents: read / packages: write` | ✅ 权限已最小化（值得保留）；但缺少 SBOM 生成与镜像签名（cosign） | 产物需 SBOM + 签名，可追溯 | P3 |

## 9. DoS 与资源约束

| # | 位置 | 缺陷描述 | 大厂标准 | 级别 |
|---|---|---|---|---|
| 9.1 | `server.js` L87/L90 `fs.readdirSync`/`fs.statSync`（异步 handler 内同步 IO）——**交叉引用 §5.8** | 同步 IO 阻塞事件循环，项目目录变大即拖慢全部请求；安全面等同于低成本 DoS | 一律异步 fs；目录扫描加上限与缓存 | P1 |
| 9.2 | `server.js` L215–222 `/api/*` 代理 | 代理路径**无请求体大小限制**（10MB 限制只作用于项目库接口），大体积 POST 全量转发 | 全局请求体上限 + 流式转发 | P2 |
| 9.3 | `server.js` L250 | 未设置 `server.timeout`/`headersTimeout`/`requestTimeout`，无连接数上限 → 慢速攻击可长期占用连接 | 显式超时（headers/body/keepalive）+ 连接数限制 | P2 |
| 9.4 | `server.js` 全文 | **无结构化日志、无访问审计、无错误上报**：安全事件（异常请求、越权尝试）不可追溯 | 结构化日志（含请求 ID、来源 IP、状态码）+ 关键操作审计 | P2 |

---

## 缺陷统计

| 级别 | 数量 | 主要构成 |
|---|---|---|
| **P0** | **6** | 任意文件读取（1.1–1.3）、进程崩溃 DoS（2.1–2.2）、项目库零鉴权（3.1）、默认绑定 0.0.0.0（3.2） |
| P1 | 11 | `/api/` 前缀逃逸、CORS 全开放、请求头透传、代理无超时、`esc()` 不完备、postMessage 无 origin、`?doc=` 无校验、安全响应头全缺、框架保护缺失、基础镜像未固定、compose 端口/资源 |
| P2 | 14 | 404 回显路径、写入无冲突控制、发送侧 `*`、`?api=` 改写数据源、MIME 策略、硬编码 IP、日志入库、HEALTHCHECK/最小权限、镜像烘焙地址、Actions 未固定 SHA、代理无体限、无超时、无审计日志等 |
| P3 | 8 | readBody 超限响应、hop-by-hop 头、`instrument.js` 未转义、`innerHTML` 拼接、`dir` 回显、COPY 范围、SBOM/签名等 |
| **合计** | **39** | 本维度独立发现 39 条（其中 4 条为对已有文档条目的安全面补充，已在表中标注交叉引用） |

---

## 修复优先级建议

### 第一批（P0，建议 1 个工作日内闭环，均为小改动）

1. **收敛静态路径**（1.1–1.3）：`const resolved = path.resolve(ROOT, '.' + urlPath); if (!resolved.startsWith(ROOT + path.sep)) return 403;` —— 一行判断同时修掉 1.1/1.3，并让 `/projects` 的 403 真正生效。
2. **包住请求解析**（2.1）：`decodeURIComponent` 与整个请求处理器包 try/catch，异常返回 400；同时给进程加 `uncaughtException`/`unhandledRejection` 兜底（记录 + 优雅退出，由容器 `restart: unless-stopped` 拉起）。
3. **项目库接口加鉴权**（3.1）+ **默认只监听 127.0.0.1**（3.2）：读接口可先要求 `X-PFD-Token`（环境变量注入），写/删接口强制；`server.listen(PORT, HOST)` 的 `HOST` 默认 `127.0.0.1`，需要局域网访问时显式设置。

### 第二批（P1，建议本周内）

4. 代理层收口：路径白名单校验（4.1）、CORS 白名单（4.2）、请求头白名单（4.3）、超时与体积上限（4.4）。
5. 前端消息与数据入口：postMessage 双向 origin 校验（5.3/5.4）、`?doc=`/`?api=` 白名单与 schema 校验（5.5/5.6）、统一 `esc()`（5.1）。
6. 响应头基线（6.1/6.2）：`X-Content-Type-Options: nosniff`、`X-Frame-Options: SAMEORIGIN`、基础 CSP。
7. 镜像与编排（8.1/8.4）：digest 固定 + 端口绑 `127.0.0.1` + 资源上限。

### 第三批（P2/P3，随迭代治理）

8. Actions 固定 SHA + Dependabot（8.6）、结构化日志与审计（9.4）、配置外部化与 `.env.example`（7.1）、`.dockerignore` 白名单化（8.5）、容器健康检查与最小权限（8.2/8.3）。
9. 规范类问题（`innerHTML` 一致性、hop-by-hop 头、错误回显路径等）可由 lint 规则 + code review checklist 批量压制。

> 说明：本清单所有"实测"结论均来自本地独立实例（端口 18091–18093）复现，**未对 192.168.1.78 上的部署实例发起任何验证请求**。PoC 命令已在文中给出，便于修复后回归验证。

---

## 修复进展

### 第一批：4 个 P0 全部修复（2026-09-22）

按「先修 P0」推进。**所有 PoC 已用原命令复测，确认堵住。**

| 条目 | 修复内容 | 复测结果（改前 → 改后） |
|---|---|---|
| **1.1 / 1.3** 路径穿越、绕过 `/projects` 403 | 在 `decodeURIComponent` 之后、任何 `fs` 调用之前，把路径收敛回 ROOT：`path.resolve(ROOT, '.' + urlPath)` + `startsWith(ROOT + path.sep)` 校验；**并按解析后的真实路径再判一次是否落在 `PROJECT_DIR` 内**（只靠原始 URL 前缀会被规范化路径绕过） | `/%2e%2e%2fscada_layout_editor.html`：200/13914 字节 → **403**<br>`/../RS/projects/x.json`：200/34820 字节 → **403**<br>`/%2e%2e%2f%2e%2e%2fetc%2fhosts`、`/../README.md` → **403**<br>正常资源 `/editor`、`/preview`、`/editor.js`、`/templates/silo.js`、`/img/磨机.png` → **全部 200** |
| **2.1 / 2.2** 畸形 URI 崩进程（远程 DoS） | `decodeURIComponent` 包 `try/catch` → 返回 400；并增加 `process.on('uncaughtException'/'unhandledRejection')` 进程级兜底（记录后优雅退出，交由容器/systemd 拉起） | `GET /%`：进程退出（http=000）→ **400 且服务存活**（后续 `/editor` 仍 200） |
| **3.1** 项目库接口零鉴权 | 新增 `PFD_API_TOKEN` 环境变量：设置后所有 `/pfd-api/*` 请求必须带 `X-PFD-Token`，否则 401（**留空则保持向后兼容不鉴权**）；令牌由 `sendFile` **自动注入到 HTML 页面**，前端 `projFetch()` 自动携带，无需人工配置 | 无令牌 → **401**；错误令牌 → **401**；正确令牌 → **200**；POST 写 / DELETE 删 无令牌 → **401**；页面内注入 `__PFD_TOKEN` 正常 |
| **3.2** 默认绑定 0.0.0.0 | `server.listen(PORT, HOST)`，`HOST` 默认 `127.0.0.1`（安全默认）；对外暴露需显式设置。启动时若「对外监听 + 无令牌」打印醒目告警 | 默认启动日志：`监听: 127.0.0.1:18097 （仅本机）`；`Dockerfile`/`docker-compose.yml`/`deploy.sh` 已**同步显式设置 `HOST=0.0.0.0`**，避免既有部署突然不可达 |

**配套改动**
- `editor.js`：新增 `projFetch()`，4 处项目库请求统一走它并自动携带令牌
- `Dockerfile` / `docker-compose.yml` / `deploy.sh`：显式 `HOST=0.0.0.0` + `PFD_API_TOKEN` 占位与注释
- 新增 `.env.example`：登记 `PORT` / `HOST` / `API_TARGET` / `PROJECT_DIR` / `PFD_API_TOKEN`
- `sendFile` 的 404 响应不再回显服务器绝对路径（顺带修掉 P2 条目 1.4）
- **`.github/workflows/ci.yml` 增加回归门禁**（防止复发）：越界路径必须 403/404、畸形 URI 必须 400 且服务存活、默认仅监听回环；另加 `auth-regression` 作业校验令牌三种状态（无/错/对）与 HTML 注入

> 说明：本轮修复未改动 `/api/*` 反向代理、CORS、请求头透传、超时等条目（§4.x 与 §6.x 的 P1），这些属第二批。

### 后续批次建议

| 批次 | 建议条目 | 理由 |
|---|---|---|
| 第二批 | **4.1**（`/api/` 前缀逃逸）、**4.2**（CORS 全开）、**4.3**（透传全部客户端头）、**4.4**（代理无超时/无体积上限）、**6.1/6.2**（安全响应头全缺 + 框架保护） | 均为代理层与响应头基线，改动集中在一个文件，可一并验证 |
| 第三批 | **5.3/5.4**（postMessage 无 origin 校验 + targetOrigin `*`）、**5.5/5.6**（`?doc=`/`?api=` 无校验）、**5.1**（`esc()` 统一） | 前端数据入口与消息通道，需与维度二审计的结论对齐 |
| 后续 | **7.x** 配置外置、**8.x** 镜像与 Actions 固定、**9.x** 日志审计与 DoS 限制 | 属工程化治理，与维度三审计清单合并推进 |
