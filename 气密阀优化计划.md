# 气密阀（valve）风格对齐 · 分步优化计划

> 目标文件：`templates/valve.js`
> 风格基准：`templates/bucketElevator.js`（斗式提升机）、`templates/reactor.js`（反应釜）
> 参照样板：`templates/desulfTower.js`（脱硫塔）、`templates/dustCollector.js`（除尘布袋），均已按同一套步骤改造完成
> 特殊指令：**铭牌直接不要了** —— 本模板既不保留、也不新增铭牌（见步骤 6）
> 本文档用于记录每一步的进度，异常中断后从第一个未勾选项继续。

## 0. 风格基准清单（必须照搬）

| 项 | 取值 |
| --- | --- |
| 金属渐变 | `#7d849e → #d9dded → #767d97`（局部 `<defs>`，id 带 uid 后缀，不用外部全局 `gEquip`） |
| 深色内腔 | `#12162b`，描边 `#6a7192` |
| 法兰 / 接管 | `#2a2f45` 填充 + `#3f445c` 描边 |
| 件体（金属件明细） | `#9aa2bc` + `#5b6280` |
| 门板 / 铰链 / 把手 | `#1a1f33` / `#3a4060` / `#8e96b6` |
| 落料口深色收口 | `#0c1020`，描边 `#6a7192` |
| 定尺封顶 | 逐量 `Math.max(下限, Math.min(上限, 比例量))`；或 `H(hr, wr) = Math.min(h*hr, w*wr)`，放大/缩小时只有中间段变长，端板 / 法兰 / 螺栓等定尺件不随之变粗 |

## 1. 现状问题清单（读码定位结果）

- [ ] P1 依赖外部全局渐变：`fill="url(#gEquip)"` 4 处（L24 左管道、L36 右管道、L45 阀体、L77 阀杆内芯）→ 改局部 `<defs>` + uid。
- [ ] P2 配色不符基准：
  - `#0d0b20`（L20 螺栓底、L27 左法兰、L38 右法兰、L69 手轮轮心、L72 手轮把手、L76 阀杆外层、L81 填料压盖）
  - `#0f0e22`（L25 / L37 管腔暗层）
  - `#0a0818`（L47 阀体内部深色层）
  → 统到 `#2a2f45`/`#3f445c`（法兰 / 端面）、`#12162b`/`#6a7192`（深腔）、`#9aa2bc`/`#5b6280`（件体）、`#0c1020`（收口）、`#1a1f33`/`#3a4060`/`#8e96b6`（门 / 铰链 / 把手）。
- [ ] P3 颜色无兜底：`const c = p.color;`（L12）→ `(p && p.color) || '#9C99FF'`。
- [ ] P4 定尺缺失（固定像素偏移）：
  - 螺栓 `r="2"` / `r="0.8"`、`stroke-width 0.6`（L20）
  - 管腔暗层内缩 `+2 / -4`（L25、L37）
  - 法兰板厚 `flangeT+2`（L27、L38）
  - 密封圈 `r="${pipeW/2-2}"`（L29、L39）
  - 阀体多边形尖角 `±4`（L45）
  - 阀体内部深色层 `+4 / +3 / -4 / +2 / -3 / -2`（L47）
  - 阀杆顶 `stemTopY = cy - bodyH/2 - 4`（L55）
  - 手轮位 `wheelCY = stemTopY - wheelR - 2`（L58）
  - 手轮把手 `r="3"`（L72）
  - 阀杆内芯 `+1 / -2`（L77）
  - 填料压盖 `y=cy-bodyH/2-2 / height 4 / rx 1`（L81）
  - 阀体螺栓 `±4`（L85-88）
  → 引入逐量 clamp。
- [ ] P5 手轮缺陷（新发现）：
  - 布局溢出：`stemTopY = cy - bodyH/2 - 4`（L55）与 `wheelR = w*0.14`（L56）混用 h 与 w，再叠加 `wheelCY = stemTopY - wheelR - 2`（L58）。默认 45×35 实算 `stemTopY = 17.5 - 12.25 - 4 = 1.25`、`wheelR = 6.3`、`wheelCY = 1.25 - 6.3 - 2 = -7.05`，手轮（半径 6.3）几乎整体越出画布上边界（y<0），被裁掉。
  - 辐条角度：`const a = i * 36 * Math.PI / 180;`（L63）—— 5 根辐条应间隔 `360/5 = 72°`，现仅铺开 144°，手轮偏半边、不对称。
  → 步骤 2 重排修复。
- [ ] P6 无提升机式细节（接管端面把合法兰 / 阀体对开把合法兰 / 填料压盖件体画法）→ 步骤 3 补齐。
- [ ] P7 标签文字（铭牌类比元素）：阀体上 `PN16` 密封等级文字（L49）→ 按用户指令删除（步骤 6），且全程不新增铭牌。

## 2. 必须保持的对外契约（不得改动）

- 模板 key：`TEMPLATES.valve`
- `name: '气密阀'`、`category: '阀门管件'`、`defaultSize: { w: 45, h: 35 }`
- `ports` 原样：
  - `in (0, 0.5, left)`
  - `out (1, 0.5, right)`
  - 端口 `y=0.5` 必须与 `cy = h/2`、管腔中心严格同轴，改造时不得偏移；管口最外侧坐标固定为 `x=0`（左）/ `x=w`（右）
- 本模板不含任何动画（无 `<animate>` / `<animateTransform>`），优化过程中不得新增 SMIL 动画
- 画布越界约束：所有图元（含手轮 / 阀杆）渲染后须落在 `x∈[0,w]`、`y∈[0,h]` 内

## 3. 分步执行

### 步骤 1：阀体主结构与接管对齐风格
- [ ] 1.1 新增局部 `<defs>`：`metalId`（横向渐变 `x1→x2`，给阀体 / 阀杆内芯）、`metalVId`（竖向渐变，给左右两根横向接管管壁），id 带 uid 后缀（`val_metal_` / `val_metalV_`），不再引用全局 `gEquip`
- [ ] 1.2 左右管道（L24 / L36）：`fill="url(#gEquip)"` → `url(#${metalVId})`，描边 `${c}` → `#3f445c`
- [ ] 1.3 阀体（L45）：`fill="url(#gEquip)"` → `url(#${metalId})`，描边 `${c}` → `#3f445c`
- [ ] 1.4 阀杆内芯（L77）：`fill="url(#gEquip)"` → `url(#${metalId})`；外层杆壳（L76）`#0d0b20` → `#2a2f45`
- [ ] 1.5 管腔暗层（L25 / L37）：`#0f0e22` → `#12162b`，描边 `#6a7192`
- [ ] 1.6 阀体内部深色层（L47）：`#0a0818` → `#12162b`，描边 `#6a7192`
- [ ] 1.7 两侧法兰（L27 / L38）：`#0d0b20` → `#2a2f45` + `#3f445c` 描边；螺栓（L20）底改 `#2a2f45`、螺栓头改 `#8e96b6`
- [ ] 1.8 `const c = p.color;` → `const c = (p && p.color) || '#9C99FF';`
- [ ] 1.9 校验：`node --check templates/valve.js` 退出码 0；多档尺寸渲染无 `gEquip` 残留、渐变 id 跨实例唯一、颜色兜底与自定义色均生效；浏览器目视金属壳 + 深腔生效
- 备注：`#0d0b20` 的阀杆外层 / 手轮轮心 / 手轮把手 / 填料压盖延续到步骤 2.2 / 2.3；固定像素偏移（P4）留步骤 4，本步只改配色与渐变

### 步骤 2：手轮 / 阀杆布局修复与重排（修 P5）
- [ ] 2.1 修正布局公式：手轮与阀杆改为在阀体上方按比例预留区排版——先算可用的顶部空间，再反推手轮半径与阀杆长度（关键约束 `wheelCY - wheelR ≥ 0`），避免混用 h/w 比例导致手轮越出画布
- [ ] 2.2 手轮配色对齐：轮缘 `${c}` 保留，轮心 / 把手底 `#0d0b20` → `#1a1f33`，把手圆 → `#8e96b6`（`opacity 0.7`）
- [ ] 2.3 修正辐条角度：`i * 36 * Math.PI/180` → `i * (360/5) * Math.PI/180`，5 根辐条均匀铺满整圈
- [ ] 2.4 阀杆外层杆壳 `#2a2f45` + 内芯金属渐变；填料压盖 `#0d0b20` → `#2a2f45` + `#3f445c` 描边
- [ ] 2.5 校验：`node --check` 退出码 0；断言默认 45×35 与极端尺寸下手轮完整可见（`wheelCY - wheelR ≥ 0` 且 `wheelCY + wheelR ≤ h`）、辐条夹角 = 72°；浏览器目视手轮对称、位于阀体正上方

### 步骤 3：接管与阀体细节对齐（提升机式画法）
- [ ] 3.1 左右两根接管统一画法（抽 `makePipe(x0,x1,cy,ph,side)`）：金属管壁（`metalVId`）+ `#12162b`/`#6a7192` 管腔 + 端面把合法兰（`#2a2f45`/`#3f445c`）+ 2 颗 `#8e96b6` 螺栓；**管口端必须与端口锚点重合**（左管口 `x=0`、右管口 `x=w`，`side` 显式传参）
- [ ] 3.2 阀体两侧对开把合法兰：在阀体与左右接管交接处加 `#2a2f45`/`#3f445c` 法兰环 + 四角螺栓，尺寸按阀体宽高封顶
- [ ] 3.3 阀杆填料压盖明细：压盖本体 `#9aa2bc`/`#5b6280` 件体色 + 两侧压盖螺母（`#2a2f45`/`#3f445c`）
- [ ] 3.4 校验：端口锚点比例实测不变（`in=0` / `out=1`、`y=0.5`）；浏览器目视左右法兰与管口位置正确
- 备注（与参照样板的差异）：本模板默认仅 45×35、无筒身，**不设提升机式观察门**（`desulfTower` / `dustCollector` 的门板 / 铰链 / 把手画法不适用于此尺寸的阀门）；门色系 `#1a1f33`/`#3a4060`/`#8e96b6` 仅在手轮轮心 / 把手上沿用

### 步骤 4：定尺封顶与自适应
- [ ] 4.1 逐量 clamp：法兰板厚 `flangeT`、管壁厚、螺栓半径（`r 2 / 0.8`）、阀体多边形尖角（`±4`）、密封圈半径、手轮半径 `wheelR`、阀杆宽 `stemW`、填料压盖厚（`height 4`）
- [ ] 4.2 替换所有固定像素偏移（P4 清单：`+2 / -4 / ±4 / +3 / -2 / +1 / r 2 / r 0.8 / r 3 / rx 1` 等）
- [ ] 4.3 管腔暗层 / 阀体内部深色层的内缩量改为比例量，避免小尺寸下暗层反向外扩或消失
- [ ] 4.4 校验：`node --check` 退出码 0；极端尺寸（45×35 / 90×70 / 22×18 / 180×140 / 45×70）冒烟无 NaN、无 `gEquip`、无越界，比例不失真

### 步骤 5：整体校验
- [ ] 5.1 `node --check templates/valve.js` 退出码 0；`GetDiagnostics` 无告警
- [ ] 5.2 浏览器目视：默认 45×35、自定义色、demo 实机尺寸
- [ ] 5.3 编辑态 / 运行态：确认本模板不新增 SMIL 动画（`<animate>` 计数恒为 0，`stripSMIL()` 无副作用）
- [ ] 5.4 镜像 `mirrored`、旋转 `rotation`、`namePos` 正常
- [ ] 5.5 临时核对页删除，清理产物

### 步骤 6：设计变更（删除铭牌 / 标签文字）
> 用户指令：**名牌直接不要了。**
> 结论：不保留、也不新增铭牌。本模板无独立 `nameplate` 变量，铭牌类比元素为阀体上的 `PN16` 文字，按指令删除。

- [ ] 6.1 删除阀体上的 `PN16` 密封等级文字（L49 整行），return 中不再输出任何 `<text>`
- [ ] 6.2 明确不新增铭牌：与 `desulfTower` / `dustCollector` 中「铭牌保留 / 对齐配色」的分支相反，本模板不做 `nameplate` 板 + 文字
- [ ] 6.3 校验：`grep -n` 确认 `templates/valve.js` 内无 `<text>`、无 `nameplate`、无 `PN16`；`node --check` 退出码 0；冒烟 `hasText=false`

## 4. 进度记录

| 步骤 | 状态 | 备注 |
| --- | --- | --- |
| 步骤 1 | 未开始 | 阀体主结构与接管对齐风格（局部 defs / 深腔 / 法兰配色 / 颜色兜底）；固定像素偏移留步骤 2/3/4 |
| 步骤 2 | 未开始 | 手轮 / 阀杆布局修复与重排（修 P5：溢出 + 辐条角度） |
| 步骤 3 | 未开始 | 接管与阀体细节对齐（端面把合法兰 / 对开法兰 / 填料压盖）；不设观察门 |
| 步骤 4 | 未开始 | 定尺封顶与自适应（逐量 clamp + 替换固定偏移） |
| 步骤 5 | 未开始 | 整体校验与产物清理 |
| 步骤 6 | 未开始 | 设计变更：删除 `PN16` 标签文字，不新增铭牌 |
