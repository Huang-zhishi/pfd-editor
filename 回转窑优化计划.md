# 回转窑（rotaryKiln）风格对齐 · 分步优化计划

> 目标文件：`templates/rotaryKiln.js`
> 风格基准：`templates/bucketElevator.js`（斗式提升机）、`templates/reactor.js`（反应釜）
> 参照样板：`templates/tubeCooler.js`（回转滚筒冷却机，上一轮按同一套步骤改造完成）、`templates/coolingPond.js`、`templates/desulfTower.js`、`templates/silo.js`、`templates/screwConveyor.js`、`templates/weighFeeder.js`、`templates/switchValve.js`
> 特殊指令：无单独指令；按已完成样板的统一惯例处理铭牌 —— **保留铭牌（`Φ3.5×54m` / `回转窑`）并仅对齐配色与定尺**（见步骤 6）
> 本文档用于记录每一步的进度，异常中断后从第一个未勾选项继续。
> 进度：**0 / 6 步完成**。

## 0. 风格基准清单（必须照搬）

| 项 | 取值 |
| --- | --- |
| 金属渐变 | `#7d849e → #d9dded → #767d97`（局部 `<defs>`，id 带 uid 后缀，不用外部全局 `gEquip`） |
| 深色内腔 / 基础 | `#12162b`，描边 `#6a7192` |
| 法兰 / 接管 | `#2a2f45` 填充 + `#3f445c` 描边 |
| 件体（金属件明细 / 亮线） | `#9aa2bc` + `#5b6280` |
| 螺栓头 / 指针件体色 | `#8e96b6` |
| 深色收口 | `#0c1020`，描边 `#6a7192` |
| 高光 / 旋转白带 | `#d9dded` |
| 渐变方向 | 立式箱体 / 底座 / 罩体 / 减速机 / 电机用**横向渐变**（`x1=0,y1=0,x2=1,y2=0`）；横向筒体（回转窑筒体）圆柱明暗用**竖向渐变**（`x1=0,y1=0,x2=0,y2=1`）；横向管（燃烧器管 / 出料管）用**竖向渐变** |
| 定尺封顶 | 逐量 `Math.max(下限, Math.min(上限, 比例量))`；或 `H(hr, wr) = Math.min(h*hr, w*wr)`，放大 / 缩小时只有中间段变长，端面 / 法兰 / 接管 / 螺栓 / 铭牌等定尺件不随之变粗 |
| 粒子 / 运动件 | 统一 `${c}`（主题色，兜底 `#9C99FF`）；物料 / 烟气 / 冷热气流粒子一律 `${c}`，仅靠透明度区分 |
| 工艺光效 | 火焰 / 热辐射保留暖色工艺色阶（归一为 `#ff6a10` 外焰、`#ffdd44` 内焰、`#fff6c8` 焰芯、`#ff4a10` 热辐射、`#ffa040` 焰根辉光、`#ff6020` 观察孔）；耐火砖衬里保留砖红工艺色（`#8B4513` / `#6B3510`） |

## 1. 现状问题清单（读码定位结果）

- [ ] P1 无局部金属渐变 + 依赖外部全局：全文件仅两处渐变 —— 筒体明暗 `drumShade`（L100-106，深紫色系 `#2a1a25/#3d2535/#4a3040`，非基准金属渐变）与温度渐变 `tempGrad`（L109，**须保留**）；基础墩 / 托轮 / 挡轮 / 轮带 / 齿圈 / 窑尾罩 / 窑头罩 / 燃烧器 / 减速机 / 电机 / 出料管全部纯平涂色；**且有 3 处引用外部全局 `url(#gEquip)`**（L341 燃烧器管体、L447 减速机、L452 电机），脱离编辑器全局 defs 即失效 → 步骤 1 新增局部 `<defs>`（uid 后缀 `rk_metal_` / `rk_metalv_`），把筒体明暗渐变一并收进 `<defs>` 并金属化，彻底去掉 `gEquip`
- [ ] P2 配色不符基准（仅铭牌板 `#0d0b20` 留步骤 6）：
  - `#1a1835`（×11：L48 基础外框、L73 托轮轮圈、L92 挡轮、L144 轮带外环、L241 齿圈外环、L267 窑尾罩、L273 烟气法兰、L306 窑头罩、L319 出料端法兰、L342 燃烧器端法兰、L441 小齿轮）
  - `#12102a`（×5：L49 基础内衬、L145 轮带内环、L242 齿圈内环、L268 窑尾罩内衬、L307 窑头罩内衬）
  - `#0d0b20`（×14：L34 螺栓、L74 托轮轮毂、L81 轴承底板、L93 挡轮内环、L279 罩门、L308 窑头观察孔、L330 小车轨道、L332 小车车体、L345 燃烧器、L442 小齿轮轮毂、L449 减速机中心、L451 减速机底座、L458 电机底座、L488 铭牌板）
  - `#151330`（×13：L78 托轮轴承座、L94 挡轮轴座、L270 烟气管、L312 燃烧器接口、L314 斜溜槽、L316 水平出料管、L333/L334 小车车轮、L338 支撑立柱、L344 燃烧器端椭圆、L445 小齿轮座、L453 电机端盖、L457 电机风罩）
  - `#252545`（×2：L139 轮带齿、L235 齿圈齿）
  - `#2a1a25`/`#3d2535`/`#4a3040`（L101-105 筒体渐变，深紫）
  - `#2a1f30`（×2：L118/L119 筒体两端封头椭圆）
  - 分隔 / 高光：`rgba(255,200,150,0.06)`（L117 筒体内壁高光）、`rgba(255,255,255,0.08)`（L146 轮带高光）、`rgba(255,255,255,0.05)`（L271 烟气管高光）、`rgba(255,255,255,0.06)`（L317 出料管高光）
  - 运动指示：`#e07040` / `#fff` / `#ff9060`（L173/174/176 旋转标记带）
  - 粒子：`#b08060`（×12：L418 物料颗粒）、`#a0b0c0`（×6：L478 烟气粒子）
  → 统到 `#7d849e→#d9dded→#767d97`（金属外壳）、`#12162b`/`#6a7192`（深腔 / 内衬 / 基础内衬）、`#2a2f45`/`#3f445c`（法兰 / 接管 / 端板 / 座体）、`#9aa2bc`/`#5b6280`（件体 / 亮线 / 散热片）、`#0c1020`（收口 / 齿牙 / 小车）、`#8e96b6`（螺栓头）、`#d9dded`（高光 / 白带）、`${c}`（粒子 / 旋转标记带 / 运动件）
- [ ] P3 颜色无兜底：`const c = p.color;`（L19）→ `(p && p.color) || '#9C99FF'`（`p` 为 `{}` 时 `c` 为 `undefined` 并渗入大量 `stroke` / `fill`；`p` 为 `null` 时直接抛 `TypeError`，其余设备均已兜底）
- [ ] P4 定尺缺失（固定像素偏移）：`bolt r=1.5 / boltPair dist=5 r=1.3`（L34-35）、基础墩 `w:36/44/44`（L41-43）、`fTop=h_cy+h_R+28`（L45）、基础内衬 `+2/-4/+10/+20`（L49-51）、`baseY-2`（L52-53）、`trunnionR=9`（L57）、`trunnionOffset=(h_R+8)*sin30*0.85`（L62）、`trunnionCY=h_cy+h_R+10`（L63）、辐条 `*0.55`（L68）、`bearH=8/bearW=14`（L70）、轴心 `r=2`（L76）、轴承底板 `+2/height=4`（L81）、挡轮 `+12/r=7/r=3/+5/14×8/+7/+11/+16`（L89-97）、`tireW=16/tTeethCount=40/tGearR=h_R+5/tToothH=4/tOuterRx=+1/tInnerRx=-2`（L122-127）、轮带内 `-5`/高光 `h_R*0.7/h_R*0.2`（L145-146）、焊缝环 `9/rx=2.5/ry=h_R-1`（L153-155）、旋转带 `+8/-16/sw=5/2/3/1.5`（L168/173-176）、焊缝螺栓 `9×4/r=1.3/h_R-2`（L180-190）、耐火砖 `+4/-8/sw=3/8/+8/-16/rx=2/ry=h_R-6`（L199-202）、扬料板 `10/h_R-5/h_R*0.3/fx`（L207-217）、齿圈 `gearTeeth=48/h_gearR=h_R+8/h_gearW=14/toothH=4`（L225-232）、窑尾罩 `w*(24/380)`/`inletPipeH=14`/`+1`/`fluePipeW=12`/`+7`/`flueTopY=h*0.22`（L247-265）、窑头罩 `w*(28/380)`/`outletPipeH=12`/`-12/-1/+18/w-5/-2/+4`（L285-320）、燃烧器 `w*(6/380)`/`w*(5/380)`/`carX-8`/`carY=h*0.82`/`32×3/18×4/r=3`（L323-338）、驱动 `h_pinionR=7/+2/h_reducerW=28/h_reducerH=22/+6/+15/h_motorW=34/h_motorH=20/+2/+15`（L435-437）、减速机 / 电机细部 `+2/r=4/3.8/-3/5/rx=+2/+1/-1.5*0.4/r=1/4/+3/-3/*0.28/-6/7/+4/-4`（L448-459）、测温点 `r=2.5/2`（L462-465）、烟气粒子 `6×12/8/25/2+2`（L469-484）、铭牌 `32×14/4/3.2/+25/+15/+26/+16/+41/+21/+28/+54/+17.5`（L487-493）→ 引入逐量 clamp 与比例量
- [ ] P5 端口锚点核对（现状基本对齐，作为契约固化）：
  - `inlet (0.057, 0.55, left)`：窑尾罩左壁 = `h_drumL - tailHoodW = w*0.12 - w*(24/380) = w*0.0568`，比例恒为 `0.0568`，与端口 `0.057` 一致；`y=0.55 = h_cy` 同轴 ✓
  - `outlet (1, 0.76, right)`：水平出料管端口在 `x=w`（端法兰右沿），管心 `outletPipeCY = h*0.76` ✓
  - `fuel (0.9713, 0.55, right)`：`burnerEndX = w*0.92947 + 0.041844w = 0.97131w`，管心 `h_cy = h*0.55` ✓
  - `flueGas (0.1, 0.22, up)`：烟气管中心 `fluePipeCX = w*0.1`，管顶法兰上沿 `flueTopY = h*0.22` ✓
  - → 步骤 2 仅做「参数化锚点常量 + 断言固化」（把 `0.0568 / 0.76 / 0.9713 / 0.22` 抽成显式常量并加注释「须与 ports 同步」），保证后续步骤不破坏比例
- [ ] P6 小尺寸越界（新发现）：`fTop = h*0.71 + 28`（L45）在 `h<40` 时大于 `baseY = h*0.94` → 基础墩高为负；铭牌 `h_cy+h_R+15+h*? = h*0.71+29`（L488-493）在 `h<约 96` 时越过 `baseY`；驱动电机底 `h_motorY+h_motorH+5 = h*0.71+30`（L437/458）在小 `h` 时越过画布底沿；电机右沿 `h_motorX+h_motorW = h_gearX + 93 = 0.414w + 93`（L437）在 `w<约 225` 时越过画布右沿（如 `w=150` → `155.1>150`）；烟气粒子轨迹上端 `8-25 = -17`（L475）恒越画布上沿 → 步骤 4 全部改为「比例 × 定尺封顶」，并对基础墩 / 铭牌 / 驱动 / 烟气粒子设上下限保证小尺寸不越界、不退化、不出负值
- [ ] P7 细部画法不统一 / 缺失：螺栓统一为 `fill=#0d0b20 + stroke=${c}`（L34）与基准「螺栓头 `#8e96b6`」不符；托轮既有轴承座但无轴座底板配色分层；燃烧器小车无端部把合法兰样式；传动小齿轮与减速机之间无联轴器 / 护罩；窑尾罩 / 窑头罩检修门无把手；烟气管道无端面法兰螺栓统一样式 → 步骤 3 统一螺栓 / 法兰 / 座体画法并补齐把手、联轴器护罩、管端法兰螺栓
- [ ] P8 铭牌 + `<text>`（L487-493）：**按惯例保留**（`Φ3.5×54m` + `回转窑`），但板色 `#0d0b20` / 内板 `#151330` / 位置 / 字号需统到基准色系与定尺（板 `#2a2f45` + `#3f445c` 描边，文字 `${c}`）→ 步骤 6

## 2. 必须保持的对外契约（不得改动）

- 模板 key：`TEMPLATES.rotaryKiln`
- `name: '回转窑/旋转煅烧窑'`、`category: '设备'`、`defaultSize: { w: 380, h: 180 }`
- `ports` 原样：
  - `inlet (0.057, 0.55, left)`
  - `outlet (1, 0.76, right)`
  - `fuel (0.9713, 0.55, right)`
  - `flueGas (0.1, 0.22, up)`
- **温度渐变编辑器集成契约（最高优先级，不得改动 class / 结构 / data 属性）**：
  - `class="kiln-temp-grad"` 的 `<linearGradient>`（`x1=0 y1=0 x2=1 y2=0`）
  - 渐变内 `<stop class="kiln-temp-stop" data-kidx="i" offset=…%>`，`i` 从 0 递增
  - `class="kiln-temp-overlay"` 的 `<rect … fill="url(#温度渐变id)" opacity="0.55" clip-path=筒体clip>`
  - 数据源 `p.tempPoints`（`editor.js addComponent` 预置 4 点 `TI_206A…F`）；`editor.js refreshKilnTemp()` 通过 `querySelectorAll('.kiln-temp-stop')` **增量**写 `stop-color`，**不触发 `renderAll()`**（避免 SMIL 复位）→ 重构只能演算几何 / 颜色，绝不可改类名、层级或移除该结构
- 动画标签形式：`<animate>` / `<animateTransform>`（编辑态由 `editor.js stripSMIL()` 剥离）
- 已有动画保留、不得增删：`<animate>` **453** 个、`<animateTransform>` **3** 个（托轮组 2 + 大齿圈小齿轮 1）→ 全模板 SMIL 总计数恒为 **456**
  - `animate` 明细：轮带齿 `40×2×2=160` + 焊缝螺栓 `9×4×2=72` + 大齿圈齿 `48×2=96` + 物料颗粒 `12×3=36` + 扬料板 `10×3=30` + 火焰 `6×4=24` + 烟气粒子 `6×4=24` + 旋转标记带 `4×2=8` + 基础/热辐射 `2` + 窑头观察孔 `1` = **453**
- 文字 / 铭牌：保留 `<text>Φ3.5×54m</text>` 与 `<text>回转窑</text>`，文字内容 / 文案不得变动
- 画布越界约束：所有图元（含铭牌、基础墩、驱动、烟气 / 物料 / 火焰粒子轨迹）渲染后须落在 `x∈[0,w]`、`y∈[0,h]` 内

> 契约实测（vm 载入 `templates/rotaryKiln.js` 后读取全局 `TEMPLATES.rotaryKiln`）：`name=回转窑/旋转煅烧窑`、`category=设备`、`defaultSize={w:380,h:180}`、`ports=[inlet(0.057,0.55,left), outlet(1,0.76,right), fuel(0.9713,0.55,right), flueGas(0.1,0.22,up)]`、`render` 为函数；默认 380×180 渲染 SMIL 计数 `animate=453` + `animateTransform=3` = **456**；`url(#gEquip)` 引用 **3** 处；`p={}` 渲染产物含 `undefined`、`p=null` 渲染抛 `TypeError`。

## 3. 分步执行

### 步骤 1：筒体主结构与接管配色对齐风格
- [ ] 1.1 新增局部 `<defs>`：`metalId`（横向渐变，给立式件：基础墩 / 窑尾罩 / 窑头罩 / 减速机 / 电机）、`metalVId`（竖向渐变，给横向筒体与横向管：筒体圆柱明暗 / 燃烧器管 / 出料管），id 带 uid 后缀（`rk_metal_` / `rk_metalv_`）；把现有筒体明暗渐变 `drumShade` 收进 `<defs>` 并改基准金属色系（竖向），**保留 `kilnTempGrad` 温度渐变原样**
- [ ] 1.2 筒体壁（`drumBody`）：`fill="url(#drumShade)"` → 基准金属竖向渐变；描边 `${c}` → `#3f445c`；两端封头椭圆 `#2a1f30` → `#12162b` + `#6a7192`；内壁高光 `rgba(255,200,150,0.06)` → `#5b6280`
- [ ] 1.3 基础墩（`foundations`）：外框 `#1a1835` → `url(#metalId)`，描边 `${c}` → `#3f445c`；内衬 `#12102a` → `#12162b`；分层线 `${c}` → `#5b6280`
- [ ] 1.4 托轮组（`drawTrunnionSet`）：轮圈 `#1a1835` → `url(#metalId)`，描边 → `#3f445c`；轮毂 `#0d0b20` → `#12162b` + `#6a7192`；辐条 `${c}` → `#9aa2bc`；轴承座 `#151330` → `#2a2f45` + `#3f445c`；轴承底板 `#0d0b20` → `#0c1020` + `#6a7192`
- [ ] 1.5 挡轮（`h_thrustWheel`）：轮圈 `#1a1835` → `url(#metalId)`，内环 `#0d0b20` → `#12162b` + `#6a7192`；轴座 `#151330` → `#2a2f45` + `#3f445c`
- [ ] 1.6 轮带（`drawTire`）：外环 `#1a1835` → `url(#metalId)`，内环 `#12102a` → `#12162b`，齿牙 `#252545` → `#0c1020`，高光 `rgba(255,255,255,0.08)` → `#5b6280`，描边统 `#3f445c` / `#6a7192`
- [ ] 1.7 大齿圈（`h_girthGear`）：外环 `#1a1835` → `url(#metalId)`，内环 `#12102a` → `#12162b`，齿牙 `#252545` → `#0c1020`，描边统 `#3f445c` / `#6a7192`
- [ ] 1.8 窑尾罩（`tailHood`）：罩体 `#1a1835` → `url(#metalId)`，内衬 `#12102a` → `#12162b`；烟气管 `#151330` → `url(#metalVId)`，高光 `rgba(255,255,255,0.05)` → `#5b6280`；顶部法兰 `#1a1835` → `#2a2f45` + `#3f445c`；检修门 `#0d0b20` → `#2a2f45` + `#3f445c`；引导线 `${c}` → `#5b6280`
- [ ] 1.9 窑头罩（`headHood`）+ 出料溜槽：罩体 `#1a1835` → `url(#metalId)`，内衬 `#12102a` → `#12162b`；观察孔 `#0d0b20` → `#0c1020` + `#6a7192`；燃烧器接口 `#151330` → `#2a2f45` + `#3f445c`；斜溜槽 / 水平出料管 `#151330` → `url(#metalVId)`（横管竖向渐变），高光 → `#5b6280`；端部法兰 `#1a1835` → `#2a2f45` + `#3f445c`
- [ ] 1.10 燃烧器（`burner`）+ 小车（`burnerCar`）：燃烧器管体 `url(#gEquip)` → `url(#metalVId)`，端部法兰 `#1a1835` → `#2a2f45` + `#3f445c`，端椭圆 `#151330` → `#12162b` + `#6a7192`；小车轨道 / 车体 `#0d0b20` → `#0c1020` + `#6a7192`，车轮 / 立柱 `#151330` → `#2a2f45` + `#3f445c`
- [ ] 1.11 传动系统（`h_driveSystem`）：小齿轮 `#1a1835` → `url(#metalId)`，轮毂 `#0d0b20` → `#12162b`；减速机 `url(#gEquip)` → `url(#metalId)`，中心 `#0d0b20` → `#12162b`，底座 `#0d0b20` → `#0c1020`；电机 `url(#gEquip)` → `url(#metalId)`，端盖 / 风罩 `#151330` → `#2a2f45`，散热片 `${c}` → `#5b6280`，底座 `#0d0b20` → `#0c1020`；`ZSY` 文字 `${c}` 保留
- [ ] 1.12 粒子 / 运动件配色：物料颗粒 `#b08060` → `${c}`；烟气粒子 `#a0b0c0` → `${c}`；旋转标记带 `#e07040/#fff/#ff9060` → 分别 `${c}`（降透明度）/ `#d9dded` / `${c}`；耐火砖 `#8B4513` / `#6B3510` 保留工艺色；火焰 / 热辐射 `#ff6a10/#ffdd44/#fff6c8/#ff4a10/#ffa040/#ff6020` 保留工艺暖色
- [ ] 1.13 `const c = p.color;` → `const c = (p && p.color) || '#9C99FF';`
- [ ] 1.14 校验：`node --check templates/rotaryKiln.js` 退出码 0；多档尺寸渲染无 `gEquip` 残留、渐变 id 跨实例唯一、颜色兜底与自定义色均生效；`#1a1835`/`#12102a`/`#151330`/`#252545`/`#2a1a25`/`#3d2535`/`#4a3040`/`#2a1f30`/`#b08060`/`#a0b0c0` 残留 = 0（铭牌板 `#0d0b20` 留步骤 6）；`kiln-temp-grad` / `kiln-temp-stop` / `data-kidx` / `kiln-temp-overlay` 仍在；SMIL 恒为 456
- 备注：固定像素偏移（P4）留步骤 4，本步只改配色 / 渐变 / 兜底与粒子色

### 步骤 2：端口锚点参数化与固化（P5）
- [x] 2.1 抽出显式锚点常量并加注释「须与 `ports` 同步」：`drumCYRatio=0.55`、`tailWallRatio=0.057`、`outletCYRatio=0.76`、`fuelXRatio=0.9713`、`flueXRatio=0.1`、`flueTopRatio=0.22`，并把窑尾罩宽（`tailHoodW=h_drumL-w*tailWallRatio`）、燃烧器管长（`burnerLen=w*fuelXRatio-burnerX`）改写为以锚点比例反推
- [x] 2.2 校验 `inlet`：窑尾罩左壁横坐标比例 = `0.057`（五档实测 0.0570），且 `h_cy = h*drumCYRatio` 与端口 `y=0.55` 同轴
- [x] 2.3 校验 `outlet`：水平出料管端法兰右沿 = `x=w`（实测 1.0000），管心 = `h*0.76`（实测 0.7600）
- [x] 2.4 校验 `fuel`：燃烧器管端法兰右沿 = `0.9713w`（实测 0.9713），管心 = `h*0.55`（实测 0.5500）
- [x] 2.5 校验 `flueGas`：烟气管中心 = `0.1w`（实测 0.1000），管顶法兰上沿 = `h*0.22`（实测 0.2200）
- [x] 2.6 校验：多尺寸实测四个端口比例不变（`0.057/0.55`、`1/0.76`、`0.9713/0.55`、`0.1/0.22`，五档尺寸全覆盖）；SMIL 恒为 456
- 备注：小尺寸（`90×40`）烟气管顶法兰因固定 `width=20` 出现 `x=-1` 越左沿，属固定像素偏移问题，留步骤 4 处理

### 步骤 3：细节补齐（提升机式画法）
- [x] 3.1 螺栓统一画法：`bolt` / `boltPair` 已输出「螺栓头 `#8e96b6` + 深色孔芯 `#0c1020`」，全文 9 处（基础墩地脚、托轮轴座、挡轮轴座、罩门、烟气法兰、出料法兰、燃烧器法兰、小齿轮座、电机地脚）均走该函数；螺栓半径按件体比例 clamp 归步骤 4 统一处理
- [x] 3.2 托轮轴座 / 轴承座分层：轴承座块 `url(#rkMetalId)` + `#3f445c` 描边，底板 `#0c1020` + `#6a7192`，每座 2 颗 `#8e96b6` 螺栓；挡轮轴座同样改为金属座块 + 深色底板
- [x] 3.3 传动细节：小齿轮与减速机之间补联轴器护罩（`#5b6280` 半透 + `#9aa2bc` 描边），减速机 / 电机各补 2 颗地脚螺栓，护罩尺寸按 `h_R` clamp
- [x] 3.4 罩体细节：窑尾罩检修门补把手（`#9aa2bc` 圆角矩形），窑头罩观察孔补铰链把手（`#9aa2bc` 圆头线）；矮尺寸自动缩小 / 省略归步骤 4
- [x] 3.5 管道细节：烟气管顶部法兰、出料端法兰、燃烧器端法兰均已有端面法兰环（`#2a2f45` + `#3f445c`）+ 2 颗 `#8e96b6` 螺栓（`boltPair`），无需新增
- [x] 3.6 校验：端口锚点比例实测不变（五档全等）；`node --check` 退出码 0、多尺寸断言全通过、SMIL 恒为 456；`@resvg/resvg-js` 3–8× 光栅化目视法兰 / 螺栓 / 轴座 / 把手与筒体 / 罩体 / 底座不错压
- 备注：默认 380×180 目视发现铭牌与减速机存在重叠（沿用原设计的铭牌初始位，非本次改动引入），与小尺寸越界一并归步骤 4 / 步骤 6 处理

### 步骤 4：定尺封顶与自适应（修 P6）
- [ ] 4.1 逐量 clamp：基础墩宽 / 高 / 基线、托轮半径 / 间距 / 轴承座尺寸、挡轮半径 / 轴座、轮带环厚与齿高、大齿圈环厚与齿高、小齿轮半径、减速机 / 电机箱体尺寸、燃烧器小车、罩宽、溜槽 / 出料管宽、烟气管宽、粒子数与轨迹幅度、铭牌板 —— 全部按筒径 / 罩高 / 机宽封顶（引入 `F` / `clamp` / `K` + 细部定尺常量块；数量类齿数 / 粒子数 / 扬料板数 / 焊缝环数保持恒定以保 SMIL 计数）
- [ ] 4.2 替换所有固定像素偏移（P4 清单：`28 / 36 / 44 / 2 / 4 / 10 / 20 / 9 / 8 / 14 / 30 / 0.85 / 16 / 40 / 5 / 4 / 2.5 / 1 / 3 / 7 / 12 / 48 / 24 / 28 / 18 / 12 / 6 / 34 / 20 / 22 / 25 / 32 / 26 / 41 / 54 / 17.5` 等逐一消除）
- [ ] 4.3 基础墩 / 铭牌 / 驱动竖向封顶：`fTop` 与 `baseY` 间距设下限（小 `h` 不出现负高、不重叠），铭牌底沿夹在 `y≤baseY` 内，电机 / 减速机底部夹在画布内
- [ ] 4.4 驱动横向封顶：电机右沿 `≤ w-1`（小 `w` 时减速机 / 电机整体左移或等比缩小），并保证与筒体 / 齿圈不重叠（`driveShift` 式左移保护）
- [ ] 4.5 烟气粒子轨迹封顶：上端不越 `y=0`（起点 / 上升幅度按管长比例并设下限），火焰 / 物料 / 旋转带轨迹幅度按筒径 clamp
- [ ] 4.6 校验：极端尺寸（380×180 / 760×340 / 300×140 / 200×110 / 150×60 / 120×50 / 90×40）冒烟均无 NaN、无 `gEquip`、无越界（含无负宽高 / 半径、无 `undefined`、铭牌与驱动在画布内），多尺寸断言全通过、SMIL 恒为 456

### 步骤 5：整体校验
- [ ] 5.1 `node --check templates/rotaryKiln.js` 退出码 0；仓库无 lint / typecheck 配置、无 `package.json`，以 `node --check` + 多尺寸冒烟为准
- [ ] 5.2 目视逐档核对：默认 380×180、自定义色、扁 760×340、极小 150×60 —— 金属筒体明暗、深色内腔、罩体法兰 + 螺栓、托轮轴座、轮带 / 齿圈、驱动、烟气管道、铭牌均正常，图元全部落在画布内，无裁切、无错压
  - 备注：沙箱内远程浏览器经代理访问 `localhost` 返回 `Unauthorized`（无法访问工作区本地静态服务），改用 `@resvg/resvg-js` 将 SVG 按 2–5× 光栅化为 PNG 后逐档目视，并叠加画布红框便于识别越界
- [ ] 5.3 编辑态 / 运行态：`stripSMIL()` 命中（动画标签全部为自闭合 `<animate …/>`），编辑态 SMIL 计数为 0，运行态动画恢复；`<animate>` + `<animateTransform>` 总计数 **恒为 456**
- [ ] 5.4 `kiln-temp` 集成回归：定制 `p.tempPoints` 后渲染产物仍含 `.kiln-temp-grad` / 4 个 `.kiln-temp-stop`（`data-kidx=0..3`）/ `.kiln-temp-overlay`；`refreshKilnTemp` 的 `querySelectorAll('.kiln-temp-stop')` 能命中全部节点（数量与 `tempPoints` 一致）；无 `tempPoints` 时不渲染该结构且不报错
- [ ] 5.5 镜像 `mirrored` / 旋转 `rotation` / `namePos` 正常：模板自身不向外发出静态 `transform`（实测 `\stransform="` 命中 0，仅 SMIL 的 `attributeName="transform"`），编辑器在外层 `.equip-group` 施加镜像（`translate(w,0) scale(-1,1)`）与旋转不受影响；铭牌为模板内 `<text>`，镜像时随图形翻转
- [ ] 5.6 临时核对页与本地静态服务已删除 / 停止，`git status` 无临时产物残留

### 步骤 6：铭牌保留 + 配色对齐
> 本模板当前已有铭牌板 + `<text>Φ3.5×54m</text>` / `<text>回转窑</text>`（L487-493），按已完成样板（冷却水池 / 脱硫塔 / 回转滚筒冷却机）的惯例**保留**，仅统色与定尺。

- [ ] 6.1 保留 `nameplate` 板 + `<text>Φ3.5×54m</text>` / `<text>回转窑</text>`，文字内容 / 文案不变
- [ ] 6.2 板色 `#0d0b20` → `#2a2f45`（法兰 / 接管件色），内板 `#151330` → `#12162b`，描边 `${c}` → `#3f445c`
- [ ] 6.3 文字填充 `${c}`（主题色）保留，字号改为按机高 clamp（`plateFs = clamp(h*0.022, 3, 6.5)`，默认 180 → ≈4；`Φ3.5×54m` 用 `plateFs`、`回转窑` 用 `plateFs*0.8`），替换固定 `4` / `3.2`
- [ ] 6.4 铭牌位置 / 尺寸定尺：`plateW = clamp(min(plateFs*8, w*0.35), 20, 60)`、`plateH = clamp(plateFs*3.5, 8, 20)`、`plateX / plateY` 夹在画布内且底沿 `≤ baseY`（`clamp(…, 1, w-plateW-1)` / `clamp(…, 1, baseY-plateH)`）；默认 380×180 实测与改造前基本等价（板 `x∈[h_gearX+25, h_gearX+57]`、`y∈[h_cy+h_R+15, +29]`），极端 150×60 不再越底沿
- [ ] 6.5 校验：`grep` 确认 `nameplate` 与 `<text>Φ3.5×54m</text>` / `<text>回转窑</text>` 仍在、`#0d0b20` 残留 = 0、`node --check` 退出码 0；冒烟 `hasText=true`、动画数恒为 456；默认色 / 自定义色光栅化目视板色 / 文字色正常

## 4. 进度记录

| 步骤 | 状态 | 备注 |
| --- | --- | --- |
| 步骤 1 | ✅ 已完成 | 局部 `<defs>` + 金属 / 深腔 / 法兰配色 + 去 3 处 `gEquip` + 颜色兜底 + 粒子 / 运动件改基准色；校验全通过（`node --check` 退出码 0、多档尺寸 SMIL 恒为 456、无 `gEquip`/`undefined`/`NaN`、渐变 id 跨实例唯一、`kiln-temp` 契约完好、遗留色仅铭牌 `#151330` 待步骤 6） |
| 步骤 2 | ✅ 已完成 | 端口锚点参数化 + 断言固化（`inlet/outlet/fuel/flueGas`）；五档尺寸实测四端口比例全等，SMIL 恒为 456 |
| 步骤 3 | ✅ 已完成 | 螺栓 / 法兰 / 轴座统一画法 + 联轴器护罩 + 减速机 / 电机地脚螺栓 + 检修门 / 观察孔把手 + 挡轮轴座分层；校验全通过（`node --check` 退出码 0、五档端口比例不变、SMIL 恒为 456、光栅化目视无错压） |
| 步骤 4 | ⬜ 未开始 | 全部固定像素偏移改「比例 × 定尺封顶」；基础墩 / 铭牌 / 驱动 / 烟气粒子设上下限 |
| 步骤 5 | ⬜ 未开始 | `node --check` + 多尺寸冒烟 + 目视 + `kiln-temp` 回归 + SMIL 恒等 + 清理 |
| 步骤 6 | ⬜ 未开始 | 铭牌保留（`Φ3.5×54m` / `回转窑`）+ 配色对齐 + 定尺封顶 |
