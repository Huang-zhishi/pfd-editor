# 斗式提升机（bucketElevator）机头造型与出料口 · 对比方案

> 目标文件：`templates/bucketElevator.js`
> 风格基准：不变（金属渐变 `#7d849e → #d9dded → #767d97`、深色内腔 `#12162b`/`#6a7192`、法兰 `#2a2f45`/`#3f445c`、件体 `#9aa2bc`/`#5b6280`）
> 本文定位：**对比方案**（现状 vs 实机 → 改造方案与取舍），供确认后再进入代码实现
> 参照样式：`固体三通阀优化计划.md`、`冷却水池优化计划.md`

---

## 0. 用户诉求

> 「提升机优化一下，一点不符合设计，一般来说，正常的提升机顶部是圆弧形状的，出料口也不对，可以去网上看一下这方面的设计，汇总一份对比方案 md 给我」

拆成两条硬指标：

| # | 诉求 | 现状是否满足 |
| --- | --- | --- |
| ① | **机头顶部应为圆弧形** | ✗ 现为 `rx="2"` 的圆角矩形 |
| ② | **出料口位置/形式不对** | ✗ 现为挂在机头底壁、朝下敞开的梯形斗 |

---

## 1. 现状读码定位

### 1.1 机头顶部（非圆弧）

[bucketElevator.js](file:///d:/Java/GY/RS/templates/bucketElevator.js#L28-L29) 几何：

```js
const headX = w*0.06, headW = w*0.88, headTop = H(0.055, 0.12), headH = H(0.17, 0.34);
const headBot = headTop + headH;
```

[bucketElevator.js](file:///d:/Java/GY/RS/templates/bucketElevator.js#L224-L225) 造型：

```js
<rect x="${f(headX)}" y="${f(headTop)}" width="${f(headW)}" height="${f(headH)}" rx="2" fill="url(#${metalId})" .../>
<rect x="${f(headX+2)}" y="${f(headTop+2)}" width="${f(headW-4)}" height="${f(headH-4)}" rx="1" fill="#12162b" .../>
```

即**上下同宽、四个角只倒 R2 的矩形**，顶部是一条水平直线 —— 这就是"一点不符合设计"的直接观感来源。

### 1.2 出料口（形式与位置）

[bucketElevator.js](file:///d:/Java/GY/RS/templates/bucketElevator.js#L136-L149) 现状：

```js
const lampCX = w*0.845;                       // 与 outlet 端口同轴
const outThroatY = headBot;                   // 收口端贴机头【底壁】
const outMouthY  = headBot + headH*0.76;      // 斗口端【朝下敞开】
const outMouthW  = w*0.195, outThroatW = w*0.075;
```

形态 = 一只**倒扣的梯形斗**，上端收口贴在**机头底壁**、下端斗口**垂直朝下**，与进料斗是同一套画法整体翻转而来（L136-137 注释自述）。

端口契约（[L14-17](file:///d:/Java/GY/RS/templates/bucketElevator.js#L14-L17)）：

```js
{id:'outlet', x:.845, y:.174, dir:'down'},  // 机头下翻转卸料斗口
{id:'inlet',  x:.10,  y:.84,  dir:'up'}
```

### 1.3 连带发现：驱动与出料**同侧**

[L31-35](file:///d:/Java/GY/RS/templates/bucketElevator.js#L31-L35)：电机 `motX = cx + w*0.20`（右缘 `0.94w`），与出料口同处机头**右侧**；而真实机头的驱动多在出料口的**对侧**（见 §2.3）。这一条虽非用户直接指出，但改圆弧顶/侧出料时会被迫触碰。

---

## 2. 真实设计参考（网查汇总）

### 2.1 机头罩壳：剖分式 + 全圆弧 / 抛物线型

| 来源 | 原文要点 |
| --- | --- |
| GSI 物料手册（GS-019） | *"Two-piece hood for interior accessibility. **Full-radius contoured hood** to minimize wear and improve material discharge flow. Fully adjustable **throat plate**."* |
| GSI PNEG-681 安装手册 | *"The bonnet has been designed in **two-piece construction** to allow the **front (discharge) side** to slide forward... The rear (up-leg) side bonnet..."* |
| 左青《国产大型输送设备改进》 | *"机头外形**剖分式罩壳、抛物线弧形**，抛料壳体高度相对较小，内衬 A8 级带钢网内衬板，钢板厚度 ≥8mm…"* |
| YT Conveyor 同文英文版 | *"The shape of the machine head is a **split cover, parabolic arc**, and the height of the throwing shell is relatively small."* |

**结论**：机头顶部不是平口矩形，而是**随头轮外缘包络的圆弧/抛物线罩壳**，且分**前罩（卸料侧）**与**后罩（上行侧）**两片把合；"抛料壳体高度相对较小"——机头不宜做得又高又方。

### 2.2 出料口：侧面斜置溜槽 + 可调喉板，唇口落在抛料抛物线上

| 来源 | 原文要点 |
| --- | --- |
| FIRGELLI《Elevator Dumping Head》 | 物料绕头轮受重力+离心力，沿**与轮圆相切的抛物线**抛出；*"**Discharge Chute and Lip**: The angled chute that catches the thrown material. The lip must sit on the **parabolic trajectory line** for the design RPM, typically **200–400 mm forward of the pulley centreline**."* 唇口带长圆孔可再调向。 |
| GSI（同上） | 机头配备 ***fully adjustable throat plate***（可调喉板）与 ***optional discharge transitions***（卸料过渡段，即接在机头侧面的溜槽）。 |
| 卸载三形式（本科论文） | 顶部卸载分**离心式 / 离心重力式 / 重力式**三种；料斗绕驱动轮旋转，物料受重力 `mg` 与离心力 `mrw²` 合力作用，合力线与头轮垂直中心线交于极点 `p`。 |

**结论**：出料口是**开在机头侧壁（卸料/前罩侧）的斜置溜槽**，用来"接住"被离心力抛出的料流；溜槽唇口位于**头轮中心线前方**、且应落在抛料抛物线上；溜槽根部带**可调喉板**调节卸料间隙。**它不在机头正下方、也不是垂直朝下的漏斗。**

### 2.3 驱动布置：多在出料口的对侧

| 来源 | 原文要点 |
| --- | --- |
| 专利 CN207903309U《一种斗式提升机》 | *"所述头部壳体(2)**一侧设有出料口(5)**，**与出料口相对的另一侧设有驱动电机(7)**，驱动电机设置在支架上…"* |
| GSI（同上） | *"HEAD BEARINGS … are mounted **externally**"* —— 头轮轴承外置，驱动挂在机头侧外壁。 |

### 2.4 来源

- [GSI Bucket Elevators · 产品页（Full-radius contoured hood / throat plate）](https://simsconstruction.com/bucket-elevators/)
- [GSI Series II Bucket Elevator · 样本 GS-019](https://www.grainsystems.com/na/wp-content/uploads/sites/grainsystems/2015/06/GS-019-bucket-elevators.pdf)
- [GSI Series II Bucket Elevator · 安装手册 PNEG-681（两片式罩壳 / front-rear bonnet）](https://www.grainsystems.com/sa/wp-content/uploads/sites/grainsystems/2015/03/pneg681-042914-C.pdf)
- [左青《国产大型输送设备改进》（剖分式罩壳、抛物线弧形）](https://m.yzzzao.com/tech_news/detail-252932.html)
- [YT Conveyor · Conveying Equipment（split cover, parabolic arc）](https://ytconveyor.com/conveying-equipment/)
- [FIRGELLI · Elevator Dumping Head（溜槽唇口落在抛物线、头轮中心线前方 200–400mm）](https://www.firgelliauto.com/blogs/mechanisms/elevator-dumping-head)
- [专利 CN207903309U · 一种斗式提升机（出料口在一侧、驱动电机在对侧）](https://m.tianyancha.com/zhuanli/69a0d25987cf15b28217fcc85d91f5fd)
- [斗式提升机顶部卸载三形式：离心式 / 离心重力式 / 重力式](https://m.book118.com/try_down/535112010104011131.pdf)

---

## 3. 逐项对比表

| 项 | 现状实现 | 真实设计 | 差距 |
| --- | --- | --- | --- |
| 机头外轮廓 | 上下同宽矩形，四角 `rx=2` | 剖分式罩壳，顶部**圆弧/抛物线**包络头轮 | **顶部形状** |
| 机头分片 | 整块矩形 | **前罩（卸料侧）+ 后罩（上行侧）** 两片把合，中间有分界法兰 | 缺分界 |
| 机头高度观感 | `headH = min(0.17h, 0.34w)` | "抛料壳体高度相对较小"、随头轮包络 | 基本吻合，但平口 |
| 出料口位置 | 机头**底壁**下方（`outThroatY = headBot`） | 机头**侧壁**（下行/前罩侧） | **位置** |
| 出料口形式 | 垂直**朝下**的梯形斗 | 向外**斜下**的溜槽（spout/chute） | **形式** |
| 出料口对位 | 居中 `0.845w`，对称斗 | 唇口落在**抛料抛物线**上、头轮中心线前方 | 无抛料依据 |
| 喉板 | 无 | **可调喉板**（throat plate）调节卸料间隙 | 缺件 |
| 卸料机理表现 | 靠重力"漏下" | **离心抛料**切向抛入 | 观感不符 |
| 驱动位置 | 机头顶部、与出料口**同侧**（右） | 多在出料口**对侧**、轴承外置 | 可优化 |

> 补充：现有"料斗绕顶轮翻转、左链上行兜料 / 右链下行倒扣"的动画（L52-82）本身是对的，且**下行侧在右**（`chainR`），所以"出料侧应在右"这一点现状 x 方向没错，错的只是 y 高度与形态。

---

## 4. 改造方案

### 4.0 共同前提（务必先定）

机头 `headTop` / `headBot` 是几何链的**上游**：`casingTop = headBot`（L42）、`driveY = headTop - driveH`（L32）、`spR`/`topSpY` 又依赖 `headH`。因此改造必须**锁定 `headTop` 与 `headBot` 不变**，圆弧只从现有矩形内部"掏"出来，否则会级联改动机筒、链条、料斗、驱动，且 `headTop` 默认仅 `11.04px`（92×380 实测），向上加高会立刻顶出画布。

**实测约束（默认 92×380）**：`headW = 80.96`，`headH = 31.28`，`headW/2 = 40.48 > headH`。即**做不到真正的半圆**（真半圆需 `headH ≥ 0.44w`，而当前封顶 `0.34w`）→ 只能用**椭圆拱冠**近似。

### 4.1 机头顶部

#### 方案 A（推荐）· 椭圆拱冠 + 前后分片罩壳

- 外轮廓改**单条闭合 `<path>`**：左壁自 `headBot` 上到 `archY` → 椭圆弧 `A rx=headW/2 ry=arcRise` 拱到右壁 → 下到 `headBot` → 底边闭合
  - `archY = headTop + arcRise`，**`arcRise = min(headH*0.62, headW*0.42)`**（默认 ≈ 19.4，平直段 ≈ 11.9）
- 内腔同法内缩 1 个壁厚（`headX+2` / `headBot-2` / `rx-2` / `ry-2`），`#12162b` + `#6a7192` 描边
- **剖分线**：在 `y = archY` 处加一条横向把合法兰（`#2a2f45`/`#3f445c`，宽 = `headW + 2×悬出`）→ 落到实机的"前罩 / 后罩"
- **顶置驱动**：拱冠顶点处留一段水平短平台承载现有 `shaftSeat`/`reducer`，或按 §4.3 把驱动侧移

#### 方案 B（折中）· 拱冠 + 顶部短平台，驱动保持顶置

顶部以圆弧为主（拱肩圆弧半径加大），仅在 `cx` 附近保留 `w*0.28` 宽的水平平台给减速机坐落实；改动量最小，仍能一眼看出"顶部是圆的"。
**代价**：顶部不是纯弧，而是"圆肩 + 小平顶"。

#### 取舍

| | 方案 A | 方案 B |
| --- | --- | --- |
| 观感 | **最贴近实机**（纯弧顶） | 接近，顶面有一小段平 |
| 驱动 | 需侧移或加平台 | 原地不动 |
| 改动面 | 机头 + 驱动 | 仅机头 |
| 风险 | 中（驱动重排） | 低 |

> 若坚持"真半圆"，须放宽 `headH` 封顶（≥0.44w）→ 机头显著变高、`driveY` 上移出画布，**不建议**。

### 4.2 出料口

#### 方案 A（推荐）· 机头侧壁斜置溜槽

- **开口**：开在机头**右侧壁**（下行 / 前罩侧，`x = headX+headW`），开口中心高 `≈ topSpY`（头轮轴心高度），即物料绕顶轮抛出的位置
- **溜槽**：自开口向右下斜出，与水平约 **55°~65°**，沿机筒右侧下行；槽体画法与现有三件套一致（金属渐变槽壁 + 深色槽腔 + 端面法兰）
- **喉板**：开口处画一块可调喉板（`#9aa2bc`/`#5b6280` 小件 + 调节长孔），对齐"fully adjustable throat plate"
- **端口归位**：`outlet` 移到溜槽末端槽口
  - `spoutMouthX ≈ min(w - w*0.06, headX+headW + 水平投影)`，`spoutMouthY ≈ headBot + headH*0.4`（示意，实现时按抛物线唇位反推）
  - `dir` 保持 `'down'`（槽口朝下）或按实际倾角取 `'down'`
- **水平净空**：机筒右缘 `casingX+casingW = 0.74w`，到画布右缘 `1.0w` 有 `0.26w`，容得下溜槽 —— **x 方向可行**

#### 方案 B（最小改动）· 仅把底部斗改成斜置溜槽

保留原开口高度与端口坐标，只把"垂直朝下的梯形斗"倾斜成"斜下溜槽"造型。
**代价**：位置仍不在机头侧壁、仍不体现抛物线唇位，属于"形似"而非"对齐设计"。

#### 取舍

| | 方案 A | 方案 B |
| --- | --- | --- |
| 位置 | 机头侧壁 ✓ | 机头底壁 ✗ |
| 形式 | 斜置溜槽 ✓ | 斜置溜槽 ✓ |
| 端口 | **需移动**（契约变更） | **不动** |
| 观感 | 对齐实机 | 部分对齐 |

### 4.3 驱动侧移（可选，建议同期）

把电机/减速机由机头顶置右移→改到机头**出料口的对侧**（左侧）或侧外壁轴承座，对齐专利 CN207903309U 与 GSI 外置轴承。
**代价**：`reducer`/`backstop`/`shaftSeat`/`coupling`/`motor` 五个子件的坐标与 `M` 铭牌全部重排；若选方案 A 的纯弧顶，此项**即变为必做**。

---

## 5. 对外契约影响

| 契约 | 影响 |
| --- | --- |
| `TEMPLATES.bucketElevator` key / `name` / `category` | 不变 |
| `defaultSize: {w:92, h:380}` | 不变（方案 A/B 均在现有 `w×h` 内收口） |
| `ports[outlet]` | **方案 A：坐标必变**（`0.845, 0.174` → 溜槽末端实测值）；方案 B：不变 |
| `ports[inlet]` | 不变 |
| 连接点/引线锚点 | 若 `outlet` 移动，已有图纸上该端口的连线锚点会随之移动 → **需在实现时明确告知** |
| 几何链 | 锁定 `headTop`/`headBot` → `casingTop`、`spR`、`topSpY`、料斗/链条/SMIL 时长**全部不变** |
| 定尺封顶 `H(hr,wr)` | 沿用；新增 `arcRise` 也按 `min(比例, 上限)` 封顶，放大时顶部不变形 |
| 极小尺寸守卫 | 新增：`arcRise` 与拱冠 `ry` 需 `Math.max()` 下限，避免 `h` 极小时拱冠塌成直边或 `ry=0`（参考冷却水池步骤 7 的守卫写法） |
| `clipPath` | 机头改 `<path>` 后，裁剪矩形 `y=headTop`（L216）需同步为拱冠顶，否则顶部圆弧内腔被裁 |
| 端口反推 | `outlet` 若移动，须由溜槽几何**反推**端口坐标（沿用固体三通阀"端口与画法对齐"做法，勿写死） |

---

## 6. 分步实施建议（待确认）

> 遵循既有惯例：先方案、确认后分步改、每步回写本文档、临时校验脚本用完即删。

- [ ] **步骤 1**：机头改椭圆拱冠（方案 A 或 B）——外轮廓 + 内腔单条闭合 `<path>`，锁 `headTop`/`headBot`，`clipPath` 同步
- [ ] **步骤 2**：拱冠剖分法兰（前罩 / 后罩分界）
- [ ] **步骤 3**：出料口改机头侧壁斜置溜槽 + 可调喉板
- [ ] **步骤 4**：`outlet` 端口按溜槽反推归位（若采方案 A）+ 更新端口注释
- [ ] **步骤 5**：（可选）驱动侧移，对齐"出料口对侧"
- [ ] **步骤 6**：定尺封顶与极小尺寸守卫；`node --check` + 多档尺寸冒烟 + 浏览器核对

**需先确认的选项**：

1. 机头顶部：**方案 A（纯弧顶，驱动需侧移/加平台）** 还是 **方案 B（圆肩+小平顶，驱动不动）**？
2. 出料口：**方案 A（侧壁溜槽，端口需移动）** 还是 **方案 B（仅改造型，端口不动）**？
3. 是否同期做 **驱动侧移**（§4.3）？
