# 除尘布袋（dustCollector）风格对齐 · 分步优化计划

> 目标文件：`templates/dustCollector.js`
> 风格基准：`templates/bucketElevator.js`（斗式提升机）、`templates/reactor.js`（反应釜）
> 参照样板：`templates/screwConveyorLite.js`（上一轮已按同一套步骤改造完成）
> 本文档用于记录每一步的进度，异常中断后从第一个未勾选项继续。

## 0. 风格基准清单（必须照搬）

| 项 | 取值 |
| --- | --- |
| 金属渐变 | `#7d849e → #d9dded → #767d97`（局部 `<defs>`，id 带 uid 后缀，不用外部全局 `gEquip`） |
| 深色内腔 | `#12162b`，描边 `#6a7192` |
| 法兰 / 接管 | `#2a2f45` 填充 + `#3f445c` 描边 |
| 件体（金属件明细） | `#9aa2bc` + `#5b6280` |
| 门板 / 铰链 / 把手 | `#1a1f33` / `#3a4060` / `#8e96b6` |
| 定尺封顶 | `H(hr, wr) = Math.min(h*hr, w*wr)`；或逐量 `Math.max(下限, Math.min(上限, 比例量))` |

## 1. 现状问题清单（读码定位结果）

- [x] P1 依赖外部全局渐变：7 处 `fill="url(#gEquip)"`（L32/54/102/110/123/201/202）→ 改局部 `<defs>` + uid。
- [x] P2 配色不符基准：`#0d0b20`（法兰/后壁）、`#0a0818`（内部深色层）→ 改 `#2a2f45`/`#3f445c`、`#12162b`/`#6a7192`。
- [x] P3 颜色无兜底：`const c = p.color;`（L12）→ `(p && p.color) || '#9C99FF'`。
- [x] P4 死代码：`const box`（L50–69）定义后未被使用，return 中（L198–224）把同一批图元又内联重写一遍 → 合并去重。
- [ ] P5 定尺缺失：`-3`/`+3`/`-6`/`r="1.8"`/`height="4"`/`bagTop = bodyTopY+6` 等固定像素偏移不随尺寸缩放 → 引入封顶量。
- [ ] P6 滤袋数量固定 6 只，箱体加宽/加高时不自适应 → 按箱体尺寸自动增减。
- [x] P7 无观察门/把合法兰/标准节等提升机式细节 → 步骤 2 / 步骤 3 补齐（检修门、顶底法兰条 + 螺栓、箱身把合法兰自动增多、进气/净化气/出料三处接管统一画法）。

## 2. 必须保持的对外契约（不得改动）

- 模板 key：`TEMPLATES.dustCollector`
- `name: '除尘布袋'`、`category: '设备'`、`defaultSize: { w: 120, h: 180 }`
- `ports`（**步骤 6 设计变更后**）：`inlet (1, 0.4, right)` 右壁中部 / `outlet (0.5, 1, down)` 底部 / `cleanGas (1, 0.2, right)` 右侧顶部
- 动画标签形式：`<animateTransform>` / `<animate>`（编辑态由 `editor.js stripSMIL()` 剥离）
- 已有的星型卸料阀旋转（3s）、灰尘粒子（4.5s）、脉冲喷吹（0.8s）保留

## 3. 分步执行

### 步骤 1：主箱体 + 灰斗对齐风格
- [x] 1.1 新增局部 `<defs>`：`metalId`（竖向渐变，给箱体/灰斗/星型阀）、`metalVId`（横向渐变，给进气/出料接管），id 带 uid 后缀
- [x] 1.2 箱体 rect / 灰斗 polygon：`fill="url(#gEquip)"` → `url(#${metalId})`，描边 `#3f445c`
- [x] 1.3 深色内腔：`#0a0818` → `#12162b`，描边 `#6a7192`
- [x] 1.4 法兰/后壁：`#0d0b20` → `#2a2f45`，描边 `#3f445c`
- [x] 1.5 `const c = p.color;` → `const c = (p && p.color) || '#9C99FF';`
- [x] 1.6 删除死代码 `const box`，让 return 中的内联图元成为唯一定义
- [x] 1.7 校验：`node --check` 退出码 0；4 档尺寸渲染无 `gEquip` 残留、渐变 id 唯一、颜色兜底生效；浏览器 5 档目视（120×180 / 自定义色 / 60×90 / 240×360 / 600×300）金属壳 + 深腔已生效
- 备注：留待步骤 4 的已知问题 —— 240×360 / 600×300 下滤袋只有 6 列、被拉得过长；法兰/螺栓仍是固定像素。

### 步骤 2：提升机式细节
- [x] 2.1 顶/底法兰条按基准色重画（`#2a2f45` + `#3f445c`），螺栓改 `#8e96b6`（`topFl` / `botFl` / `bolt()`）
- [x] 2.2 箱体侧壁把合法兰：箱体加高时自动增多（`nSec = Math.max(2, Math.round(bodyH/(w*0.34)))`，`joints` 循环），厚度 `jointH` 封顶
- [x] 2.3 箱体检修门（`door`：`#1a1f33` 门板 + `#3a4060` 铰链×2 + `#8e96b6` 把手），贴左壁下半段，尺寸按箱宽/箱高封顶
- [x] 2.4 净化气出口接管 `cleanPort`（`cleanPortY = h*0.3` 与端口严格同轴），金属管 + 深色管腔 + 端面法兰
- [x] 2.5 铭牌保留并对齐配色（`#2a2f45` / `#3f445c` / 文字用 `${c}`）
- [x] 2.6 校验：`node --check` 退出码 0；6 档尺寸冒烟无 `gEquip` / 无 NaN；浏览器目视 5 档通过；`ports` 原样未变
- 附注：顺带修掉 `bodyLeft` / `bodyRight` 重复声明（原 L34-35 与 L75-76，会让 `templates.js` 加载时报 SyntaxError）。
- 备注：`240×360` / `600×300` 下滤袋仍是 6 列且被拉长、门板随箱高变大 → 留待步骤 4。

### 步骤 3：进风口 / 出料接管 / 灰斗斗体
- [x] 3.1 进气口 `inlet`：方形把合法兰板（`#2a2f45`/`#3f445c`）+ 短接管（`metalVId` 管壁 + `#12162b`/`#6a7192` 管腔）+ 3 颗螺栓；法兰厚 `inFlH`、管壁厚 `inWall` 封顶
- [x] 3.2 灰斗：拆成 `hopperBack`（第 1 层后壁，外扩 wallT）/ `hopperBody`（第 2 层金属渐变斗体）两段，接上 `#0c1020` 落料颈 `hopperThroat`；斗壁厚 `hopWall = wallT*0.9` 与箱壁同厚，`clipDef` 漏斗边界同步内缩（`hopInBot`），暗层不再溢出斗壁
- [x] 3.3 星型卸料阀 `starValve`：壳体金属渐变 + `#12162b`/`#6a7192` 深腔 + 两端 `#2a2f45` 端盖（`starWall`/`starCapW` 封顶），十字转子 `${c}` 与 3s `animateTransform` 保留
- [x] 3.4 出料口 `outletFlange`：法兰厚改用封顶 `flH` + 2 颗螺栓，卸料短管金属管壁 + 深色管腔，管壁厚 `outWall` 封顶
- [x] 3.5 校验：`node --check` 退出码 0；5 档尺寸冒烟无 NaN / 无 `gEquip` / 渐变 id 唯一 / 动画数 34 / `#0c1020` 存在；浏览器 5 档目视通过（灰斗分层、落料颈连贯、星型阀深腔与端盖、进气口方形法兰均正确）；editor 实机加载无报错
- 附注：`60×90` 及更小尺寸下 `neckH ≤ 0.5`，落料颈自动省略（避免出现负高矩形），灰斗口直接对接阀体。
- 备注：灰斗内部仍为大面积 `#12162b`（与箱体剖切画法一致，金属斗壁为斜边一圈）；`240×360` / `600×300` 滤袋仍 6 列被拉长、检修门偏大 → 留待步骤 4。

### 步骤 4：定尺封顶与自适应
- [x] 4.1 逐量 clamp：脉冲清灰装置 `pulseH = Math.max(2, Math.min(4, h*0.02))`、`pulseY = topY + Math.max(1.5, padT*0.5)`；新增内衬量 `padT = Math.max(2, Math.min(5, wallT*0.5))`、`innerL/innerW`；出料短管长 `outPipeH = Math.max(4, Math.min(10, h*0.04))`；花板厚 `tsH = Math.max(2.2, Math.min(4.5, bodyH*0.075))`；滤袋直径 `bagW0 = Math.max(2.5, Math.min(12, bodyW*0.075))`；铭牌 `npW = Math.max(8, Math.min(26, bodyW*0.24))`、`npH = Math.max(3.5, Math.min(11, npW*0.42))`；顶部量 `topY = Math.max(4, Math.min(14, h*0.055))`
- [x] 4.2 滤袋数量与间距自适应：`bagZoneW = innerW - padT`；`bagCount = Math.max(2, Math.min(20, Math.round(bagZoneW/(bagW0*2))))`；`bagSpacing = bagZoneW/bagCount`；`bagW = Math.min(bagW0, bagSpacing*0.62)`；花板开孔 `tsHoles` 与袋笼 `bags` 同循环统一生成
- [x] 4.3 替换固定像素偏移：`tubeSheet` / `frame` / `dustParticles` / `pulseDust` / `nameplate` / 内部深色层矩形改用 `innerL`/`innerW`/`bagTop`/`bagBot`/`hopperBotY`/`padT` 等比例量；粒子半径 `Math.max(0.8, bagW*0.2)`、喷吹距离 `Math.max(6, Math.min(14, bodyH*0.1))`
- [x] 4.4 校验：`node --check` 退出码 0；6 档冒烟 `NaN=false / gEquip=false / grads=2 / uniq=2 / anims=34`，自定义色与颜色兜底、跨实例 uid 唯一均通过；滤袋列数自适应 20×20=3 / 40×60=6 / 60×90=7 / 120×180=7 / 240×360=9 / 600×300=21；浏览器 7 档目视通过（极小尺寸元素拥挤但可辨识、无溢出；宽箱体 21 列布局合理；定尺件不随尺寸变粗）
- 附注：顺带修掉 `outPipeH` 重复声明（L28 新增封顶量后 L172 旧声明未删，同块级作用域重复 `const` 会致 SyntaxError）。
- 遗留：clipPath 矩形（±1/±2）、`cleanPort`（0.9/1.8/1.6）、`door`（1.6）等 1～2px 级描边/留边偏移按原样保留，非结构性失真来源。

### 步骤 5：整体校验
- [ ] 5.1 `node --check templates/dustCollector.js` 退出码 0；`GetDiagnostics` 无告警
- [ ] 5.2 浏览器目视：默认 120×180、demo 实机尺寸
- [ ] 5.3 编辑态 `stripSMIL` 生效（`animateTransform` 计数为 0）
- [ ] 5.4 运行态动画恢复（星型阀 / 灰尘粒子 / 脉冲喷吹）
- [ ] 5.5 镜像 `mirrored`、旋转 `rotation`、`namePos` 正常
- [ ] 5.6 临时核对页删除，清理产物

### 步骤 6：设计变更（气流路径重排）
> 用户指令：出口顶部管道删除、出口移到右侧顶部、入口移到中部、滤网偏上占仓体三分之一、气流由入口进经滤网从出口出、铭牌删除。
> 澄清答复：入口落位 = **右壁中部**；滤网摆法 = **花板贴箱顶、滤袋缩短**；底部出料口与星型卸料阀 = **保留**。

- [x] 6.1 删除顶部立管：`topY` / `inletW` 常量、原顶部方形把合法兰 + 竖管 + 3 螺栓整块，以及嵌在其下的脉冲清灰装置 `pulse`（`pulseW/pulseH/pulseX/pulseY`）全部删除，return 中移除 `${inlet}` / `${pulse}`
- [x] 6.2 出口移到右侧顶部：`cleanPortY` 由 `h*0.3` 改为 `h*0.2`，与 `ports.cleanGas.y=0.2` 严格同轴
- [x] 6.3 入口移到右壁中部：新增 `inletPort`（`inletPortY = h*0.4`，与 `ports.inlet.y=0.4` 同轴），画法统一为金属渐变管壁 + `#12162b`/`#6a7192` 深色管腔 + 端面把合法兰；管径 `ipH` 比净化气出口 `cpH` 略粗（含尘气量大）
- [x] 6.4 滤网改为占仓体上 1/3：`bagBot` 由 `bodyBotY-padT` 改为 `Math.max(bodyTopY + bodyH/3, bagTop + 2)`，花板 `tsY` 仍贴箱顶内邊；下方留出含尘气缓冲区
- [x] 6.5 箱宽收窄 `bodyW` 由 `w*0.85` → `w*0.70`，给右壁两根接管（`cleanPort` / `inletPort`）留出可见管长（顶部立管取消后右壁接管是唯一进出口）
- [x] 6.6 删除铭牌 `nameplate`（原文字 `MC-3000`）及 return 中的 `${nameplate}`
- [x] 6.7 校验：`node --check` 退出码 0；6 档冒烟（120×180 / 120×350 / 60×140 / 240×700 / 400×300 / 160×420）全部 `bad=false`、`anims=34`、`gEquip=false`、`nameplate=false`；正则提取右壁接管矩形中心线与 `ports` 同轴（如 120×180 → `[36,72]` = `h*0.2/h*0.4`）；滤袋区占箱体高度 **33.3%**；浏览器目视 120×180(k=4) / 400×300(k=1) / 60×140(k=4) 通过 —— 顶部无立管、花板贴顶、滤袋缩到上 1/3、右侧上下两根接管可见、观察门与灰斗/星型阀/出料法兰正常
- 附注：右壁接管长 = `w - (cx + bodyW/2 + wallT)` ≈ `0.10w`，小尺寸（60 宽 → 约 6px）接管偏短但可辨识；极端小尺寸如需加长可再收 `bodyW`。
- 遗留：`pulseDust` 喷吹粒子动画保留（现语义为自花板向下冲刷滤袋，与已删除的顶部脉冲装置无关）。

## 4. 进度记录

| 步骤 | 状态 | 备注 |
| --- | --- | --- |
| 步骤 1 | 已完成 | 局部 defs 金属渐变 + 深腔 #12162b + 法兰 #2a2f45/#3f445c；颜色兜底；删 box 死代码；node --check 通过，浏览器 5 档目视通过 |
| 步骤 2 | 已完成 | 顶/底法兰条 + 螺栓、箱身把合法兰（自动增多）、检修门、净化气金属接管（h*0.3 同轴）；修掉 bodyLeft/bodyRight 重复声明；node --check 通过，浏览器 5 档目视通过 |
| 步骤 3 | 已完成 | 进气口方形法兰 + 短管、灰斗拆后壁/斗体两层 + `#0c1020` 落料颈、星型阀金属壳/深腔/端盖（动画保留）、出料法兰对齐；node --check 通过，浏览器 5 档目视通过 |
| 步骤 4 | 已完成 | 逐量 clamp（padT/innerL/innerW/topY/outPipeH/tsH/bagW0/npW/npH/pulseH）+ 滤袋列数自适应（3~21 列）+ 固定偏移替换；修掉 outPipeH 重复声明；node --check 通过，6 档冒烟全绿，浏览器 7 档目视通过 |
| 步骤 5 | 进行中 | 整体校验与产物清理 |
| 步骤 6 | 已完成 | 设计变更：删顶部立管与脉冲装置、删铭牌；出口移右上（`h*0.2`）、新增右壁中部进气接管（`h*0.4`）；滤袋改占箱体上 1/3；`bodyW` 0.85→0.70 给右壁接管留管长；node --check 通过，6 档冒烟全绿，浏览器 3 档目视通过 |
