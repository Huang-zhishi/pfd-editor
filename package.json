{
  "name": "pfd-editor",
  "version": "1.0.0",
  "private": true,
  "description": "工艺流程组态编辑器（原生 JS + SVG/SMIL 前端 + 零依赖 Node 服务端）",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check editor.js && node --check templates.js",
    "render-check": "node .github/scripts/render-check.js",
    "test": "npm run check && npm run render-check",
    "lint": "eslint . --max-warnings 2",
    "lint:fix": "eslint . --fix"
  },
  "comment": "运行时零依赖：dependencies 为空是刻意设计。此文件只用于声明版本、Node 版本约束与脚本入口。",
  "devDependencies": {
    "eslint": "^10.11.0",
    "globals": "^17.12.0"
  }
}
