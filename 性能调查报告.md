# PFD Editor 卡顿问题调查报告

> 调查时间：2026-09-21
> 调查对象：`editor.html` + `editor.js` + `templates/*.js`（34 个组件模板）
> 用户反馈：**在笔记本上操作非常卡**

---

## 0. 结论速览（TL;DR）

**最要命的一条：左侧组件库那 34 个图标，是"完整模板渲染"出来的，里面塞了 2109 个 SMIL 动画节点，而且它们不在 `#canvas` 里、不受编辑态 `pauseAnimations()` 管控 —— 也就是说你打开编辑器什么都不做，浏览器也在满负荷跑 2109 个动画。**

| 排名 | 卡顿原因 | 实测证据 | 优化性价比 |
| --- | --- | --- | --- |
| 1 | 组件库图标含 2109 个后台动画 | 组件库 5561 节点 / 2109 个 `<animate>`，且不受暂停管控 | ★★★★★ 改 1 行、立竿见影 |
| 2 | 模板动画节点爆炸 | 画布 12 个组件就有 1171 个动画节点；单个 `tubeCooler` 614 个、`rotaryKiln` 456 个、`miningDryer` 417 个 | ★★★★★ |
| 3 | `renderAll()` 全量重建，且拖拽改尺寸时**每帧**都调 | 12 个组件 22.5 ms/次；全仓 62 处调用；改尺寸分支在 mousemove 里直接 `renderAll()` | ★★★★★ |
| 4 | 每次渲染都换随机 id（`Math.random()`） | 34 个模板每个都调一次，`<defs>` 渐变/裁剪 id 每次都变 | ★★★★☆ 改造成本低 |
| 5 | 粒子用独立 DOM 节点 + 逐粒子 SMIL | `circle` 节点 473 个（12 组件）；还有对 `points` 属性做动画的写法 | ★★★☆☆ 改动量中等 |
| 6 | 运行态流动粒子每帧写 2625 次属性 | 35 管道 × 3 组 × 5 节点 × 5 属性 | ★★★☆☆ |
| 7 | 任意布局变化要 29.3 ms | 整页强制 reflow 实测 | ★★☆☆☆ 属结果，随上面几条一起改善 |

**先做 1 和 3，用户主观感受就能立刻好转；2 决定上限。**

---

## 1. 测量环境与方法

| 项 | 说明 |
| --- | --- |
| 环境 | 本机 headless Chrome，`--disable-gpu`（软件渲染）、`file://` 打开 |
| 场景 | 按用户真实工程「1#还原系统」的**同类型同数量**搭场景：29 个组件（含回转窑 ×1、回转滚筒冷却机 ×1、烘干机类 ×1、除尘 ×1、脱硫塔 ×1、风机 ×4、泵 ×4、螺旋输送机 ×5…）+ 35 条管道 |
| 计时 | `performance.now()`，**不带 `--virtual-time-budget`**（虚拟时钟会让 `performance.now()` 冻结，测出来全是 0） |
| 脚本 | 临时基准脚本用完即删，未入库 |

> ⚠️ 口径说明：软件渲染下的绝对毫秒数比真实笔记本（尤其带独显/核显差异）会偏悲观或偏乐观，**但节点数量、动画数量、以及各路径的相对快慢是可信的**，报告里主要依据这些。

---

## 2. 实测数据

### 2.1 规模（节点与动画数量）

| 区域 | DOM 节点 | 其中 SMIL 动画节点 |
| --- | --- | --- |
| **左侧组件库**（34 个图标） | **5561** | **2109** ⚠️ |
| 画布（12 个组件 / 12 条管道） | 1506（circle 473 / rect 230 / path 47 / text 21） | **1171** |
| 整页合计（空画布） | 3671 | 2109 |

按用户真实工程（29 组件）外推，**画布侧动画节点约 2500~3000 个**，加上组件库，整页 SMIL 动画节点在 **4500~5000 量级**。

### 2.2 各路径耗时（12 组件场景）

| 路径 | 耗时 | 评价 |
| --- | --- | --- |
| `renderAll()` 全量渲染 | **22.5 ms/次** | ✗ 一帧只有 16.7 ms，单次全量渲染就掉帧 |
| 组件 render 生成字符串 | **14.0 ms（473 KB）** | ✗ 字符串拼接 + 解析都贵 |
| `stripSMIL()` 正则剥离 | 1.7 ms（473 KB → 164 KB） | 剥离后仍剩 65% 是动画节点 |
| `buildPalette()` 重建组件库 | **65.6 ms/次** | ✗ 组件库搜索框每敲一个字都会触发 |
| 整页强制 reflow | **29.3 ms** | ✗ 任何布局变化都掉 2 帧 |
| `incrementalMove()`（拖动单帧） | **0.07 ms** | ✓ 这条路径已经优化得很好 |
| `renderPropsLive()`（面板实时刷新） | ~0 ms | ✓ |
| 实时数据刷新一轮 | 0.04 ms | ✓ |
| `zoomFit()` | 7.9 ms | 可接受 |

### 2.3 最贵的模板（单次 render / 自带动画节点）

| 模板 | 耗时 | 动画节点 |
| --- | --- | --- |
| `tubeCooler`（回转滚筒冷却机） | 9.40 ms | **614** |
| `miningDryer`（烘干机） | 8.32 ms | **417** |
| `rotaryKiln`（回转窑） | 5.00 ms | **456** |
| `desulfTower`（脱硫塔） | 2.32 ms | 249 |
| `classifier`（选粉机） | 0.84 ms | 54 |
| `cyclone` / `vrm` / `dustCollector` | 0.5 ms 级 | 28~60 |

这 4 个模板合计贡献了画布侧 **1736 / 1171** 的动画节点（前三个就占绝大部分）。

---

## 3. 为什么会卡：逐条根因

### 3.1 组件库图标带着 2109 个动画在后台跑（**首要原因**）

`editor.js:1221` 生成组件库图标时，直接把模板的完整渲染结果塞进缩略图：

```js
const sv = `<svg viewBox="0 0 ${t.defaultSize.w} ${t.defaultSize.h}" ...>${t.render(...)}</svg>`;
```

- 没有 `stripSMIL()`，所以 34 个图标把每个模板的动画全带上了 → **2109 个 `<animate>`**；
- 编辑态暂停动画靠的是 `svg.pauseAnimations()`，而 `svg = $('canvas')`（`editor.js:36`），**组件库图标在 `#paList` 里、不在 `#canvas` 内，根本不受管控** → 这 2109 个动画一直在跑；
- 附带问题：`buildPalette()` 每次重建要 65.6 ms，而组件库搜索框的每次输入都会调用它。

**这一条与用户"什么都不做也卡"的感受完全吻合。**

### 3.2 模板动画节点数量爆炸

粒子（物料、气流、水）都是"**一个粒子 = 一个 DOM 节点 + 2~3 个 `<animate>`**"：

```js
// templates/tubeCooler.js 片段
<animate attributeName="y" values="${yVals.join(';')}" dur="..." begin="${delay}s" repeatCount="indefinite"/>
<animate attributeName="opacity" values="${opVals.join(';')}" .../>
...
<animate attributeName="points" values="${tPts.join(';')}" .../>   // ← 对 points 属性做动画，最贵
```

- `tubeCooler` 单个组件就 614 个动画节点、`rotaryKiln` 456 个、`miningDryer` 417 个；
- 对 `points` 做 SMIL 动画意味着浏览器**每帧重新解析整条多边形点串**，是所有 SMIL 里最贵的一类；
- 这些动画都是 `repeatCount="indefinite"`，运行态下全部同时在跑。

### 3.3 `renderAll()` 全量重建，且改尺寸时每帧都调

`renderAll()` 的做法是**清空三个图层再整体重建**：

```js
layerPipes.innerHTML = ''; layerEquip.innerHTML = ''; layerTop.innerHTML = ''; layerFlow.innerHTML = '';
```

- 12 个组件就要 22.5 ms，用户真实工程（29 组件）约 **50 ms/次** → 每调一次掉 3 帧；
- 全仓 **62 处**调用 `renderAll()`；
- 最严重的是拖拽改尺寸分支（`editor.js` `processMouseMove`）：

```js
if(resizeState){
  ...
  comp.x=snapV(nx); comp.y=snapV(ny); comp.w=nw; comp.h=nh;
  renderAll(); renderPropsLive(); setDirty();     // ← mousemove 每帧全量重建
}
```

**拖拽移动**走的是 `incrementalMove()`（0.07 ms/帧，很好），但**拖拽缩放**走的是全量重建 —— 这就是"一拉尺寸就卡成幻灯片"的直接原因。

### 3.4 每次渲染都换随机 id

34 个模板每个开头都是：

```js
const uid = Math.random().toString(36).substr(2,6);
const metalId = 'xxx_metal_' + uid;   // <defs> 里的渐变/裁剪 id
```

后果：每次渲染 `<defs>` 的 id 都变 → 浏览器无法复用上一次的渐变/裁剪与已光栅化的图层 → 每次全量渲染都要重建这些定义。

### 3.5 其他次要项

| 项 | 说明 |
| --- | --- |
| 运行态流动粒子 | 每帧对每条管道 `3×(1+FLOW_TRAIL)=15` 个节点写 `cx/cy/r/fill/opacity`，35 条管道 = **每帧 2625 次 `setAttribute`** |
| 整页 reflow 29.3 ms | 任何引起布局变化的面板刷新（如 `renderPropsLive`）都要付这个代价 |
| 图层规模 | 12 组件就有 473 个 `circle`，多为粒子；节点越多，每次重绘越贵 |

---

## 4. 优化方向（按性价比排序）

### A. 组件库图标剥掉动画 + 缓存缩略图 ★★★★★（建议立刻做）

- **做法**：`buildPalette()` 里对模板输出套 `stripSMIL()`；缩略图按 `type + 尺寸` 缓存成字符串，搜索过滤时不重新渲染模板。
- **预期收益**：整页后台动画 **2109 → 0**；组件库重建 **65.6 ms → 几 ms**。
- **成本**：约 10 行。**风险**：无（缩略图本来就不需要动）。

### B. 拖拽改尺寸改为增量更新 ★★★★★

- **做法**：照抄已经写好的 `incrementalMove()` 思路，给 resize 也写一个"只改被拖组件那几个节点的 transform/尺寸属性"的增量函数；松手时再 `renderAll()` 一次。
- **预期收益**：缩放从 **22.5 ms/帧 → 亚毫秒/帧**，主观感受提升最明显。
- **成本**：中等（需要处理"改尺寸会改变内部布局"的组件，可先只做"整体缩放 + 松手重渲染"的近似）。

### C. 削减模板动画节点 ★★★★★（决定上限）

- **做法（按优先级）**：
  1. **去掉对 `points` 的 SMIL 动画**，改用 `<animateTransform>` 平移/旋转，或改用编辑器的粒子层（canvas/RAF）统一画；
  2. 粒子**按"组"动画**而不是"逐粒子动画"：同一批粒子用一个 `<animate>` 驱动外层 `<g>` 的 transform；
  3. 粒子数按组件尺寸自适应，给一个**硬上限**（例如单组件最多 40 个粒子）；
  4. 编辑器编辑态一律 `stripSMIL()`（现在已做），运行态再按需注入。
- **预期收益**：`tubeCooler` 614 → 几十个，画布侧总动画数可降 **70%~90%**。
- **成本**：较大（要逐模板改 `tubeCooler` / `rotaryKiln` / `miningDryer` / `desulfTower`），建议一个一个来、每改一个用现有 `render-check.js` 回归。

### D. 稳定 defs id ★★★★☆

- **做法**：模板里 `uid` 改为由组件 id（或类型+实例序号）派生并传入，保证同一组件每次渲染的 `<defs>` id 不变；同类型多实例仍不冲突。
- **预期收益**：减少 `<defs>` 重建与图层失效，全量渲染可省下一部分时间。
- **成本**：低（模板改一行 + 传参约定）。

### E. 运行态粒子层改造 ★★★☆☆

- **做法**：管道粒子改为在单个 `<canvas>` 上绘制（或用一个 `<g>` + CSS 动画），不再对 2625 个属性逐帧赋值。
- **预期收益**：运行态 CPU 占用显著下降，笔记本风扇会更安静。
- **成本**：中等。

### F. 大工程的分层与可见性裁剪 ★★☆☆☆

- **做法**：视口外的组件不渲染（或渲染成一张静态缩略图）；超大规模时按需渲染。
- **预期收益**：组件数量继续增长时（50+）才有必要。
- **成本**：较高。

---

## 4.5 已实施：A + B（2026-09-21 实测数据）

### A. 组件库缩略图剥掉 SMIL + 按类型缓存 —— 已实施

`editor.js` 新增 `paletteThumb(type, t)`：模板渲染结果套 `stripSMIL()` 后按类型缓存，`buildPalette()` 与拖拽幽灵（`startPaletteDrag`）都改用它。

| 指标 | 改前 | 改后 |
| --- | --- | --- |
| 组件库 SMIL 动画节点 | **2109** | **0** |
| 组件库 DOM 节点 | 5561 | 3452（−38%） |
| `buildPalette()` 首次 | 65.6 ms | **22.0 ms** |
| `buildPalette()` 带筛选（搜索框每敲一字） | 65.6 ms | **1.0 ms** |

### B. 拖拽改尺寸改增量刷新 —— 已实施

`editor.js` 新增 `incrementalResize(comp, ow, oh)`：拖拽过程中只更新外框 transform、内容等比缩放预览（把按旧尺寸渲染的内容 `scale` 到新尺寸、中心对齐，旋转与镜像一并处理）、标签、相连管道与选中框；松手时 `mouseup` 里本来就会 `renderAll()` 一次，内容按新尺寸重画。resize 分支里的 `renderAll()` 已替换为它。

| 指标 | 改前 | 改后 |
| --- | --- | --- |
| 改尺寸每帧耗时 | **40.6 ms/帧**（11 组件，含回转窑/冷却机/烘干机） | **0.07 ms/帧** |

验证：正常 / 镜像 / 旋转 90° 三种组件的缩放预览 transform 均正确（`translate(0,0) scale(1.6,1.6)`、`translate(144,0) scale(-1.6,1.6)`、旋转走外框），相连管道同步重算，松手后全量重建把内层缩放清掉、按新尺寸重画。

> C/D/E/F 尚未实施。**C（削减模板动画节点）是下一个大头**，决定画布侧上限。

---

## 5. 建议的实施顺序

1. ~~**A（组件库剥动画 + 缓存）**~~ —— ✅ **已实施**（后台动画 2109 → 0，带筛选重建 65.6 → 1.0 ms）
2. ~~**B（改尺寸增量更新）**~~ —— ✅ **已实施**（40.6 → 0.07 ms/帧）
3. **D（稳定 defs id）** —— 顺手做，降低全量渲染成本；
4. **C（削减模板动画节点）** —— 按 `tubeCooler → rotaryKiln → miningDryer → desulfTower` 顺序逐个改，每改一个回归一次；
5. **E / F** —— 视后续工程规模再决定。

---

## 5.5 补充调查：还没做的方向（2026-09-21 第二轮，29 组件实测）

A/B 做完后再扫一遍，新增的量化结论：

| 项 | 实测 | 判断 |
| --- | --- | --- |
| `layerEquip` 图层节点数 | **2729**（29 组件），`layerPipes` 只有 168 | 画布侧成本几乎全在组件内部，**降单组件节点数 = 降大头** |
| 画布 `linearGradient` 定义数 | **51**（29 组件），`clipPath` 21 | 每个实例各带一份 defs；同款渐变可上提到根 `<defs>` 共用 |
| 运行态管道粒子写入 | 每帧约 **2100 次 `setAttribute`** | 独立方向，见 E |
| `pushHistory()` | **0.06 ms / 12 KB**（29 组件） | ✅ **不是瓶颈，不建议动** |
| `routePipe` 全量重算 | **0.08 ms/次**（28 管道） | ✅ **不是瓶颈，不建议动** |
| 网格矩形 | `100×100`，而画布 viewBox 是 `2000×1200` | ⚠️ **功能 bug**：网格实际没铺满画布 |

### H 的可行性核实：动画到底在动什么属性（10 个主要模板，共 1938 个动画）

| 目标属性 | 个数 | 占比 | 能否用 CSS 动画 | 能否走合成层 |
| --- | --- | --- | --- | --- |
| `opacity` | 777 | 40% | ✅ 可以 | ✅ **可以**（迁 CSS 后收益最大的一块） |
| `points`（多边形点串） | 344 | 18% | ❌ **无法表达** | ❌ |
| `cy` | 341 | 18% | ⚠️ 可以（SVG2 几何属性，Chrome/Edge ✅，Safari 老版本 ❌） | ❌ 仍是主线程 |
| `cx` | 198 | 10% | ⚠️ 同上 | ❌ |
| `r` | 125 | 6% | ⚠️ 同上 | ❌ |
| `y` | 63 | 3% | ❌ 不是 CSS 属性 | ❌ |
| `y1` / `y2` | 54 | 3% | ❌ 不是 CSS 属性 | ❌ |
| `transform:rotate` | 21 | 1% | ✅ 可以 | ✅ 可以（注意 SVG `transform-origin`/`transform-box`） |
| `rx` / `d` | 14 | <1% | ❌ | ❌ |

**结论：只有 41% 的动画是 `transform`/`opacity` 类，可以直接吃合成层红利；59% 动的是几何属性，CSS 要么表达不了（`points`/`y1`/`y2`/`d`，合计约 25%）、要么表达了也只能留在主线程（`cx`/`cy`/`r`，约 34%）。**

所以**"SMIL 换 CSS"本身不是关键，把粒子改成"用 `transform` 移动而不是改几何属性"才是关键** —— 改完之后，无论用 SMIL 还是 CSS，`transform`/`opacity` 才可能被合成器接管。建议顺序：
1. 先把粒子重构成 `<g transform="translate(...)">` + 只动 `opacity`（顺带砍粒子数上限）—— 引擎无关，收益最大；
2. 再决定要不要把动画换成 CSS（此时才真正吃到合成层）。

### 步骤一试点已落地：`tubeCooler` 粒子改 transform 驱动（2026-09-21）

按"先把粒子重构成 transform 驱动 + 顺带砍数量上限"做完第一个模板试点。

| 指标 | 改前 | 改后 | 变化 |
| --- | --- | --- | --- |
| 动画总数 | 614 | **374** | **−39%** |
| 几何属性动画（含 `points`） | 364（points 108） | **114**（points 72） | **−69%** |
| `transform` 动画 | 6 | **93** | ×15 |
| **可合成占比（transform+opacity）** | 41% | **70%** | +29pt |
| 渲染节点数 | 1015 | **773** | −24% |
| 标记体积 | 232 KB | **161 KB** | −31% |

**改法**：
1. **位置动画从几何属性改为 `transform: translate`**：滚带条带（`y`）、焊缝螺栓（`cy`）、扬料板铆点（`y`）、物料/气流/水珠粒子（`cx`+`cy`）——`cx`/`cy` 两个动画合并成一个 translate，值取"相对首个采样点"的偏移。
   - ⚠️ **坑**：`clip-path` 必须留在外层 `<g>`。SVG 里元素自身的 `transform` 会影响它的裁剪空间，若把裁剪放在被平移的元素上，裁剪框会跟着粒子一起移动（视觉上"裁剪失效"）。
2. **数量上限**：齿轮/滚带齿 36→24、小齿轮 14→10、物料粒子 28→18、气流 16→10、水珠 12→8、每环螺栓 4→3、每块扬料板铆点 3→2。视觉几乎无差别（见对照图）。
3. **保留未动**：`opacity`（本身就是可合成属性）、扬料板 `y1`/`y2`（两端点各自沿不同半径运动，属形变，translate 表达不了）、齿形 `points`（齿的底宽随 cos 变化，刚性旋转复现不了）。

**兼容性确认**：`stripSMIL()` 的正则已覆盖 `animateTransform`，所以编辑态剥动画、运行开关定格、预览页暂停这三套机制**都不用改**。

**视觉验证**：`doc/回转滚筒冷却机-粒子动画改transform-对照.png`（改前/改后并排，肉眼一致）；运行时采样确认 translate 在动（t1=none → t2=0,9）、裁剪仍在外层。

**下一步（铺开）**：按同样手法处理 `rotaryKiln`（456 个动画）、`miningDryer`（417）、`desulfTower`（249）。这几个的构成与 `tubeCooler` 类似，可复用同一套改法。

### 新增方向

**G. 增量渲染 / DOM 节点复用（替代 `innerHTML=''` 全量重建）** ★★★★☆
`renderAll()` 每次把三个图层清空重建。改成"保留已有组件组、只更新变化的部分"（每组件一个节点缓存 + diff），可以把全量渲染从 22~50 ms 降到接近增量更新的水平。改动量最大，但和 B 是同一套思路的彻底版。

**H. 粒子渲染从 SMIL 迁到 CSS 动画 / Canvas** ★★★★★（C 的彻底版）
SMIL 的动画引擎比 CSS 动画慢一个量级，更远慢于 canvas 绘制。把 `tubeCooler`/`rotaryKiln`/`miningDryer` 的粒子改成：
- 只动 `transform` / `opacity`（可走合成层，不触发重绘）；
- 或者统一挪到编辑器的粒子层（canvas 绘制，像管道粒子那样）。
这一步做完，动画节点从"上千"降到"几十"。

**I. 同款 `<defs>` 上提到根节点共用** ★★★☆☆
同类型组件的渐变/裁剪内容完全一致，只是 id 不同。把通用渐变定义到 SVG 根的 `<defs>` 一次，组件内直接引用，可省掉 51 → 十几个定义。（与 D 稳定 id 一起做最顺。）

**J. 无绑定 tag 时跳过轮询** ★★☆☆☆
编辑器固定每 5s 拉一次实时值、选中组件时每 3s 刷一次 tag 值。文档里一个 tag 都没绑时可以整个跳过。

**K. 网格改用 CSS 背景** ★★☆☆☆
现在用 SVG `<pattern>` + 一个 100×100 的 rect（且没铺满）。改成画布的 CSS `background-image`（repeating-linear-gradient）既修掉覆盖问题，又把绘制交给合成器，比 SVG pattern 便宜。

**明确不建议优化的（实测证明不是瓶颈）**：
- `pushHistory()` 的 `JSON.stringify` —— 0.06 ms / 12 KB，没必要换增量快照；
- `routePipe` 管道重算 —— 0.08 ms，没必要做缓存；
- 坐标换算 `getScreenCTM` —— 已有缓存（`invalidateCTM`）。

---

## 6. 附：本次调查用到的可复用检查手段

- **计时**：`performance.now()` 必须在**不带** `--virtual-time-budget` 的情况下测，否则虚拟时钟会让它恒为 0；
- **节点与动画计数**：`document.querySelectorAll('#canvas animate, #canvas animateTransform').length`，可直接量化"动画节点爆炸"；
- **溢出/规模体检**：`el.getBoundingClientRect()` 逐元素比对容器边界（本报告与之前的布局修复都用了它）；
- **注意**：`--dump-dom` 在 load 事件时就输出，异步测量（如帧率采样）读不到 —— 需要把测量写成**同步**的。
