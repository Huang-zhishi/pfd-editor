# 脱硫塔（desulfTower）风格对齐 · 分步优化计划

> 目标文件：`templates/desulfTower.js`
> 风格基准：`templates/bucketElevator.js`（斗式提升机）、`templates/reactor.js`（反应釜）
> 参照样板：`templates/dustCollector.js`（除尘布袋，已按同一套步骤改造完成）
> 本文档用于记录每一步的进度，异常中断后从第一个未勾选项继续。

## 0. 风格基准清单（必须照搬）

| 项 | 取值 |
| --- | --- |
| 金属渐变 | `#7d849e → #d9dded → #767d97`（局部 `<defs>`，id 带 uid 后缀，不用外部全局 `gEquip`） |
| 深色内腔 | `#12162b`，描边 `#6a7192` |
| 法兰 / 接管 | `#2a2f45` 填充 + `#3f445c` 描边 |
| 件体（金属件明细） | `#9aa2bc` + `#5b6280` |
| 门板 / 铰链 / 把手 | `#1a1f33` / `#3a4060` / `#8e96b6` |
| 落料口深色收口 | `#0c1020`，描边 `#6a7192` |
| 定尺封顶 | 逐量 `Math.max(下限, Math.min(上限, 比例量))`：组件放大/缩小时只有中间段变长，端板 / 法兰 / 接管 / 螺栓等定尺件不随之变粗 |

## 1. 现状问题清单（读码定位结果）

- [x] P1 依赖外部全局渐变：`fill="url(#gEquip)"` 2 处（L55 塔体 `body`、L64 烟囱直管 `stackBody`）→ 改局部 `<defs>` + uid。（步骤 1 已完成，冒烟 `gEquip=false`）
- [x] P2 配色不符基准：
  - `#1a1835`（L51 锥底、L62 烟囱锥形过渡、L136 烟气入口管、L145 喷淋液入口管、L154 溢流管、L251 人孔、L269 外部循环管内层）
  - `#0d0b20`（L53 底座、L69 烟囱法兰环、L81 烟囱开口、L137/146/155 三处端面法兰、L198 除雾器、L252 人孔中心、L264 铭牌、L270 循环管接头）
  - `#252545`（L57 顶帽、L75 防雨帽、L165 喷淋层横管、L259 环箍）
  - `#060515`（L82 烟囱内腔）
  → 统到 `#2a2f45`/`#3f445c`（法兰 / 端面）、`#12162b`/`#6a7192`（深腔）、`#9aa2bc`/`#5b6280`（件体）、`#0c1020`（收口）、`#1a1f33`/`#3a4060`/`#8e96b6`（门 / 铰链 / 把手）。
- [x] P3 颜色无兜底：`const c = p.color;`（L18）→ `(p && p.color) || '#9C99FF'`。（步骤 1 已完成）
- [ ] P4 定尺缺失（固定像素偏移）：
  - 烟囱法兰环 `-4 / fy-3 / stackW+8 / height 5 / r 1.2`（L69-71）
  - 防雨帽支杆 `±3 / +1 / +2`（L77-79）
  - 顶帽 `cy=topY+2 / rx=r-3`（L58）
  - 三处端面法兰 `width 5 / height +4`、管口圆 `r 2`（L137/139、L146、L155/156）
  - 喷淋层 `sy-2 / height 4`、喷嘴 `tx+8 / (tw-16)/4 / r 2.5 / r 1`（L165-170）
  - 除雾器 `demistY = topY+6`、`tx+3 / tw-6`、栅条 `tx+4+gi*((tw-8)/7)`、`+3.5`（L196-201）
  - 人孔 `rx 5 / ry 7 / r 2 / r 1`（L251-253）
  - 环箍 `tx-1 / tw+2 / height 4`（L259）
  - 铭牌 `width 56 / rx 1 / font-size 6`（L264-265）
  - 外部循环管 `tx-9 / stroke-width 2.5 / 1.5 / width 4 / height 6`（L268-270）
  - 液面 `tx+4 / (tw-8)/20 / 波动幅度 2`（L229-241）
  - 烟气粒子 `r 1.2+t*0.8 / sway 6 / t*4`（L216-217）
  - 排气烟 `r 2.5+t*8 / sway 4~8 / -2 / -1 / 0.7`（L95-121）
  → 引入逐量 clamp。
- [ ] P5 塔内件数量固定：喷淋喷嘴 5 只、除雾器栅条 8 条、环箍 3 道、烟囱法兰环 3 道 → 随塔宽 / 塔高自适应。
- [x] P6 无提升机式细节（把合法兰 / 标准节 / 观察门画法）→ 步骤 4 补齐（塔体把合法兰、人孔改观察门画法）。（已完成）
- [x] P7 烟囱锥形过渡退化（新发现）：`coneTopY = topY - h*0.02` 与 `stackBotY = h*0.437` 恒等，导致 `stackCone` 多边形四点共线、高度为 0，喇叭口实际不可见（且宽窄方向相反）→ 步骤 2.1 修复为「下宽 stackBaseW @topY → 上窄 stackW @stackBotY」。（已完成）

## 2. 必须保持的对外契约（不得改动）

- 模板 key：`TEMPLATES.desulfTower`
- `name: '工业脱硫塔'`、`category: '设备'`、`defaultSize: { w: 120, h: 280 }`
- `ports` 原样：
  - `gasIn (0.084, 0.74, left)`
  - `gasOut (0.5, 0, up)`
  - `slurryOut (0.5, 1, down)`
  - `sprayIn (0.916, 0.52, right)`
  - `overflow (0.23625, 0.86, left)`
  - 端口 x 由 `tx = w*0.21` / `stubLen = tx*0.6 = w*0.126` / `coneT = 0.75` 推导，均与 h 无关，务必保持推导式不变。
- 动画标签形式：`<animate>`（编辑态由 `editor.js stripSMIL()` 剥离）
- 已有动画保留：喷淋液滴（2.5s）、烟气上升粒子（3s）、浆液池液面波纹（3s）、排气蒸汽（3.5s+）

## 3. 分步执行

### 步骤 1：塔体主结构对齐风格 —— 已完成
- [x] 1.1 新增局部 `<defs>`：`metalId`（横向渐变，给竖向的塔体 / 锥底 / 底座 / 烟囱）、`metalVId`（竖向渐变，给三根横向接管），id 带 uid 后缀
- [x] 1.2 `body` / `stackBody`：`fill="url(#gEquip)"` → `url(#${metalId})`，描边 `#3f445c`
- [x] 1.3 锥底 / 底座：`#1a1835` / `#0d0b20` → 后壁 `#2a2f45` + 金属件体渐变，描边 `#3f445c`
- [x] 1.4 塔顶封头：`#252545` / `#1a1835` → `#2a2f45` / `#3f445c` + 内面 `#12162b`
- [x] 1.5 `const c = p.color;` → `const c = (p && p.color) || '#9C99FF';`
- [x] 1.6 校验：`node --check` 退出码 0；多档尺寸渲染无 `gEquip` 残留、渐变 id 唯一、颜色兜底生效；浏览器目视
  结果：`node --check` 退出码 0；冒烟 5 档（120×280 / 120×350 / 60×140 / 240×700 / 400×300）无 NaN、`gEquip=false`、渐变 id 跨实例唯一、颜色兜底与自定义色均生效；浏览器 7 档目视：塔体金属壳 + `#12162b` 深腔生效，与除尘布袋「金属壳 + 内缩深腔」画法一致，无溢出。
- 备注：防雨帽 / 环箍配色并入步骤 2.4 / 4.4；`clipPath` 同步内缩到内腔（`innerL` / `innerW` / `wallT`）。

### 步骤 2：烟囱段与排气特效 —— 已完成
- [x] 2.1 烟囱锥形过渡 + 直管：金属渐变 + `#3f445c` 描边
  → 同时修复 P7：过渡段改为「下宽 `stackBaseW` @`topY` → 上窄 `stackW` @`stackBotY`」，删去恒等的 `coneTopY`。
- [x] 2.2 法兰环改 `#2a2f45` / `#3f445c`，螺栓 `#8e96b6`；道数随烟囱高自适应（步骤 5 细化）
  → 板厚 `stackFlH` / 外伸 `stackFlPad` / 螺栓 `stackBoltR` 已按烟囱宽高定尺封顶。
- [x] 2.3 烟囱顶部开口：`#0c1020` 收口 + `#12162b` 内腔（描边 `#6a7192`）→ 口沿厚度 `stackRimH` 定尺封顶。
- [x] 2.4 防雨帽：`#2a2f45` / `#3f445c` 帽面 + `#12162b` 内面，三根支杆按烟囱直径定位
  → 支杆 `#9aa2bc`，位置 `±stackW*0.38`、线宽 `capStrutW` 随烟囱直径。
- [x] 2.5 排气蒸汽粒子：基础半径 / 摆动幅度按烟囱直径缩放（现为固定 2.5+t*8 / 4~8px）
  → `exR0` / `exRGrow` / `exSway` 三量定尺封顶（默认尺寸下 ≈2.5 / 8 / 4，与原观感一致）。
- [x] 2.6 校验：`node --check`；冒烟无 NaN / 无 `gEquip`；浏览器目视烟囱段与塔体衔接自然
  结果：`node --check` 退出码 0；冒烟 5 档 `bad=false`、`gEquip=false`、`#060515` 已清零（0 处）；
  锥形过渡多边形已非退化（120×280：下边 y=127.96 宽 29.24 → 上边 y=122.36 宽 20.88）；断言 249 个 `<animate>` 不变；
  浏览器 3× 目视：喇叭口可见、法兰环与螺栓、顶部收口、防雨帽支杆均对齐配色。

### 步骤 3：三根接管统一画法（对齐净化气出口接管）—— 已完成
- [x] 3.1 烟气入口（左侧，端口 `gasIn`）：金属渐变管壁 + `#12162b` 管腔（`#6a7192` 描边）+ `#2a2f45`/`#3f445c` 端面法兰 + 2 颗螺栓
- [x] 3.2 喷淋液入口（右侧，端口 `sprayIn`）：同上
- [x] 3.3 溢流口（锥底左侧，端口 `overflow`）：同上；外部循环管改件体 / 深腔画法（`#9aa2bc` 管 + `#1a1f33` 内衬）
- [x] 3.4 校验端口锚点不变（`0.084` / `0.916` / `0.23625`），浏览器目视三管管口位置与编辑态端口圆重合
- 附注：管口端 = 端口锚点，改造时管口最外侧坐标不得变动。
- 实现：抽出统一工厂 `makePipe(x0,x1,cy,ph,side)`——`side='left'` 表示管口在左端 `x0`、`'right'` 表示在右端 `x1`（**必须显式传参**，不可用 `x1>x0` 判断：烟气入口与喷淋液入口都满足 `x1>x0`，隐式判断会把法兰画到塔壁侧）。管壁厚 `pipeWallT` / 法兰板厚 `pipeFlW` / 螺栓半径 `pipeBoltR` 按管高定尺封顶；三根管高 `gasInH` / `sprayPipeH` / `overflowH` 亦改为 clamp。
- 结果：`node --check` 退出码 0；6 档（120×280 / 120×350 / 60×140 / 240×700 / 400×300 / 900×1600）冒烟 `bad=false`、`gEquip=false`、`<animate>` 恒为 249、三根管口锚点比例精确为 `0.08400 / 0.91600 / 0.23625`；结构化断言：端面法兰恰 3 片且分别贴在三管管口端、金属管壁 3 根（宽度 = `stubLen`）位置正确：全部 `PIPES_ALL_OK=true`。
  浏览器（`?only=120x280&k=3`）实测 DOM：管壁 `x/width` 为 `10.08/15.12`（gasIn）、`28.35/15.12`（overflow）、`94.80/15.12`（sprayIn，右端 109.92），与 `ports` 锚点 `10.08 / 28.35 / 109.92` 逐一吻合；6× 裁切目视三管均为「金属管壁 + 深色管腔 + 端面法兰 + 2 颗螺栓」，外部循环管为件体色管壁 + 深色内衬 + `#2a2f45` 弯头。
- 备注：铭牌 `width 56` / `cx-28` 仍为固定像素，窄尺寸（60 宽）下几乎撑满组件——已列在步骤 5.3 替换清单，本步不改。
- 备注（文档更正）：步骤 1.1 中「metalId 竖向 / metalVId 横向」的表述与实现相反，实际为 `metalId` 横向（`x1→x2`，用于竖向塔体 / 锥底 / 烟囱，与 `reactor.js` 一致）、`metalVId` 竖向（`x1=0,y1=0,x2=0,y2=1`，用于三根横向接管）。

### 步骤 4：塔内件对齐风格 + 提升机式细节 —— 已完成
- [x] 4.1 喷淋层横管 + 喷嘴：`#2a2f45` / `#3f445c` 管体 + `#12162b` 喷口 + `${c}` 液滴保留
- [x] 4.2 除雾器：`#9aa2bc` / `#5b6280` 件体栅板 + 斜栅条
- [x] 4.3 人孔改提升机式观察门画法（`#1a1f33` 门板 / `#3a4060` 铰链 / `#8e96b6` 把手）
- [x] 4.4 环箍改件体色（`#9aa2bc` / `#5b6280`）
- [x] 4.5 塔体把合法兰 / 标准节（提升机式），塔身加高时自动增多
- [x] 4.6 铭牌对齐配色（`#2a2f45` / `#3f445c` / 文字 `${c}`）并底边收进 h 范围
- [x] 4.7 校验：`node --check`；浏览器目视塔内件层次清晰、观察门比例协调
- 实现：
  - 4.1 横管 `fill="#252545" stroke="${c}"` → `fill="#2a2f45" stroke="#3f445c"` 并加 `#6a7192` 高光线；喷嘴 `fill="#1a1835" stroke="${c}"` → `fill="#12162b" stroke="#3f445c"`；数字统一 `f()` 两位小数。
  - 4.2 除雾器栅板 `fill="#5b6280" stroke="#9aa2bc"`（原 `#0d0b20` + `${c}`），斜栅条 `stroke="#9aa2bc"`，`demistY = topY+6` / `demistH = h*0.05` 结构保留（像素偏移留步骤 5）。
  - 4.3 人孔 `ellipse #1a1835 + 同心圆` → 提升机式观察门 `door()` 同款画法：`#1a1f33` 门板 + 两条 `#3a4060` 铰链线 + `#8e96b6` 把手圆；门宽 `mhW = clamp(4,7,tw*0.09)`、门高 `mhH = mhW*1.35`，贴塔壁右侧 `mhX = tx+tw+1`。
  - 4.4 环箍 `#252545` + `${c}` → `#9aa2bc` / `#5b6280` 并加中线高光；**位置由固定 `[0.2,0.5,0.8]` 改为 `sprayFracs.map(fr=>fr+0.10)`（即 0.28/0.48/0.68）**——改亮色后原 0.2 档与除雾器（tH 的 0.06~0.21）及第 1 层喷淋（0.18）叠压，挪到喷淋层空档后层次清晰。
  - 4.5 新增 `bodyFlange`：塔顶 `topY` / 塔底 `botY` 两道把合法兰（`#2a2f45` / `#3f445c` + 两侧 `#8e96b6` 螺栓，板厚 `bodyFlH` / 外伸 `bodyFlPad` / 螺栓 `bodyBoltR` 均 clamp，渲染在 `topCap` 之后以免被封头盖住）；新增 `segments`：标准节焊缝只画在塔壁壳上（左右各一小段 `#5b6280` 短刻度，长 `segTick`），道数 `segCount = clamp(1,6,round(tH/max(10,h*0.09)))` 随塔身加高增多——只画在塔壁，不再全宽穿越塔内件。
  - 4.6 铭牌 `#0d0b20` + `${c}` 描边 → `#2a2f45` / `#3f445c` 板面 + `${c}` 钢印字（opacity 0.85）；高度 `namePlateH = max(4, nameBotY-nameY-2)`，底边 `≤ h*0.99-2` 收进 h 范围。
- 结果：`node --check` 退出码 0；冒烟 6 档（120×280 / 120×349.87 / 60×140 / 240×700 / 400×300 / 900×1600）全部 `bad=false`、`<animate>` 恒为 **249**、`gEquip=false`；**残留旧配色已全部清零** `#1a1835`/`#252545`/`#0d0b20`/`#060515` = 0/0/0/0；全部 `<rect>` 边界检查 `xOver=false` / `yOver=false`（无越界）；结构断言观察门 `#1a1f33`×1 + 铰链 `#3a4060`×3（2 铰链 + 1 门框描边）、件体环箍 + 底座脚板 4 处、除雾器栅板 1 处。
  浏览器（`_dt_check.html`）目视：7 档整体比例正常；6×/8× 裁切确认「塔顶把合法兰 → 除雾器斜栅条 → 喷淋层横管 + 5 只深色喷口 + 液滴 → 环箍 → 观察门（门板 / 铰链 / 把手）」层次清晰、互不叠压；塔底把合法兰、液面波纹、锥底、溢流口端面法兰、循环管、铭牌均正常。
- 备注：喷嘴数 5、除雾器栅条 8、环箍 3、烟囱法兰环 3 仍为固定值（P5 未勾选），留步骤 5.2 自适应；`mhW`/`bodyFlH`/`bodyFlPad` 已先行 clamp，步骤 5.1 补齐其余定尺量。

### 步骤 5：定尺封顶与自适应
- [ ] 5.1 逐量 clamp：管壁厚、法兰板厚、螺栓半径、人孔尺寸、铭牌尺寸、栅条 / 支杆偏移、防雨帽支杆
- [ ] 5.2 自适应数量：喷淋喷嘴数、除雾器栅条数、环箍道数、烟囱法兰环数随塔宽 / 塔高变化
- [ ] 5.3 替换所有固定像素偏移（P4 清单：`-9`/`+4`/`r 1.2`/`width 56`/`demistY=topY+6` 等）
- [ ] 5.4 校验：极端尺寸（60×140 / 120×280 / 120×350 / 240×700 / 400×300）目视比例不失真、无溢出

### 步骤 6：整体校验
- [ ] 6.1 `node --check templates/desulfTower.js` 退出码 0；`GetDiagnostics` 无告警
- [ ] 6.2 浏览器目视：默认 120×280、demo 实机尺寸 120×349.87
- [ ] 6.3 编辑态 `stripSMIL` 生效（`<animate>` 计数为 0）
- [ ] 6.4 运行态动画恢复（喷淋液滴 / 烟气粒子 / 液面波纹 / 排气蒸汽）
- [ ] 6.5 镜像 `mirrored`、旋转 `rotation`、`namePos` 正常
- [ ] 6.6 临时核对页删除，清理产物

## 4. 进度记录

| 步骤 | 状态 | 备注 |
| --- | --- | --- |
| 步骤 1 | 已完成 | 塔体主结构对齐风格（局部 defs / 深腔 / 法兰配色 / 颜色兜底）。残留旧配色 `#1a1835`/`#252545`/`#0d0b20`/`#060515` 与固定像素偏移留待步骤 2/3/4/5 |
| 步骤 2 | 已完成 | 烟囱段与排气特效（含 P7 锥形过渡退化修复）。`#060515` 已清零 |
| 步骤 3 | 已完成 | 三根接管统一 `makePipe` 画法 + 外部循环管改件体色；端口锚点 `0.084`/`0.916`/`0.23625` 实测不变 |
| 步骤 4 | 已完成 | 塔内件对齐风格 + 提升机式细节（喷淋层 / 除雾器 / 观察门 / 环箍 / 把合法兰 + 标准节 / 铭牌）；旧配色全部清零，`<animate>` 249 不变 |
| 步骤 5 | 进行中 | 定尺封顶与自适应 |
| 步骤 6 | 未开始 | 整体校验 |
