# 配料秤（weighFeeder）风格对齐 · 分步优化计划

> 目标文件：`templates/weighFeeder.js`
> 风格基准：`templates/bucketElevator.js`（斗式提升机）、`templates/reactor.js`（反应釜）
> 参照样板：`templates/dustCollector.js`（除尘布袋，已按同一套步骤改造完成）
> 本文档用于记录每一步的进度，异常中断后从第一个未勾选项继续。

## 0. 风格基准清单（必须照搬）

| 项 | 取值 |
| --- | --- |
| 金属渐变 | `#7d849e → #d9dded → #767d97`（局部 `<defs>`，id 带 uid 后缀，不用外部全局 `gEquip`） |
| 深色内腔 | `#12162b`，描边 `#6a7192` |
| 法兰 / 接管 | `#2a2f45` 填充 + `#3f445c` 描边 |
| 件体（金属件明细） | `#9aa2bc` + `#5b6280` |
| 门板 / 铰链 / 把手 | `#1a1f33` / `#3a4060` / `#8e96b6` |
| 定尺封顶 | `H(hr, wr) = Math.min(h*hr, w*wr)`；或逐量 `Math.max(下限, Math.min(上限, 比例量))` |
| 动画 | SMIL `<animate>` / `<animateTransform>`（编辑态由 `editor.js stripSMIL()` 剥离） |

## 1. 现状问题清单（读码定位结果）

- [ ] P1 颜色无兜底：`const c = p.color;`（L12）→ 改 `(p && p.color) || '#9C99FF'`。
- [ ] P2 配色不符基准：全套硬编码 `#151330` / `#1a1835` / `#0a1a0a`，未使用金属渐变与基准色。
- [ ] P3 无局部 `<defs>`：无 `metalId` 渐变、无 `clipPath`。
- [ ] P4 固定像素偏移：`inletH = 9`、`topY = inletH + 3`、`-6` / `+6` / `-2` / `-3`、`outletH = h - coneBotY - 6`、`legW = 4`、`senH = 7`、`boxW/boxH = 15`、`font-size="4.5"` 均不随尺寸缩放。
- [ ] P5 无定尺封顶：法兰 / 传感器 / 仪表 / 支腿随尺寸等比失真。
- [ ] P6 无提升机式细节：无把合法兰条 / 螺栓 / 观察门 / 支腿 / 基础。
- [ ] P7 无 SMIL 动画：无进料粒子、无料位指示、指示灯不闪烁。
- [ ] P8 无 `clipPath`：元素可能溢出料斗边界（尤其小尺寸）。
- [ ] P9 小尺寸失真：`outletH = h - coneBotY - 6` 在 `h` 偏小时可能为负；实例实测尺寸 **约 55×54**（见 `demo-template.json`），远小于 `defaultSize` 110×160，必须保证极小尺寸可辨识、不溢出。

## 2. 必须保持的对外契约（不得改动）

- 模板 key：`TEMPLATES.weighFeeder`
- `name: '配料秤/称重料斗'`、`category: '设备'`、`defaultSize: { w: 110, h: 160 }`
- `ports`：`inlet (0.5, 0, up)` 顶部进料 / `outlet (0.5, 1, down)` 底部出料
- 接管绘制中心线须与 `ports.*.y*h` 严格同轴
- 动画标签形式：`<animate>` / `<animateTransform>`（编辑态由 `editor.js stripSMIL()` 剥离）

## 3. 分步执行

### 步骤 1：料斗主体对齐风格
- [x] 1.1 新增局部 `<defs>`：`metalId`（竖向渐变，给筒体/锥体/出料管）、`metalVId`（横向渐变，给接管），id 带 uid 后缀
- [x] 1.2 料斗筒体 rect / 锥体 polygon 改 `url(#${metalId})` + `#3f445c` 描边
- [x] 1.3 进料口 / 出料管改接管三件套画法（金属管壁 + `#12162b` 管腔 + `#2a2f45` 端面法兰）
- [x] 1.4 颜色兜底：`const c = (p && p.color) || '#9C99FF';`
- [x] 1.5 校验：`node --check` 退出码 0；4 档尺寸渲染无 `gEquip` 残留、渐变 id 唯一、颜色兜底生效；浏览器目视

### 步骤 2：提升机式细节
- [x] 2.1 顶 / 底法兰条按基准色重画（`#2a2f45` + `#3f445c`），螺栓改 `#8e96b6`（`bolt()`）
- [x] 2.2 筒身把合法兰：料斗加高时自动增多（`nSec` 循环）
- [x] 2.3 筒体观察门（`door`：`#1a1f33` 门板 + `#3a4060` 铰链×2 + `#8e96b6` 把手）
- [x] 2.4 校验：`node --check` 退出码 0；冒烟无 NaN / 无 `gEquip`；`ports` 原样未变；浏览器目视

### 步骤 3：称重系统与支腿
- [x] 3.1 称重传感器（左右压式）改基准配色（件体 `#9aa2bc` + `#5b6280`），保留指示圆点
- [x] 3.2 支腿 / 基础板改件体配色 + 螺栓，按料斗尺寸封顶
- [x] 3.3 称重仪表：面板 `#2a2f45` + 屏幕 `#12162b` / `#6a7192` + `kg` 文字用 `${c}` + 绿色指示灯
- [x] 3.4 信号线（虚线）改 `#6a7192`，端点贴仪表与传感器
- [x] 3.5 校验：`node --check` 退出码 0；冒烟通过；浏览器目视

### 步骤 4：定尺封顶与自适应
- [x] 4.1 逐量 clamp：进料口高 / 出料管长 / 传感器尺寸 / 仪表尺寸 / 法兰厚 / 支腿宽 / 字号全部加下限与上限
- [x] 4.2 消除固定像素偏移：`+3` / `±6` / `-2` 等改为按尺寸比例或封顶量
- [x] 4.3 修掉小尺寸负高：`outletH` 加 `Math.max` 下限，逼近 0 时省略出料管
- [x] 4.4 校验：`node --check` 退出码 0；多档冒烟（含 **55×54** 实机尺寸、`30×30` 极小、`220×320` 放大）无 NaN / 无负高 / 无溢出；浏览器目视

### 步骤 5：动画与防溢出
- [x] 5.1 `clipPath`：筒体 + 锥体边界，防止内部图元/粒子溢出
- [x] 5.2 进料粒子动画：自进料口落入料斗（`<animate>` `translate` + `opacity`）
- [x] 5.3 称重仪表指示灯闪烁（`<animate>` `opacity`），信号线保持静态
- [x] 5.4 校验：`node --check` 退出码 0；编辑态 `stripSMIL` 后 `<animate` 计数为 0；运行态动画恢复；浏览器目视

### 步骤 6：整体校验
- [x] 6.1 `node --check templates/weighFeeder.js` 退出码 0；`GetDiagnostics` 无告警
- [x] 6.2 浏览器目视：默认 110×160、demo 实机 55×54、自定义色兜底
- [x] 6.3 编辑态 `stripSMIL` 生效（`animate` 计数为 0）
- [x] 6.4 运行态动画恢复
- [x] 6.5 镜像 `mirrored`、旋转 `rotation`、`namePos` 正常
- [x] 6.6 清理临时核对页与产物

## 4. 进度记录

| 步骤 | 状态 | 备注 |
| --- | --- | --- |
| 步骤 1 | 已完成 | 局部 defs 双渐变（`metalId` 竖向 / `metalVId` 横向）+ 后壁/金属/内腔三层 + 接管三件套 + 颜色兜底 |
| 步骤 2 | 已完成 | 顶/底法兰条 + `bolt()` 双圆螺栓 + 筒身把合法兰 `nSec` 自动增多 + 检修门 |
| 步骤 3 | 已完成 | 称重支腿组（耳座/传感器指示圆点/支腿/基础板）×2 + 称重仪表（面板/屏幕/kg/绿灯/虚线信号线） |
| 步骤 4 | 已完成 | 各量逐级 clamp；筒体/锥体比例 0.26→0.30；`outletH` 消负高；`outletH ≤ 0.6` 时省略出料管 |
| 步骤 5 | 已完成 | `clipPath` 防溢出 + 3 颗料流粒子（6 个 `<animate>`）+ 指示灯闪烁（1 个），共 7 个 |
| 步骤 6 | 已完成 | 详见下方「校验记录」 |

## 5. 校验记录

- **语法 / 静态**：`node --check templates/weighFeeder.js` → 退出码 `0`；`GetDiagnostics` → 无告警。
- **Node 冒烟（9 档尺寸）**：110×160 / 55×54 / 30×30 / 220×320 / 110×60 / 300×200 / 自定义色 / 无 `p` / 60×900
  → 全部 `NaN=false`、`neg=0`、`grads=2`、`clip=1/1`、`anim=7`、`stripSMIL` 后 `anim=0`、`orphan=false`；`uniq_uids=9/9`（多实例 id 不冲突）。
  `ports` / `name` / `defaultSize` 与改造前一致。
- **集成校验（复刻 `editor.js` 包裹层）**：
  | 场景 | `animate` 计数 | `NaN` |
  | --- | --- | --- |
  | 正常 | 7 | 无 |
  | 镜像 `mirrored`（`translate(w,0) scale(-1,1)`） | 7 | 无 |
  | 旋转 `rotation=90°` | 7 | 无 |
  | `namePos='center'` | 7 | 无 |
  | 编辑态（`stripSMIL`） | 0 | 无 |
  | 镜像 + 旋转 | 7 | 无 |
- **浏览器目视**：默认 110×160 / demo 55×54 / 极小 30×30 / 放大 220×320 / 扁 110×60 / 宽 300×200 / 自定义色 全部正常 —— 锥体梯形正确、料流粒子在腔内下落、元素不溢出、镜像后仪表与检修门正确翻到左侧。
- **已知设计取舍**：`namePos='center'` 时名称压在设备中心（与其它组件一致的全局行为，非本组件问题）。
- **陷阱记录**：`server.js` 的 `sendFile` 不发 `Cache-Control`，浏览器会复用缓存的 `templates.js` / 模板文件，核对页必须带 `?t=<Date.now()>` 强制刷新，否则会看到旧渲染。
