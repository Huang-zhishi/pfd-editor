# 冷却水池（coolingPond）风格对齐 · 分步优化计划

> 目标文件：`templates/coolingPond.js`
> 风格基准：`templates/bucketElevator.js`（斗式提升机）、`templates/reactor.js`（反应釜）
> 参照样板：`templates/valve.js`（气密阀）、`templates/pump.js`（工业抽水泵，上一轮已按同一套步骤改造完成）、`templates/desulfTower.js`（脱硫塔）、`templates/dustCollector.js`（除尘布袋）
> 特殊指令：**保留铭牌并对齐配色**（区别于气密阀「铭牌直接不要了」；本模板当前已有铭牌板 + `<text>冷却水池</text>`，保留文字，仅把板 / 描边统到基准色系，见步骤 6）
> 本文档用于记录每一步的进度，异常中断后从第一个未勾选项继续。
> 进度：**3 / 6 步完成**。

## 0. 风格基准清单（必须照搬）

| 项 | 取值 |
| --- | --- |
| 金属渐变 | `#7d849e → #d9dded → #767d97`（局部 `<defs>`，id 带 uid 后缀，不用外部全局 `gEquip`） |
| 深色内腔 / 基础 | `#12162b`，描边 `#6a7192` |
| 法兰 / 接管 | `#2a2f45` 填充 + `#3f445c` 描边 |
| 件体（金属件明细 / 亮线） | `#9aa2bc` + `#5b6280` |
| 门板 / 铰链 / 把手（螺栓头 / 指针件体色） | `#1a1f33` / `#3a4060` / `#8e96b6` |
| 深色收口 | `#0c1020`，描边 `#6a7192` |
| 渐变方向 | 竖向池体 / 立式箱体用**横向渐变**（`x1=0,y1=0,x2=1,y2=0`）；横向接管 / 管道用**竖向渐变**（`x1=0,y1=0,x2=0,y2=1`） |
| 定尺封顶 | 逐量 `Math.max(下限, Math.min(上限, 比例量))`；或 `H(hr, wr) = Math.min(h*hr, w*wr)`，放大 / 缩小时只有中间段变长，端面 / 法兰 / 接管 / 螺栓 / 铭牌等定尺件不随之变粗 |

## 1. 现状问题清单（读码定位结果）

- [x] P1 无金属渐变：全文件仅一处渐变 `water_`（L51-54），**且直接内联在返回体中、未放进 `<defs>`**；池壁外壁 / 压顶 / 内腔 / 进 / 出水管 / 法兰全部纯平涂色，壳体无明暗。不依赖全局 `gEquip`。→ 步骤 1 新增局部 `<defs>` + uid 后缀，并把水面渐变 `water_` 与 `waveclip_` 一并收进 `<defs>`。**✅ 步骤 1 已完成**
- [x] P2 配色不符基准（仅铭牌板 `#0d0b20` 留步骤 6）：
  - `#12102a`（L38 混凝土地面）
  - `#1a1835`（L42 池壁外壁、L82 进水管顶法兰、L84 进水管颈圈、L90 出水管法兰）
  - `#0e1c33`（L43 内腔水体外框）
  - `#151330`（L47 压顶、L81 进水管、L85 进水阀体、L89 出水管、L92 出水管颈圈、L95 / L96 溢流口）
  - `#0d0b20`（L19 螺栓底、L105 铭牌板）
  - `#6FC3FF`（L30 落水滴，青蓝高饱和，未随 `${c}` 主题色）
  - `rgba(255,255,255,0.04)`（L44）、`rgba(0,0,0,0.25)`（L45）、`rgba(255,255,255,0.06)`（L48）、`rgba(255,255,255,0.5)`（L69）
  → 统到 `#12162b`/`#6a7192`（深腔 / 基础）、`#2a2f45`/`#3f445c`（法兰 / 接管 / 阀体 / 压顶）、`#9aa2bc`/`#5b6280`（件体 / 亮线）、`#0c1020`（收口）、`#8e96b6`（螺栓头）、`#d9dded`（水波高光）、`${c}`（粒子与运动件）。
- [x] P3 颜色无兜底：`const c = p.color;`（L16）→ `(p && p.color) || '#9C99FF'`（`p` 缺省时会抛错，其余设备均已兜底）。**✅ 步骤 1 已完成**
- [ ] P4 定尺缺失（固定像素偏移）：池壁 `wall=6`、`rimY=16`、`rimBottom=h-10`（L21）；内壁内缩 `innerX=wall+4`（L22）；水体 `waterY=Math.round(h*0.46)`、`waterBottom=rimBottom-wall-2`（L23）；螺栓 `r=1`（L19 默认参、L83 / L91 调用）；落水滴半径 `1.2+(i%2)*0.5`（L30）；地面高 `6`（L38）；池壁描边 `2`、内腔上探 `6`（L42-43）；压顶 `-3 / +6 / -4 / height 6 / -1,+2 / 1.5`（L47-48）；波纹振幅 `3 / 5`、线宽 `1.2 / 0.8`（L68-69）；刻度 `+2 / +4 / +8`（L74-77）；进水管 `±3 / 6 / ±5 / 10 / 4 / -4 / 8 / 3 / +4 / 4 / 11`（L80-85）；出水管 `±3 / 6 / ±5 / 10 / 4 / -4 / 8 / 3`（L89-92）；溢流口 `10 宽 / 8 高 / -2 / 3 / 12`（L95-96）；爬梯 `x=3 / 9`、`-4`、`(rimBottom-rimY-12)/5` 间距（L100-102）；铭牌 `w-30 / 24 / 10 / w-18 / rimY+15`（L105-106）。→ 引入逐量 clamp 与比例量。
- [ ] P5 铭牌 + `<text>`（L104-106）：**用户决定保留**，但板色 `#0d0b20` / 描边 / 文字需统到基准色系（板 `#2a2f45` + `#3f445c` 描边，文字 `${c}`）→ 步骤 6。
- [ ] P6 细节缺失（提升机式画法）：进水管无把合法兰环 + 端面螺栓规范化、无阀门手轮；出水管无把合法兰环；池壁液位标尺仅 3 条短线、缺少主刻度 / 数字基线感 → 步骤 3 补齐接管统一画法（管壁 + 管腔 + 端面法兰 + 螺栓）、阀门手轮、液位标尺。
- [ ] P7 溢流口与端口锚点错位（新发现）：端口 `overflow(1, 0.4, right)` 锚点纵坐标 = `0.4h`，而溢流口图元绘制在 `y=waterY=h*0.46`（L95-96），中心为 `waterY+4`。默认 160×120 实算锚点 `y=48`、图元中心 `y≈59`，**偏差约 11px**，连线 / 视觉错位；且溢流口应高于常水位（`0.4h < 0.46h` 恰好在其上方）才合理 → 步骤 2 以端口锚点 `h*0.4` 重定位溢流口并令其中心对齐。**✅ 步骤 2 已完成**

## 2. 必须保持的对外契约（不得改动）

- 模板 key：`TEMPLATES.coolingPond`
- `name: '冷却水池'`、`category: '设备'`、`defaultSize: { w: 160, h: 120 }`
- `ports` 原样：
  - `inlet (0.5, 0, up)`
  - `outlet (0.5, 1, down)`
  - `overflow (1, 0.4, right)`
- 端口对齐约束：
  - `inlet` / `outlet` 的 `x=0.5` 必须与进 / 出水管中心 `x=w*0.5` 严格同轴，改造时不得偏移；`inlet` 管口顶边固定 `y=0`，`outlet` 管口底边固定 `y=h`
  - `overflow` 管口最外侧固定 `x=w`，其**垂直中心必须对齐锚点 `y=0.4h`**（修 P7）
- 动画标签形式：`<animate>` / `<animateTransform>`（编辑态由 `editor.js stripSMIL()` 剥离）
- 已有动画保留、不得增删：落水滴 6 颗，每颗 `animate`(cx) + `animate`(cy) + `animate`(opacity) = `6×3 = 18`；水面波纹 3 条，每条 `animateTransform`(translate) = `3` → 全模板 `<animate>` + `<animateTransform>` 总计数恒为 **21**
- 文字 / 铭牌：保留 `<text>冷却水池</text>` 与实际 `nameplate` 板（用户决定），文字内容与文案不得变动
- 画布越界约束：所有图元（含铭牌、溢流口、爬梯、落水滴轨迹）渲染后须落在 `x∈[0,w]`、`y∈[0,h]` 内

> 契约实测（编辑器中读取全局 `TEMPLATES.coolingPond`）：`name=冷却水池`、`category=设备`、`defaultSize={w:160,h:120}`、`ports=[inlet(0.5,0,up), outlet(0.5,1,down), overflow(1,0.4,right)]`、`render` 为函数，均未改动。

## 3. 分步执行

### 步骤 1：池体主结构与接管配色对齐风格
- [x] 1.1 新增局部 `<defs>`：`metalId`（横向渐变 `x1=0,y1=0,x2=1,y2=0`，给竖向池壁外壁 + 竖向进 / 出水管管壁）、`metalVId`（竖向渐变 `x1=0,y1=0,x2=0,y2=1`，给横向溢流口短管），id 带 uid 后缀（`pond_metal_` / `pond_metalv_`）；同时把现有 `water_` 渐变与 `waveclip_` 一并移入 `<defs>`
- [x] 1.2 池壁外壁：`fill="#1a1835"` → `url(#${metalId})`，描边 `${c}` → `#3f445c`
- [x] 1.3 内腔水体外框：`#0e1c33` → `#12162b`，描边 `${c}` → `#6a7192`
- [x] 1.4 压顶：`#151330` → `#2a2f45` + `#3f445c` 描边；高光条 `rgba(255,255,255,0.06)` → `#5b6280`
- [x] 1.5 混凝土地面：`#12102a` → `#12162b`（深色基础），描边 `${c}` → `#6a7192`；内部分隔线改 `#6a7192`
- [x] 1.6 进 / 出水管管体：管壁 `#151330` → `url(#${metalId})`（竖管用**横向**渐变），描边 `${c}` → `#6a7192`
- [x] 1.7 进 / 出水管法兰：`#1a1835` / `#151330` → `#2a2f45` + `#3f445c` 描边
- [x] 1.8 进水阀体：`#151330` → `#2a2f45` + `#3f445c` 描边
- [x] 1.9 螺栓：底座 `#0d0b20` → `#2a2f45`，螺栓头 `${c}` → `#8e96b6`
- [x] 1.10 溢流口：短管 `#151330` → `url(#${metalVId})`（横管用**竖向**渐变）+ `#3f445c` 描边；左侧封头竖板 → `#2a2f45` + `#3f445c`
- [x] 1.11 落水滴：`#6FC3FF` → `${c}`，与其余设备液体 / 粒子用主题色的约定统一
- [x] 1.12 其余半透明高光：`rgba(255,255,255,0.04)` → `#5b6280`；`rgba(0,0,0,0.25)` → `#0c1020`；波纹高光 `rgba(255,255,255,0.5)` → `#d9dded`
- [x] 1.13 `const c = p.color;` → `const c = (p && p.color) || '#9C99FF';`
- [x] 1.14 校验：`node --check templates/coolingPond.js` 退出码 0；多档尺寸渲染无 `gEquip` 残留、渐变 id 跨实例唯一、颜色兜底与自定义色均生效；浏览器目视金属池壁 + 深腔生效
- 备注：固定像素偏移（P4）留步骤 4，本步只改配色 / 渐变 / 兜底与粒子色
- ✅ 实测：`node --check` 退出码 0；6 档尺寸（160×120 / 320×240 / 80×60 / 40×30 / 160×60 / 300×200）冒烟 `smil=21`、`defs=true`、`metal=true`、无 `NaN`、无 `gEquip`；非基准色残留仅铭牌板 `#0d0b20`（=1，属步骤 6 范围），其余为 0；无 `p` 兜底与自定义色 `#ff9a5c` 均生效；浏览器目视（静态剥离 SMIL，2 色 × 3 尺寸）金属池壁横向明暗、深色内腔、法兰 / 螺栓、水体渐变 + 波纹、铭牌 / 爬梯均正常，无裁切 / 错压

### 步骤 2：溢流口与端口锚点对齐（修 P7）
- [x] 2.1 定义 `const overflowY = h*0.4;`（与端口 `overflow(1,0.4,right)` 锚点一致，加注释说明须与 ports 保持同步）
- [x] 2.2 溢流口重定位：横向短管垂直中心对齐 `overflowY`，即 `y = overflowY - h*0.4 处高/2`；左侧封头竖板同步平移，保证整组中心 = `0.4h`
- [x] 2.3 管口最外侧仍收在 `x=w`（不伸出设备边界），管身高 / 宽按池宽 / 池高 clamp
- [x] 2.4 校验：断言 `overflow` 图元垂直中心 = `0.4h`（默认 160×120 → 48）、端口锚点比例不变（`x=1` / `y=0.4`）；浏览器目视溢流口位于常水位之上、与右侧连线对齐
- 实现：`overflowW = clamp(w/16, 5, 14)`、`overflowH = clamp(h/15, 4, 12)`、封头竖板 `overflowPlateW = clamp(overflowW*0.3, 2, 4)` / `overflowPlateH = overflowH + clamp(overflowH*0.5, 2, 6)`；短管 `x = w-overflowW`、`y = overflowY-overflowH/2`，竖板同轴居中
- ✅ 实测：`node --check` 退出码 0；6 档尺寸（160×120 / 320×240 / 80×60 / 40×30 / 160×60 / 300×200）× 2 色冒烟断言全通过 —— 溢流口垂直中心恒等于 `0.4h`（默认 160×120 → 48，原为 59，修正 11px）、最外侧恒为 `x=w`、整组（含封头竖板）在界内、位于常水位 `0.46h` 之上、`smil=21`、无 `NaN`、无 `gEquip`；`ports` 实测仍为 `overflow(1,0.4,right)` 比例不变；浏览器目视（`0.4h` 参考虚线，5 组尺寸 / 配色）溢流口中心与虚线重合、收于右边界，无裁切 / 错位

### 步骤 3：细节补齐（提升机式画法）
- [x] 3.1 进水管统一画法：抽出 `makePipe` 式函数，输出「金属管壁（`metalId`，竖管用横向渐变）+ `#12162b`/`#6a7192` 管腔 + 端面把合法兰（`#2a2f45`/`#3f445c`）+ 2 颗 `#8e96b6` 螺栓」，管口顶边对齐 `y=0`、中心对齐 `x=w*0.5`
- [x] 3.2 进水管阀门手轮：阀体上补手轮（轮毂 + 辐条 + 轮缘，件体色 `#9aa2bc`/`#5b6280`），辐条数 4~5，尺寸按池宽 clamp，矮尺寸下自动缩小 / 省略
- [x] 3.3 出水管统一画法：同 3.1，管口底边对齐 `y=h`、中心对齐 `x=w*0.5`，底部端面把合法兰 + 螺栓
- [x] 3.4 液位标尺：在内腔左壁补齐主 / 次刻度（起点在水面 `waterY`，等分 `waterH`），线长 / 线宽按内腔宽 clamp（替换原固定 `+2 / +4 / +8`）
- [x] 3.5 爬梯：双竖杆 + 横档，横档数按池高 clamp，间距由 `(rimBottom-rimY-2*留边)/(档数-1)` 计算，替换固定 `x=3 / 9` 与 `/5`
- [x] 3.6 校验：端口锚点比例实测不变（`inlet=0.5/0`、`outlet=0.5/1`、`overflow=1/0.4`）；浏览器目视法兰 / 螺栓 / 手轮 / 标尺 / 爬梯与池体不错压
- 实现：抽出竖管统一画法 `makeVPipe(x, y0, y1, end)`（金属管壁 `url(#metalId)` + 深色管腔 `#12162b`/`#6a7192` st0.5 + 端面法兰 `#2a2f45`/`#3f445c` + 2 螺栓 `#8e96b6`）与接口把合法兰 `makeJointV(x, y)`；管身 `pipeW = clamp(w*0.0375, 3, 8)`（默认 160 → 6）、`wallT = clamp(pipeW*0.18, 0.6, 1.4)`、端面法兰 `flW = clamp(pipeW*1.7, 6, 14)` / `flH = clamp(pipeW*0.7, 2, 5)`、螺栓 `boltR = clamp(flW*0.12, 0.6, 1.4)`；进水管 `makeVPipe(inletX, 0, rimY, 'top')`、出水管 `makeVPipe(outletX, rimBottom, h, 'bottom')`。手轮：`vbW/vbH` 按管宽 / 池顶 clamp，`wheelR = clamp((vbY-hPad)*0.42, 0, min(w*0.14, vbH*0.55))`，`wheelR >= 1.4` 才绘制（矮尺寸自动省略），辐条 `wheelR >= 2.2 ? 5 : 4`（默认 5），轮缘 `#9aa2bc` st1.4 / 轮毂 `#5b6280` / 把手 `#8e96b6`。标尺：`tickLenMajor = clamp(innerW*0.16, 3, 8)`、次刻度 `0.55×`，主刻度 5 条（含首尾，`opacity 0.7`）+ 次刻度 4 条（等分中点，`opacity 0.4`）= 9 条。爬梯：双竖杆 `railX1 = clamp(w*0.02, 2, 4)` / `railGap = clamp(w*0.04, 3, 6)`，`rungN = clamp(round((rimBottom-rimY)/14), 3, 8)`，间距 `(rungBot-rungTop)/(rungN-1)`
- ✅ 实测：`node --check` 退出码 0；6 档尺寸（160×120 / 320×240 / 80×60 / 40×30 / 160×60 / 300×200）× 2 色断言全通过 —— `smil` 恒为 21、无 `NaN`、无 `undefined`、无 `gEquip`、局部渐变 / clipPath 命中、铭牌文字保留、管腔 `#12162b`/`#6a7192` 命中、端面法兰 + 螺栓（`#8e96b6 opacity 0.5`）≥ 8 颗、进 / 出水管口锚点顶边 `y=0` / 底边 `y=h`、溢流口中心恒为 `0.4h`；默认 160×120 细节：手轮存在（`wheelR 2.6`、5 辐条）、手轮左缘 > 顶部法兰右缘、手轮不越画布上沿、阀体不越池顶、标尺 9 条、爬梯线数 = `rungN+2`；极小 24×20 无 `NaN`/`undefined`、`smil=21`。浏览器实测（独立预览页）：4 档尺寸全部渲染且包围盒恰好 = 画布（无越界），默认档 —— 进水管中心 `x=80=0.5w` / 管口顶边 `y=0`、出水管口底边 `y=120=h`、手轮顶 `y=1≥0`、手轮左缘与顶部法兰右缘间隙 `+0.70`（不错压）、阀体顶 `y=3.6≥0`、标尺 9 条（最右 `x=19` 收在内腔 `10~150` 内）

### 步骤 4：定尺封顶与自适应
- [ ] 4.1 逐量 clamp：池壁厚 `wall`、压顶高 `rimH` / 上探 `rimY`、内壁内缩量、地面高 `groundH`、管腔高 / 管壁厚、法兰宽 / 高、螺栓半径、手轮半径 / 辐条长、波纹振幅 / 线宽、刻度线长、爬梯杆位 / 横档线宽、铭牌宽 / 高 / 字号——全部按池宽 / 池高封顶
- [ ] 4.2 替换所有固定像素偏移（P4 清单：`6 / 16 / +4 / 0.46 / -2 / 1.2 / 1.5 / 2 / 3 / 4 / 5 / 8 / 10 / 11 / 12 / 24 / 30` 等均已消除）
- [ ] 4.3 水体 / 水面按高度比例：`waterY = clamp(h*0.46, ...)`、`waterH` 保证 `≥ 下限`，矮尺寸下水体不消失、波纹振幅 / 线宽按 `waterH` 比例并设下限（避免退化为直线或糊成一片）
- [ ] 4.4 落水滴轨迹按 `waterY` 比例：`yStart = rimY-2`、`yEnd = waterY` 均随几何量走；粒半径按池宽 clamp
- [ ] 4.5 校验：极端尺寸（160×120 / 320×240 / 80×60 / 40×30 / 160×60 / 300×200）冒烟均无 NaN、无 `gEquip`、无越界，比例不失真

### 步骤 5：整体校验
- [ ] 5.1 `node --check templates/coolingPond.js` 退出码 0；仓库无 lint / typecheck 配置、无 `package.json`，以 `node --check` + 多尺寸冒烟为准
- [ ] 5.2 浏览器目视：默认 160×120、自定义色（如 `#ff9a5c`）、极小 40×30、扁 160×60 逐一核对 —— 金属池壁明暗、深色内腔、水体渐变 + 波纹、进 / 出水管法兰 + 螺栓、阀门手轮、液位标尺、溢流口对位、爬梯、铭牌均正常，无裁切、无错压
- [ ] 5.3 编辑态 / 运行态：`stripSMIL()` 命中（动画标签为自闭合 `<animate …/>`），编辑态 SMIL 计数为 0，运行态动画恢复（落水滴 1.3~1.6s、波纹 3~5s）；`<animate>` + `<animateTransform>` 总计数 **恒为 21**（默认色与自定义色实测均为 21）
- [ ] 5.4 镜像 `mirrored` / 旋转 `rotation` / `namePos` 正常：模板自身仅在波纹 `<path>` 内使用 `animateTransform`，不向外发出 `transform`，编辑器在外层 `.equip-group` 施加镜像（`translate(w,0) scale(-1,1)`）与旋转不受影响；铭牌为模板内 `<text>`，镜像时随图形翻转（如需正向文字由编辑器 `namePos` 标签承担，本次不改）
- [ ] 5.5 临时核对页与本地静态服务已删除 / 停止，无临时产物残留

### 步骤 6：铭牌保留 + 配色对齐（用户决定）
> 本模板当前已有铭牌板 + `<text>冷却水池</text>`（L104-106），且**与气密阀「铭牌直接不要了」相反**：本次**保留**，仅统色与定尺。

- [ ] 6.1 保留 `nameplate` 板 + `<text>冷却水池</text>`，文字内容 / 文案不变
- [ ] 6.2 板色 `#0d0b20` → `#2a2f45`（法兰 / 接管件色），描边 `${c}` → `#3f445c`
- [ ] 6.3 文字填充 `${c}`（主题色）保留，`font-weight` / `opacity` 维持，字号改为按池高 clamp（替换固定 `4.5`）
- [ ] 6.4 铭牌位置 / 尺寸定尺：`x / y / width / height` 由池宽 / 池高 clamp（替换固定 `w-30 / 24 / 10 / w-18 / rimY+15`），保证不越出池壁右边界
- [ ] 6.5 校验：`grep` 确认 `templates/coolingPond.js` 内 `nameplate` 与 `<text>冷却水池</text>` 仍在、`node --check` 退出码 0；冒烟 `hasText=true`、动画数恒为 21

## 4. 进度记录

| 步骤 | 状态 | 备注 |
| --- | --- | --- |
| 步骤 1 | ✅ 完成 | 局部 `<defs>`（`pond_metal_` / `pond_metalv_`）+ 深腔 / 法兰配色 + 颜色兜底 + 落水滴改 `${c}`；`node --check` 退出码 0、6 档冒烟 `smil=21` / 无 NaN、浏览器目视通过（详见步骤 1 实测行） |
| 步骤 2 | ✅ 完成 | 定义 `overflowY=h*0.4`（与端口锚点同步）并重定位溢流口整组，修正原约 11px 错位；管身定尺 clamp、最外侧仍收 `x=w`；`node --check` 退出码 0、6 档 × 2 色断言中心恒为 `0.4h`、`smil=21`、端口比例不变、浏览器目视通过（详见步骤 2 实测行） |
| 步骤 3 | ✅ 完成 | 抽出 `makeVPipe` / `makeJointV` 统一进 / 出水管画法（管壁 + 管腔 + 端面法兰 + 2 螺栓）、阀门补手轮（按池宽 clamp、矮尺寸省略）、液位标尺补主 / 次刻度共 9 条、爬梯改双竖杆 + 档数 clamp；`node --check` 退出码 0、6 档 × 2 色断言全通过（`smil=21`、端口锚点比例不变）、浏览器实测无越界 / 不错压（详见步骤 3 实测行） |
| 步骤 4 | ⬜ 未开始 | 逐量 clamp 替换全部固定像素偏移；多档尺寸冒烟无 NaN / 无越界 / 比例不失真 |
| 步骤 5 | ⬜ 未开始 | `node --check` + 浏览器目视 + 动画数 21 不变 + 镜像 / 旋转 + 临时产物清理 |
| 步骤 6 | ⬜ 未开始 | 保留铭牌 / 文字，板统 `#2a2f45`/`#3f445c`、文字 `${c}`，尺寸定尺 |
