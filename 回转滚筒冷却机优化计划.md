# 回转滚筒冷却机（tubeCooler）风格对齐 · 分步优化计划

> 目标文件：`templates/tubeCooler.js`
> 风格基准：`templates/bucketElevator.js`（斗式提升机）、`templates/reactor.js`（反应釜）
> 参照样板：`templates/coolingPond.js`（冷却水池，上一轮按同一套步骤改造完成）、`templates/pump.js`、`templates/valve.js`、`templates/desulfTower.js`、`templates/dustCollector.js`、`templates/silo.js`、`templates/screwConveyor.js`、`templates/weighFeeder.js`、`templates/switchValve.js`
> 特殊指令：**保留铭牌并对齐配色**（本模板当前已有铭牌板 + `<text>回转滚筒冷却机</text>`，保留文字 / 文案，仅把板 / 描边统到基准色系，见步骤 6）
> 本文档用于记录每一步的进度，异常中断后从第一个未勾选项继续。
> 进度：**6 / 6 步完成**。

## 0. 风格基准清单（必须照搬）

| 项 | 取值 |
| --- | --- |
| 金属渐变 | `#7d849e → #d9dded → #767d97`（局部 `<defs>`，id 带 uid 后缀，不用外部全局 `gEquip`） |
| 深色内腔 / 基础 | `#12162b`，描边 `#6a7192` |
| 法兰 / 接管 | `#2a2f45` 填充 + `#3f445c` 描边 |
| 件体（金属件明细 / 亮线） | `#9aa2bc` + `#5b6280` |
| 螺栓头 / 指针件体色 | `#8e96b6` |
| 深色收口 | `#0c1020`，描边 `#6a7192` |
| 渐变方向 | 竖式箱体 / 底座用**横向渐变**（`x1=0,y1=0,x2=1,y2=0`）；横向风管用**竖向渐变**（`x1=0,y1=0,x2=0,y2=1`）；回转筒体圆柱明暗用**竖向渐变** |
| 定尺封顶 | 逐量 `Math.max(下限, Math.min(上限, 比例量))`；或 `H(hr, wr) = Math.min(h*hr, w*wr)`，放大 / 缩小时只有中间段变长，端面 / 法兰 / 接管 / 螺栓 / 铭牌等定尺件不随之变粗 |

## 1. 现状问题清单（读码定位结果）

- [x] P1 无局部金属渐变 + 依赖外部全局：全文件仅一处渐变 `drumShade_`（L72-79，且为深紫色系 `#1a1835/#262450/#353370`，非基准金属渐变），筒体壁 / 底座 / 料箱 / 托轮 / 滚带 / 齿圈 / 驱动 / 风管全部纯平涂色；**且有 5 处引用外部全局 `url(#gEquip)`**（L265 电机壳内衬、L276 进料箱内衬、L280 出料箱内衬、L376 送风管、L379 排风管），脱离编辑器全局 defs 即失效 → 步骤 1 新增局部 `<defs>`（uid 后缀 `tc_metal_` / `tc_metalv_`），把筒体明暗渐变一并收进 `<defs>`，彻底去掉 `gEquip`
- [x] P2 配色不符基准（仅铭牌板 `#0d0b20` 留步骤 6）：
  - `#1a1835`（×18：L38/L41 底座外框、L59 托轮轮圈 ×4、L73/L78 筒体渐变端色、L192 滚带外环 ×2、L219 齿圈外环、L257 小齿轮、L262 减速机、L264 电机、L275 进料箱、L279 出料箱、L375/L378 风管）
  - `#12102a`（×7：L39/L42 底座内衬、L193 滚带内环 ×2、L220 齿圈内环、L263 减速机内衬、L272 驱动底板）
  - `#0d0b20`（×12：L40/L43 底座踏面、L60 托轮轮毂 ×4、L258 小齿轮轮毂、L277/L281 料箱端板、L377/L380 风管端板、L412 铭牌板）
  - `#1f1d40`（×2：L83/L84 筒体两端封头椭圆）
  - `#080614`（×2：L278/L282 料箱内腔）
  - `#252545`（×108：L206 滚带齿 ×36×2、L234 齿圈齿 ×36）
  - `#7ec8ff`（×16：L360 冷空气粒子）、`#a8ddff`（×16：L366 冷空气尾迹）、`#b0c4ff`（×12：L403 热气）
  - `#262450`（×2）/ `#353370`（×2）：L74-77 筒体渐变中段
  - `rgba(255,255,255,0.06)`（L85 筒体内壁高光线）、`#ffffff`/`#fff`（L111/L112/L114 筒体旋转白带）
  → 统到 `#7d849e→#d9dded→#767d97`（金属外壳）、`#12162b`/`#6a7192`（深腔 / 内衬）、`#2a2f45`/`#3f445c`（法兰 / 接管 / 端板）、`#9aa2bc`/`#5b6280`（件体 / 亮线）、`#0c1020`（收口 / 齿牙）、`#8e96b6`（螺栓头）、`#d9dded`（高光 / 白带）、`${c}`（粒子与运动件）
- [x] P3 颜色无兜底：`const c = p.color;`（L17）→ `(p && p.color) || '#9C99FF'`（`p` 缺省时会抛错，其余设备均已兜底）
- [ ] P4 定尺缺失（固定像素偏移）：`+8`（L23-24 筒体裁切内缩）、`h-6`（L28 底座基线）、`tireW=16`（L36）、`baseH=14`（L37）、`trunnionR=10`（L46）、`+2 / 0.85`（L50 托轮间距）、`flightCount=8`（L139）、`tTeethCount=36 / tToothH=5 / -3 / -6`（L186-193 滚带）、`gearR=R+10 / rx=7 / rx=4 / gTeethCount=36`（L217-220 齿圈）、`pinionR=8 / pTeethCount=14 / +6 / +18 / +4`（L242-254 驱动）、`22×26 / 18×22 / 24×22 / 16×18`（L262-265 减速机 / 电机）、粒子数 `28 / 16 / 12`（L290/L341/L384）、`+25 / +12 / -50`（L296 料床粒子排布）、风管 `±12 / ±8 / +4 / +14 / -5 / -7 / +5`（L375-380）、`+20 / -40 / -2-28*t`（L389/L394 热气）、`-15 / -20 / 4 / 3`（L345-347 冷气）→ 引入逐量 clamp 与比例量
- [x] P5 端口锚点错位（新发现，类冷却水池 P7）：
  - `airIn (0.9, 1, down)` 锚点横坐标 = `0.9w`，而送风管绘制在 `airInX = w*0.88`（L374），默认 300 宽偏差 **6px**；且送风管最下端只到 `baseY+2 = h-4`（L377 端板 y=`baseY-3` 高 5），未抵 `y=h`，与端口 `y=1` 差 **4px**
  - `airOut (0.1, 0, up)` 锚点横坐标 = `0.1w`，而排风管绘制在 `airOutX = w*0.12`（L374），偏差 **6px**
  - `inlet (0, 0.5, left)` / `outlet (1, 0.5, right)`：料箱左 / 右端已收在 `x=0` / `x=w`，中心与 `h*0.5` 同轴，正常 → 步骤 2 以端口锚点重定位送 / 排风管（`airInX=w*0.9`、`airOutX=w*0.1`），并令最外端分别抵 `y=h` / `y=0`
- [ ] P6 小尺寸越界（新发现）：热气轨迹最高点 `drumTop-2-28 = h*0.3-30`（L394），`h<100` 时越画布上沿（如 `h=60` → `y=-12`）；排风管固定 `y=5` 且高 `cy-R-19 = h*0.3-19`（L379），`h<64` 时高为负；`baseY-baseH = h-20` 与筒体底 `h*0.7` 在小 `h` 时重叠（如 `h=60` → 底座顶 `40` < 筒底 `42`）；托轮圆心 `baseY-baseH-trunnionR = h-30` 会越过筒心 `h*0.5` → 步骤 4 全部改为「比例 × 定尺封顶」，并对热气 / 风管 / 底座设上下限保证小尺寸不越界、不退化
- [x] P7 细节缺失（提升机式画法）：送 / 排风管无端面把合法兰环 + 螺栓、料箱无检修人孔 / 门、托轮无轴座 / 轴承座、底座无地脚螺栓 → 步骤 3 补齐风管统一画法（管壁 + 管腔 + 端面法兰 + 螺栓）、料箱人孔门、托轮轴座、底座地脚螺栓
- [ ] P8 铭牌 + `<text>`（L412-413）：**用户决定保留**，但板色 `#0d0b20` / 描边 / 文字需统到基准色系（板 `#2a2f45` + `#3f445c` 描边，文字 `${c}`）→ 步骤 6

## 2. 必须保持的对外契约（不得改动）

- 模板 key：`TEMPLATES.tubeCooler`
- `name: '回转滚筒冷却机'`、`category: '设备'`、`defaultSize: { w: 300, h: 140 }`
- `ports` 原样：
  - `inlet (0, 0.5, left)`
  - `outlet (1, 0.5, right)`
  - `airIn (0.9, 1, down)`
  - `airOut (0.1, 0, up)`
- 端口对齐约束：
  - `inlet` / `outlet` 的 `y=0.5` 必须与进 / 出料箱中心 `cy = h*0.5` 严格同轴；进料箱左端固定 `x=0`、出料箱右端固定 `x=w`
  - `airIn` 送风管最外端横坐标固定 `x=0.9w`（修 P5），管口底边固定 `y=h`
  - `airOut` 排风管最外端横坐标固定 `x=0.1w`（修 P5），管口顶边固定 `y=0`
- 动画标签形式：`<animate>` / `<animateTransform>`（编辑态由 `editor.js stripSMIL()` 剥离）
- 已有动画保留、不得增删：`<animate>` **608** 个、`<animateTransform>` **6** 个（托轮 4 + 小齿轮 / 电机风扇 2）→ 全模板 SMIL 总计数恒为 **614**
- 文字 / 铭牌：保留 `<text>回转滚筒冷却机</text>` 与实际 `nameplate` 板（用户决定），文字内容与文案不得变动
- 画布越界约束：所有图元（含铭牌、风管、托轮、热气 / 冷气 / 物料粒子轨迹）渲染后须落在 `x∈[0,w]`、`y∈[0,h]` 内

> 契约实测（vm 载入 `templates/tubeCooler.js` 后读取全局 `TEMPLATES.tubeCooler`）：`name=回转滚筒冷却机`、`category=设备`、`defaultSize={w:300,h:140}`、`ports=[inlet(0,0.5,left), outlet(1,0.5,right), airIn(0.9,1,down), airOut(0.1,0,up)]`、`render` 为函数；默认 300×140 渲染 SMIL 计数 `animate=608` + `animateTransform=6` = **614**。

## 3. 分步执行

### 步骤 1：筒体主结构与接管配色对齐风格
- [x] 1.1 新增局部 `<defs>`：`metalId`（横向渐变，给竖式件：底座 / 料箱 / 减速机 / 电机 / 驱动底板）、`metalVId`（竖向渐变，给横向风管与回转筒体圆柱明暗），id 带 uid 后缀（`tc_metal_` / `tc_metalv_`）；把现有筒体明暗渐变 `drumShade` 收进 `<defs>` 并改基准金属色系（竖向）
- [x] 1.2 筒体壁（`drumBody`）：`fill="url(#drumShade)"` 保留但改基准金属竖向渐变，描边 `${c}` → `#3f445c`；两端封头椭圆 `#1f1d40` → `#12162b` + `#6a7192`；内壁高光线 `rgba(255,255,255,0.06)` → `#5b6280`
- [x] 1.3 底座（`base1`/`base2`）：外框 `#1a1835` → `url(#metalId)`，描边 `${c}` → `#3f445c`；内衬 `#12102a` → `#12162b`；踏面 `#0d0b20` → `#0c1020` + `#6a7192` 描边
- [x] 1.4 托轮（`drawWheel`）：轮圈 `#1a1835` → `url(#metalId)`，描边 → `#3f445c`；轮毂 `#0d0b20` → `#12162b` + `#6a7192`；辐条 `${c}` → `#9aa2bc`；中心轴点保留 `${c}`
- [x] 1.5 滚带（`drawTire`）：外环 `#1a1835` → `url(#metalId)`，内环 `#12102a` → `#12162b`，齿牙 `#252545` → `#0c1020`，描边统 `#3f445c` / `#6a7192`
- [x] 1.6 大齿圈（`girthGear`）：外环 `#1a1835` → `url(#metalId)`，内环 `#12102a` → `#12162b`，齿牙 `#252545` → `#0c1020`，描边统 `#3f445c` / `#6a7192`
- [x] 1.7 驱动（`drive`）：小齿轮 / 减速机 / 电机壳体 `#1a1835` → `url(#metalId)`，内衬 `#12102a` → `#12162b`，小齿轮轮毂 `#0d0b20` → `#12162b`；电机壳内衬 `url(#gEquip)` → `#12162b`；驱动底板 `#12102a` → `#2a2f45` + `#3f445c`
- [x] 1.8 进 / 出料箱（`inBox` / `outBox`）：箱体 `#1a1835` → `url(#metalId)`；内衬 `url(#gEquip)` → `#12162b`；端板 `#0d0b20` → `#2a2f45` + `#3f445c`；内腔椭圆 `#080614` → `#12162b` + `#6a7192`
- [x] 1.9 送 / 排风管（`airDucts`）：管体 `#1a1835` → `url(#metalVId)`（横管用竖向渐变）；内衬 `url(#gEquip)` → `#12162b`；端板 `#0d0b20` → `#2a2f45` + `#3f445c`
- [x] 1.10 粒子 / 运动件配色：冷空气粒子 `#7ec8ff` → `${c}`、尾迹 `#a8ddff` → `${c}`（降透明度）、热气 `#b0c4ff` → `${c}`；筒体旋转白带 `#ffffff`/`#fff` → `#d9dded`；料床 `${c}` 保留
- [x] 1.11 `const c = p.color;` → `const c = (p && p.color) || '#9C99FF';`
- [x] 1.12 校验：`node --check templates/tubeCooler.js` 退出码 0；多档尺寸渲染无 `gEquip` 残留、渐变 id 跨实例唯一、颜色兜底与自定义色均生效；`#1a1835`/`#12102a`/`#1f1d40`/`#080614`/`#252545`/`#262450`/`#353370`/`#7ec8ff`/`#a8ddff`/`#b0c4ff` 残留 = 0（铭牌板 `#0d0b20` 留步骤 6）
- 备注：固定像素偏移（P4）留步骤 4，本步只改配色 / 渐变 / 兜底与粒子色

### 步骤 2：端口锚点对齐（修 P5）
- [x] 2.1 定义 `const airInX = w*0.9;`、`const airOutX = w*0.1;`（与端口 `airIn(0.9,1,down)` / `airOut(0.1,0,up)` 锚点一致，加注释说明须与 ports 同步）
- [x] 2.2 送风管重定位：竖直管中心对齐 `airInX`，管口底边抵 `y=h`
- [x] 2.3 排风管重定位：竖直管中心对齐 `airOutX`，管口顶边抵 `y=0`
- [x] 2.4 校验：断言送风管中心 = `0.9w` 且底边 = `h`、排风管中心 = `0.1w` 且顶边 = `0`；`inlet` 左端 = `0` / 中心 = `0.5h`、`outlet` 右端 = `w` / 中心 = `0.5h`；端口比例实测不变（`0/0.5`、`1/0.5`、`0.9/1`、`0.1/0`）

### 步骤 3：细节补齐（提升机式画法）
- [x] 3.1 风管统一画法：抽出 `makeDuct` 式函数，输出「金属管壁（`metalVId`，横管用竖向渐变）+ `#12162b`/`#6a7192` 管腔 + 变径短节 + 端面把合法兰（`#2a2f45`/`#3f445c`）+ 2 颗 `#8e96b6` 螺栓」，送风管口底边对齐 `y=h`、中心 `airInX`；排风管口顶边对齐 `y=0`、中心 `airOutX`
- [x] 3.2 进 / 出料箱检修人孔 / 门：料箱正面补人孔盖 + 铰链 + 把手（件体色 `#9aa2bc`/`#5b6280`），尺寸按料箱宽 / 高 clamp，矮尺寸下自动缩小 / 省略
- [x] 3.3 托轮轴座 / 轴承座：每个托轮补轴承座块（`url(#metalId)` + `#3f445c` 描边）+ 地脚螺栓（`#8e96b6`），尺寸按托轮半径 clamp
- [x] 3.4 底座地脚螺栓：两底座顶面各补 2 颗 `#8e96b6` 螺栓头，位置按底座宽 clamp
- [x] 3.5 校验：端口锚点比例实测不变；浏览器目视法兰 / 螺栓 / 人孔 / 轴座与筒体 / 底座不错压 —— `node --check` OK、6 尺寸 ×3 配色 133 项 + 36 项锚点断言全通过、SMIL=614；人孔在 ≥150×70 档渲染、75×35 档自动省略（`#9aa2bc` 计数 28→26）

### 步骤 4：定尺封顶与自适应
- [x] 4.1 逐量 clamp：筒体裁切内缩、底座高 / 基线、托轮半径 / 间距、滚带 / 齿圈环厚与齿高、小齿轮半径、减速机 / 电机箱体尺寸、驱动底板、风管宽 / 壁厚 / 法兰 / 螺栓、料箱端板 / 内腔、粒子数与轨迹幅度——全部按筒径 / 机宽 / 机高封顶（引入 `F` / `clamp` / `K` + 细部定尺常量块；数量类齿数 / 粒子数 / 扬料板数保持恒定以保 SMIL 计数）
- [x] 4.2 替换所有固定像素偏移（P4 清单：`8 / 6 / 16 / 14 / 10 / 2 / 0.85 / 8 / 36 / 5 / 3 / 6 / 7 / 10 / 4 / 18 / 22 / 26 / 24 / 16 / 25 / 12 / 50 / 20 / 40 / 28 / 15 / 19` 等均已消除）
- [x] 4.3 热气轨迹按筒顶比例：最高点随 `drumTop` 走并设下限 `≥0`（小尺寸不越上沿），振幅 / 半径按筒径 clamp（`hwTop - hwRise*t`，`hwRise = max(0, …)`）
- [x] 4.4 风管高度按筒径 / 底座比例：竖直段高 `max(下限, …)`，矮尺寸下不出现负高 / 反向；底座与筒体间距设下限，避免重叠（`shaftH = max(ductMinShaft, …)`）
- [x] 4.5 校验：极端尺寸（300×140 / 600×280 / 150×70 / 75×35 / 300×70 / 560×140 / 40×20）冒烟均无 NaN、无 `gEquip`、无越界（含无负宽高 / 半径、热气 `cy≥0`），133 + 36 项断言全通过、SMIL=614；并附加驱动部件右移越界保护（`driveShift` 左移）

### 步骤 5：整体校验
- [x] 5.1 `node --check templates/tubeCooler.js` 退出码 0；仓库无 lint / typecheck 配置、无 `package.json`，以 `node --check` + 多尺寸冒烟为准
- [x] 5.2 目视逐档核对：默认 300×140、自定义色 `#ff9a5c`、极小 150×70、扁 560×140、极端 75×35 —— 金属筒体明暗、深色内腔、料箱法兰 + 螺栓、托轮轴座、滚带 / 齿圈、驱动、风管把手、人孔、铭牌均正常，图元全部落在画布红框内，无裁切、无错压
  - 备注：沙箱内远程浏览器经代理访问 `localhost` 返回 `Unauthorized`（无法访问工作区本地静态服务），改用 `@resvg/resvg-js` 将 SVG 按 2–10× 光栅化为 PNG 后逐档目视，并叠加画布红框便于识别越界——较肉眼更利于判断裁切
- [x] 5.3 编辑态 / 运行态：`stripSMIL()` 命中（动画标签全部为自闭合 `<animate …/>`），编辑态 SMIL 计数为 0，运行态动画恢复；`<animate>` + `<animateTransform>` 总计数 **恒为 614**（默认色与自定义色实测均为 614）
- [x] 5.4 镜像 `mirrored` / 旋转 `rotation` / `namePos` 正常：模板自身不向外发出静态 `transform`（实测 `\stransform="` 命中 0，仅 SMIL 的 `attributeName="transform"`），编辑器在外层 `.equip-group` 施加镜像（`translate(w,0) scale(-1,1)`）与旋转不受影响；铭牌为模板内 `<text>`，镜像时随图形翻转
- [x] 5.5 临时核对页（`_tc_check.html` 与其共享包）已删除、本地静态服务（8090 遗留 + 8099）已停止，`git status` 无临时产物残留

### 步骤 6：铭牌保留 + 配色对齐（用户决定）
> 本模板当前已有铭牌板 + `<text>回转滚筒冷却机</text>`（L412-413），本次**保留**，仅统色与定尺。

- [x] 6.1 保留 `nameplate` 板 + `<text>回转滚筒冷却机</text>`，文字内容 / 文案不变
- [x] 6.2 板色 `#0d0b20` → `#2a2f45`（法兰 / 接管件色），描边 `${c}` → `#3f445c`
- [x] 6.3 文字填充 `${c}`（主题色）保留，`opacity` 维持 `0.75`，字号改为按机高 clamp（`plateFs = clamp(h*0.043, 3.5, 9)`，默认 140 → ≈6，替换固定 `6`）
- [x] 6.4 铭牌位置 / 尺寸定尺：`plateW = clamp(min(plateFs*10.7, w*0.6), 24, 96)`、`plateH = clamp(plateFs*2, 7, 18)`、`plateX / plateY` 夹在画布内（`clamp(…, 1, w-plateW-1)` / `clamp(…, 1, h-plateH-1)`）；默认 300×140 实测与改造前基本等价（板 x∈[117.9, 182.1]、y∈[117, 129]，文字基线 ≈125），极端 75×35 板宽由 64 收到 37.45、字号由 6 收到 3.5，不再越界 / 压筒
- [x] 6.5 校验：`grep` 确认 `nameplate` 与 `<text>回转滚筒冷却机</text>` 仍在、`#0d0b20` 残留 = 0、`node --check` 退出码 0；冒烟 `hasText=true`、动画数恒为 614；133 + 36 + 7 + 17 项断言全通过；默认色 / 自定义色 `#ff9a5c` 光栅化目视板色 / 文字色正常

## 4. 进度记录

| 步骤 | 状态 | 备注 |
| --- | --- | --- |
| 步骤 1 | ✅ 已完成 | 局部 `<defs>`（`tc_metal_`/`tc_metalv_` + `drumShade` 金属化）+ 金属 / 深腔 / 法兰配色 + 去 5 处 `gEquip` + 颜色兜底 + 粒子 / 白带改基准色；`node --check` OK、6 尺寸 ×3 配色 133 项断言全通过、SMIL=614 |
| 步骤 2 | ✅ 已完成 | `airInX=w*0.9`（口底边抵 `y=h`）/ `airOutX=w*0.1`（口顶边抵 `y=0`）重定位；6 尺寸 36 项锚点断言全通过 |
| 步骤 3 | ✅ 已完成 | `makeDuct`（管壁 + 管腔 + 变径 + 端面法兰 + 2 螺栓）、料箱人孔门（含铰链 / 把手，小尺寸省略）、托轮轴座 / 轴承座（轮轴 + 座块 + 2 螺栓）、底座地脚螺栓（每底座 2 颗）；`node --check` OK、133 + 36 项断言全通过、SMIL=614 |
| 步骤 4 | ✅ 已完成 | 全部固定像素偏移改「比例 × 定尺封顶」（`F` / `clamp` / `K` + 细部定尺常量块）；热气 / 风管 / 底座设上下限，小尺寸不越界 / 不重叠；数量类齿数 / 粒子数 / 扬料板数保持恒定（SMIL=614）；7 档尺寸（含 40×20）无 NaN / 无负宽高 / 热气 `cy≥0`，133 + 36 项断言全通过；附加驱动部件越界保护 `driveShift` |
| 步骤 5 | ✅ 已完成 | `node --check` 退出码 0；133 + 36 + 7 + 17 项断言全通过；运行态 SMIL=614、编辑态 `stripSMIL` 后 =0、无静态 `transform`；5 档尺寸 / 配色经 `@resvg/resvg-js` 光栅化逐档目视无裁切 / 错压；临时核对页与本地服务已清理 |
| 步骤 6 | ✅ 已完成 | 铭牌保留（`nameplate` + `<text>回转滚筒冷却机</text>` 文案不变）；板色 `#0d0b20` → `#2a2f45` + 描边 `#3f445c`，文字 `${c}`；字号 / 板宽 / 板高 / 位置全部按机高定尺封顶并夹在画布内；`#0d0b20` 残留 = 0、`node --check` OK、133 + 36 + 7 + 17 项断言全通过、SMIL=614 |
