# 料仓（silo）风格对齐 · 分步优化计划

> 目标文件：`templates/silo.js`（联动：`editor.js`、`preview.html` 的「料仓落料粒子」硬编码分支）
> 风格基准：`templates/bucketElevator.js`（斗式提升机）、`templates/reactor.js`（反应釜）
> 参照样板：`templates/dustCollector.js`（除尘布袋）、`templates/weighFeeder.js`（配料秤，已按同一套步骤改造完成）
> 本文档用于记录每一步的进度，异常中断后从第一个未勾选项继续。

## 0. 风格基准清单（必须照搬）

| 项 | 取值 |
| --- | --- |
| 金属渐变 | `#7d849e → #d9dded → #767d97`（局部 `<defs>`，id 带 uid 后缀，不用外部全局 `gEquip`） |
| 深色内腔 | `#12162b`，描边 `#6a7192` |
| 法兰 / 接管 | `#2a2f45` 填充 + `#3f445c` 描边 |
| 件体（金属件明细） | `#9aa2bc` + `#5b6280` |
| 门板 / 铰链 / 把手 | `#1a1f33` / `#3a4060` / `#8e96b6` |
| 定尺封顶 | `H(hr, wr) = Math.min(h*hr, w*wr)`；或逐量 `Math.max(下限, Math.min(上限, 比例量))` |
| 动画 | SMIL `<animate>` / `<animateTransform>`（编辑态由 `editor.js stripSMIL()` 剥离） |
| 接管三件套 | 金属渐变管壁 + 深色管腔（`#12162b`/`#6a7192`，内缩约 0.9）+ 端面法兰（`#2a2f45`/`#3f445c`） |

## 1. 现状问题清单（读码定位结果）

- [x] P1 依赖外部全局渐变：`class="equip-body"`（L14/16/17 共 3 处）→ 步骤 1 改局部 `<defs>` + uid 解决
- [x] P2 颜色被 CSS 覆盖：`.equip-body{stroke:var(--purple)}` 优先级高于行内 `stroke`，自定义色失效 → 步骤 1 去掉 class 解决
- [x] P3 颜色无兜底 → 步骤 1 加 `const c = (p && p.color) || '#9C99FF';` 解决
- [x] P4 死代码 `const hop=new Path2D();` → 步骤 1 删除
- [x] P5 配色不符基准（`#252545` / `#1a1a30`）→ 步骤 1~3 全面改基准配色
- [x] P6 无定尺封顶（料位条固定 `height="6"`）→ 步骤 3 改 `clamp(2,6,bodyH*0.06)`
- [x] P7 无任何细节 → 步骤 2~4 补齐接管三件套、法兰条、螺栓、人孔门、爬梯、支腿/基础/斜撑
- [x] P8 侧口无图形 → 步骤 2 新增右侧横向接管（中心线 `0.4h`，管口贴 `x=w`）解决
- [x] P9 无动画：仓内无落料表现（当前靠 `editor.js` L2848–2871 / `preview.html` L1329–1352 的**硬编码落料粒子**外挂实现）→ 步骤 5 改为模板 SMIL 驱动解决
- [x] P10 外挂粒子与仓体形状不匹配：粒子轨迹写死 `topY = y+5`、`bottomY = y+0.92h`、`sway = ±0.15w`，在锥体收口段会**飘到金属锥面之外**（锥体半宽 < `0.15w`）；且几何在 `editor.js` 与 `preview.html` 各存一份，改仓体形状需三处同步 → 步骤 5 用内腔 `clipPath` + 模板内唯一一份几何解决
- [x] P11 无 `clipPath`：内部图元/粒子无裁剪保护 → 步骤 5 加 `clipPath`（仓身 rect + 锥斗 polygon）解决

## 2. 必须保持的对外契约（不得改动）

- 模板 key：`TEMPLATES.silo`
- `name: '料仓'`、`category: '设备'`、`defaultSize: { w: 90, h: 150 }`（`demo-template.json` 实机 2 台均为 90×150）
- `ports`：`top (0.5, 0, up)` 顶部进料 / `bottom (0.5, 1, down)` 底部出料 / `side (1, 0.4, right)` 右侧进料或回料
- 接管绘制中心线须与 `ports.*.x*w` / `ports.*.y*h` 严格同轴：
  - 顶部进料管中心 → `x = 0.5w`，管口到 `y = 0`
  - 底部出料管中心 → `x = 0.5w`，管口到 `y = h`
  - 右侧接管中心 → `y = 0.4h`，管口到 `x = w`
- 动画标签形式：`<animate>` / `<animateTransform>`（编辑态由 `editor.js stripSMIL()` 剥离）

## 3. 分步执行

### 步骤 1：仓体主体对齐风格（已完成）
- [x] 1.1 删除死代码 `const hop=new Path2D();`；新增局部 `<defs>`：`metalId`（竖向渐变，给仓身/锥斗）、`metalVId`（横向渐变，给竖直接管），id 带 uid 后缀
- [x] 1.2 仓身 rect / 锥斗 polygon 改 `url(#${metalId})` + `#3f445c` 描边，去掉 `class="equip-body"`（解除对 `#gEquip` 与 CSS `stroke:var(--purple)` 的依赖）
- [x] 1.3 颜色兜底：`const c = (p && p.color) || '#9C99FF';`（组件自定义色必须真正生效）
- [x] 1.4 三层结构对齐除尘布袋/配料秤：后壁层（大一圈 `wallT`）→ 金属层 → 内腔深色层（`#12162b` + `#6a7192`，内缩 `padT`）
- [x] 1.5 校验：`node --check` 退出码 0；冒烟无 `gEquip` / 无 `equip-body` / 无 `NaN`、渐变 id 跨实例唯一、自定义色生效；浏览器目视

### 步骤 2：接管三件套与端口对齐（已完成）
- [x] 2.1 顶部进料管（竖）：金属管壁 + `#12162b` 管腔 + `#2a2f45` 端面法兰，管口贴 `y=0`，中心线 `0.5w`
- [x] 2.2 底部出料管（竖）：同上，管口贴 `y=h`，中心线 `0.5w`
- [x] 2.3 右侧接管（横，新增）：横向渐变管壁 + 管腔 + 端面法兰，中心线严格 `0.4h`，管口贴 `x=w`
- [x] 2.4 校验：`node --check` 退出码 0；结构化断言三管中心线与端口坐标一致；`ports` 原样未变；浏览器目视

### 步骤 3：提升机式细节（已完成）
- [x] 3.1 顶/底法兰条按基准色重画（`#2a2f45` + `#3f445c`），螺栓用 `bolt()`（双圆：`#2a2f45` + `#8e96b6` 内点）
- [x] 3.2 仓身人孔/检修门（`#1a1f33` 门板 + `#3a4060` 铰链×2 + `#8e96b6` 把手）
- [x] 3.3 爬梯：两条竖杆 + 横档，横档数量按 `bodyH` 自适应（`nRung`）
- [x] 3.4 料位条改为定尺：条厚 `clamp(2, 6, bodyH*0.06)`、填充比例与长度按仓身内宽计算，颜色用 `${c}`
- [x] 3.5 校验：`node --check` 退出码 0；冒烟无 `NaN`；浏览器目视门/梯/料位条不越界

### 步骤 4：支腿、基础与定尺封顶（已完成）
- [x] 4.1 支腿（件体 `#9aa2bc` + `#5b6280`）左右各一，自锥顶（`bodyBotY`）直落到基础板上沿；斜撑自支腿内侧斜拉到锥面 85% 高度处的锥壁点
- [x] 4.2 基础板（`#2a2f45` + `#3f445c`）+ 地脚螺栓（`#8e96b6` 圆点 ×4），底边严格落位 `y=h`（不出包围盒）；支腿横向让开中部出料管
- [x] 4.3 逐量 clamp：进料管长 `clamp(4,14,h*0.055)` / 出料管长 `max(0,...)` / 仓身与锥斗高受 `availH` 约束 / 法兰厚 / 螺栓半径 / 料位条厚 / 爬梯与斜撑宽度均已定尺封顶（本组件无文字，字号项不适用）
- [x] 4.4 消除负高：`outletH = Math.max(0, h-coneBotY)`，`outletH <= 0.6` 时整段出料管与锥底法兰一并省略；支腿/基础/斜撑带 `legH > 0.5`、`legH > 2 && coneH > 1` 守卫
- [x] 4.5 校验：`node --check` 退出码 0；7 档冒烟（90×150 实机、55×54、30×30、60×300、300×120、600×600、110×60）无 `NaN` / 无负值 / `over=0` / `leg=2 base=2 clash=0 bOver=0`，端口同轴仍 `dTop=dBot=dSide=0.00`；浏览器目视支腿、基础板、斜撑与锥斗/出料管互不干涉

### 步骤 5：动画与防溢出（含 editor/preview 联动）（已完成）
- [x] 5.1 `clipPath`：仓身 + 锥斗内腔边界，防止料流粒子溢到金属壁外
- [x] 5.2 仓内落料粒子 SMIL：自内腔顶部落到锥底出料口（`<animate>` `cy` + `opacity`），多颗用负 `begin` 错相
- [x] 5.3 移除 `editor.js` 与 `preview.html` 中的**硬编码料仓落料粒子分支**（`need += 8*n` 与 `if(comp.type!=='silo') return;` 两段），改由模板 SMIL 驱动
  - 理由：外挂粒子几何写死、锥体段会飘出金属面，且需在 `editor.js` / `preview.html` 各维护一份；模板 SMIL 天然被 `stripSMIL()` 控制，编辑态/运行态一致
- [x] 5.4 校验：`node --check` 退出码 0；编辑态 `stripSMIL` 后 `<animate` 计数为 0；运行态动画恢复；`editor.js` / `preview.html` 中已无 `silo` 粒子分支；浏览器目视运行态落料

### 步骤 6：整体校验（已完成）
- [x] 6.1 `node --check templates/silo.js` 退出码 0；`GetDiagnostics` 无告警
- [x] 6.2 浏览器目视：默认 90×150（`demo-template.json` 实机尺寸）、极小、细高、扁平、放大、自定义色兜底
- [x] 6.3 编辑态 `stripSMIL` 生效（`animate` 计数为 0）
- [x] 6.4 运行态动画恢复（`editor.html` 实机运行按钮）
- [x] 6.5 镜像 `mirrored`、旋转 `rotation`、`namePos` 正常；三个 `ports` 命中新接管管口
- [x] 6.6 清理临时核对页与产物

## 4. 进度记录

| 步骤 | 状态 | 备注 |
| --- | --- | --- |
| 步骤 1 | 已完成 | 仓体主体对齐风格（局部 defs / 三层结构 / 颜色兜底 / 料位条定尺） |
| 步骤 2 | 已完成 | 顶/底竖管 + 右侧横管三件套；三管中心线与端口偏差 0.00 |
| 步骤 3 | 已完成 | 把合法兰条（自动增多）+ 顶/底法兰条 + 螺栓 + 人孔门 + 仓内爬梯 |
| 步骤 4 | 已完成 | 支腿 ×2 + 基础板 ×2 + 地脚螺栓 ×4 + 锥斗斜撑；7 档冒烟 `leg=2 base=2 clash=0 bOver=0`，端口同轴 0.00 |
| 步骤 5 | 已完成 | 内腔 `clipPath` + 4 颗 SMIL 落料粒子；`editor.js` / `preview.html` 硬编码粒子分支移除；编辑态 `anim=0`、运行态 `24=3×8` |
| 步骤 6 | 已完成 | 整体校验通过；临时核对页 `_silo_check.html` 与冒烟脚本 `_silo_smoke.js` 已删除 |

## 5. 校验记录

### 步骤 1（仓体主体对齐风格）
- `node --check templates/silo.js` → 退出码 0；`GetDiagnostics` 无告警
- 7 档冒烟：`gq=false eb=false grad=2 nan=0 fb=true cu=true`（渐变 id 跨实例唯一、颜色兜底与自定义色均生效）
- 浏览器目视 `_silo_check.html`：三层结构、料位条无越界

### 步骤 2（接管三件套与端口对齐）
- `node --check` → 退出码 0
- 结构化断言：`dTop=dBot=dSide=0.00`（三管中心线与 `ports` 坐标零偏差）；`ports` 定义原样未变
- 期间修正：右侧接管管径由 `min(w*0.16, bodyH*0.42)` 收紧为 `min(w*0.11, bodyH*0.35)`，避免扁平/细高尺寸下接管过粗
- 浏览器目视：顶/底/右三管均与端口对齐，管腔与法兰配色正确

### 步骤 3（提升机式细节）
- `node --check` → 退出码 0；`GetDiagnostics` 无告警
- 7 档冒烟：`fl=9`（60×300 时 `fl=14`，把合法兰条按仓高自动增多）；无 `NaN` / 无负值 / `over=0`
- 料位条由 `bodyH*0.10` 收紧为 `bodyH*0.06`，避免大尺寸下过粗
- 浏览器目视：人孔门、爬梯、料位条三者互不重叠且不越界

### 步骤 4（支腿、基础与定尺封顶）
- `node --check templates/silo.js` → 退出码 0；`GetDiagnostics` 无告警
- 7 档冒烟（90×150 实机 / 55×54 / 30×30 / 60×300 / 300×120 / 600×600 / 110×60）全部 `FAIL = 0`：
  - `nan=false/false/false`（含 `null` props 与自定义色两次渲染）
  - `neg=0`（无负宽负高）、`over=0`（无元素越出包围盒）
  - `leg=2`（支腿 ×2）、`base=2`（基础板 ×2，底边贴 `y=h`）、`clash=0`（支腿不与中部出料管横向重叠）、`bOver=0`（斜撑端点均落在包围盒内）
  - 端口同轴未回退：`dTop=dBot=dSide=0.00`
- 冒烟脚本期间修正：基础板计数原先会把「出料管端面法兰（同贴 `y=h`）」误计入，改为排除中部 `0.40w~0.60w` 区间的矩形后 `base=2` 正确
- 浏览器目视（90×150 放大 6 倍、30×30 放大 12 倍、300×120、60×300）：支腿自锥顶落地、基础板与地脚螺栓在 `y=h` 收口、斜撑端点精确落在锥面 85% 高度处，与中部出料管、锥底法兰均不干涉

### 步骤 5（动画与防溢出 + editor/preview 联动）
- `node --check templates/silo.js`、`node --check editor.js` → 退出码均 0；`GetDiagnostics` 无告警
- 7 档冒烟（90×150 实机 / 55×54 / 30×30 / 60×300 / 300×120 / 600×600 / 110×60）全部 `FAIL = 0`，新增动画/裁剪断言逐档通过：
  - `anim=8/left0`：运行态 8 个 `<animate>`（4 颗粒子 × `cy` + `opacity`）；`stripSMIL()` 后 `<animate` 计数为 0
  - `clip=true/true`：`<clipPath id="silo_clip_*">` 定义与 `clip-path="url(#silo_clip_*)"` 引用均存在
  - `dot=4/out0`：粒子 4 颗，无一颗越出 `0.10w~0.90w` 横向安全区
  - 未回退项：`dTop=dBot=dSide=0.00`、`leg=2 base=2 clash=0 bOver=0 neg=0 over=0`、渐变 2 组、uid 唯一
- 硬编码粒子分支移除核对：`Grep silo` → `editor.js` 仅剩 L2847 说明注释 + L3041 内置 demo JSON；`preview.html` 仅剩 L1328 说明注释；`need += 8*n` 与粒子循环两段均已删除
- 浏览器核对页 `_silo_check.html`（10 格，`?t=Date.now()`）：全 10 格 `clip=1`、前 9 格 `anim=8 dot=4`，「编辑态 stripSMIL」格 `anim=0`；6 倍放大目视 90×150 可见 4 颗粒子在仓身内腔不同相位下落，最低一颗落在锥斗内且被锥面收口约束
- `editor.html` 实机（载入 `1#.json`，47 组件 / 38 管道，画布 3 台料仓）：
  - 编辑态：`canvasSilo=3 anim=0`（`stripSMIL()` 生效，静态粒子位于裁剪区之上不可见）
  - 运行态：`canvasSilo=3 anim=24 dots=12`，即每台 8 个动画、4 颗粒子，动画恢复正常

### 步骤 6（整体校验与产物清理）
- 6.1 `node --check templates/silo.js` → 0；`node --check editor.js` → 0；`GetDiagnostics(templates/silo.js)` 返回空数组
- 6.2 核对页 10 格目视：实机 90×150、自定义色 `#FF6600`（接管/法兰/粒子随色而变，无 CSS 覆盖）、极小 55×54、极小 30×30、细高 60×300、扁平 300×120、放大 600×600、镜像、旋转 90°、编辑态 stripSMIL —— 均无 `NaN`、无越界、无穿模
- 6.3 编辑态 `stripSMIL`：核对页该格 `anim=0` 且无可见粒子；`editor.html` 画布 3 台料仓 `anim=0`
- 6.4 运行态恢复：`editor.html` 点击「运行」后 `anim=24 dots=12`，逐台 8 个动画
- 6.5 变换与端口：核对页镜像格（`translate(w,0) scale(-1,1)`）左右接管互换正常、旋转 90° 格（`rotate(90,w/2,h/2)`）整体随转且无错位；`namePos` 由 `editor.js` 统一绘制、本次未触碰模板；冒烟中三个 `ports`（`top` / `bottom` / 侧口）与接管中心线偏差恒为 `0.00`
- 6.6 清理：`_silo_check.html`、`_silo_smoke.js` 已删除（`Glob _*` 无残留）；`editor.js` / `preview.html` 仅保留说明注释，无外挂粒子代码

