# 工业抽水泵（pump）风格对齐 · 分步优化计划

> 目标文件：`templates/pump.js`
> 风格基准：`templates/bucketElevator.js`（斗式提升机）、`templates/reactor.js`（反应釜）
> 参照样板：`templates/valve.js`（气密阀，上一轮已按同一套步骤改造完成）、`templates/desulfTower.js`（脱硫塔）、`templates/dustCollector.js`（除尘布袋）
> 特殊指令：无（区别于气密阀的「铭牌直接不要了」；本模板本就无 `<text>`，全程亦不新增文字／铭牌，见步骤 6）
> 本文档用于记录每一步的进度，异常中断后从第一个未勾选项继续。
> 进度：**0 / 6 步完成**（计划编制中）。

## 0. 风格基准清单（必须照搬）

| 项 | 取值 |
| --- | --- |
| 金属渐变 | `#7d849e → #d9dded → #767d97`（局部 `<defs>`，id 带 uid 后缀，不用外部全局 `gEquip`） |
| 深色内腔 | `#12162b`，描边 `#6a7192` |
| 法兰 / 接管 | `#2a2f45` 填充 + `#3f445c` 描边 |
| 件体（金属件明细） | `#9aa2bc` + `#5b6280` |
| 门板 / 铰链 / 把手 | `#1a1f33` / `#3a4060` / `#8e96b6` |
| 深色收口 | `#0c1020`，描边 `#6a7192` |
| 定尺封顶 | 逐量 `Math.max(下限, Math.min(上限, 比例量))`；或 `H(hr, wr) = Math.min(h*hr, w*wr)`，放大/缩小时只有中间段变长，端板 / 法兰 / 接管 / 螺栓等定尺件不随之变粗 |

## 1. 现状问题清单（读码定位结果）

- [ ] P1 无金属渐变（既未依赖全局 `gEquip`，也**没有任何** `url(#…)`）：全文件为纯平涂色，蜗壳 / 法兰 / 接管均无壳体明暗 → 步骤 1 新增本文件局部 `<defs>` + uid 后缀。
- [ ] P2 配色不符基准：
  - `#1a1835`（L22 蜗壳主体、L39 左端面法兰、L44 右端面法兰）
  - `#0d0b20`（L13 螺栓底、L33 压力表外圈）
  - `#252545`（L24 蜗壳轮毂）
  - `#151330`（L32 表座接管、L34 压力表内圈、L38 吸入管腔、L43 排出管腔）
  - `#6ecbf5`（L50 / L59 进/出口水流粒子，青蓝高饱和，非基准且未随 `${c}` 主题色）
  - `rgba(255,255,255,0.06)`（L40 / L45 法兰高光条，半透明白，非基准）
  → 统到 `#2a2f45`/`#3f445c`（法兰 / 端面）、`#12162b`/`#6a7192`（深腔）、`#9aa2bc`/`#5b6280`（件体）、`#0c1020`（收口）、`#8e96b6`（螺栓头 / 指针）、`${c}`（粒子与运动件）。
- [ ] P3 颜色无兜底：`const c = p.color;`（L12）→ `(p && p.color) || '#9C99FF'`（`p` 缺省时会抛错，其余设备均已兜底）。
- [ ] P4 定尺缺失（固定像素偏移）：
  - 螺栓半径 `r=1.8`（L13 默认参）、螺栓头 `r*0.4`、`stroke-width 0.6`（L13）
  - 压力表半径 `gaugeR = 4.5`、纵向偏移 `- gaugeR - 1`（L16）
  - 法兰宽 `flangeW = 9`（L17）
  - 表座接管 `gaugeCx-3 / width 6 / height 3`（L32）
  - 指针铰点 `r="1.4"`（L36）
  - 管腔 `cy-3 / height 6`（L38、L43）
  - 端面法兰 `cy-5 / height 10 / rx 1`（L39、L44）
  - 法兰高光条 `height 2`（L40、L45）
  - 螺栓 x 位 `bolt(2, …)`（L41、L46）
  - 水流粒子 `r="1.8"`（L50、L59）、起止 `x="3"` / `w-3`（L49、L58）
  → 引入逐量 clamp。
- [ ] P5 压力表在矮尺寸下越出上边界（新发现）：`gaugeCy = cy - R - gaugeR - 1 = h*0.12 - gaugeR - 1`，需满足 `gaugeCy - gaugeR ≥ 0`，即 `h*0.12 ≥ 2*gaugeR + 1`。默认 120×90 实算 `gaugeCy = 45 - 34.2 - 4.5 - 1 = 5.3`、表顶 `0.8`，**仅余 0.8px 勉强可见**；当 `h < 83.3`（如 60×45、40×30）时表顶 `< 0`，压力表被画布裁掉 → 步骤 2 重排修复。
- [ ] P6 无提升机式细节（端面把合法兰标准化 / 泵体中开把合法兰 / 泵底座机脚画法均缺失）→ 步骤 3 补齐。
- [ ] P7 文字元素决策：本模板当前无任何 `<text>`（压力表刻意无字，见 L31 注释），无铭牌、无型号字 → 步骤 6 明确**不新增**任何文字 / 铭牌，仅以几何细节表达（与气密阀「铭牌不要了」结论一致）。

## 2. 必须保持的对外契约（不得改动）

- 模板 key：`TEMPLATES.pump`
- `name: '工业抽水泵'`、`category: '阀门管件'`、`defaultSize: { w: 120, h: 90 }`
- `ports` 原样：
  - `in (0, 0.5, left)`
  - `out (1, 0.5, right)`
  - 端口 `y=0.5` 必须与 `cy = h/2`、两侧管腔中心严格同轴，改造时不得偏移；管口最外侧坐标固定为 `x=0`（左）/ `x=w`（右）
- 动画标签形式：`<animate>` / `<animateTransform>`（编辑态由 `editor.js stripSMIL()` 剥离）
- 已有动画保留、不得增删：叶轮旋转 `animateTransform`（3s）×1；进 / 出口水流粒子各 4 颗，每颗 `animate`(opacity) + `animateTransform`(translate)，合计 `4×2 + 4×2 = 16` → 全模板 `<animate>` + `<animateTransform>` 总计数恒为 **17**
- 画布越界约束：所有图元（含压力表、机脚）渲染后须落在 `x∈[0,w]`、`y∈[0,h]` 内

> 契约实测（编辑器中读取全局 `TEMPLATES.pump`）：`name=工业抽水泵`、`category=阀门管件`、`defaultSize={w:120,h:90}`、`ports=[in(0,0.5,left), out(1,0.5,right)]`、`render` 为函数，均未改动。

## 3. 分步执行

### 步骤 1：蜗壳主结构与接管对齐风格
- [ ] 1.1 新增局部 `<defs>`：`metalId`（横向渐变 `x1→x2`，给蜗壳圆盘 / 泵体）、`metalVId`（竖向渐变 `x1=0,y1=0,x2=0,y2=1`，给两根横向接管管壁），id 带 uid 后缀（`pump_metal_` / `pump_metalv_`），不再平涂
- [ ] 1.2 蜗壳主体（L22）：`fill="#1a1835"` → `url(#${metalId})`，描边 `${c}` → `#3f445c`
- [ ] 1.3 蜗壳内圈（L23）：保留描边环；轮毂（L24）`#252545` → `#2a2f45` + `#3f445c` 描边
- [ ] 1.4 两侧接管管腔（L38 / L43）：`#151330` → `#12162b`，描边 `${c}` → `#6a7192`
- [ ] 1.5 两侧端面法兰（L39 / L44）：`#1a1835` → `#2a2f45` + `#3f445c` 描边；高光条（L40 / L45）`rgba(255,255,255,0.06)` → `#5b6280`（件体亮线）
- [ ] 1.6 螺栓（L13）：底座 `#0d0b20` → `#2a2f45`，螺栓头 `${c}` → `#8e96b6`
- [ ] 1.7 压力表（L32 / L33 / L34）：表座接管 `#151330` → `#2a2f45` + `#3f445c`；表盘外圈 `#0d0b20` → `#12162b` + `#6a7192` 描边；内圈 `#151330` → `#12162b`；指针（L35）与铰点（L36）保留 `${c}`
- [ ] 1.8 水流粒子（L50 / L59）：`#6ecbf5` → `${c}`，与其余设备液体 / 粒子用主题色的约定统一
- [ ] 1.9 `const c = p.color;` → `const c = (p && p.color) || '#9C99FF';`
- [ ] 1.10 校验：`node --check templates/pump.js` 退出码 0；多档尺寸渲染无 `gEquip` 残留、渐变 id 跨实例唯一、颜色兜底与自定义色均生效；浏览器目视金属壳 + 深腔生效
- 备注：固定像素偏移（P4）留步骤 4，本步只改配色 / 渐变 / 兜底与粒子色

### 步骤 2：压力表布局重排（修 P5）
- [ ] 2.1 按可用顶部空间反推表径：可用间距 `gaugeGap = cy - R`（= `h*0.12`），令 `gaugeR = Math.max(1.6, Math.min(4.5, (gaugeGap - 1)/2))`，保证 `2*gaugeR + 1 ≤ gaugeGap`
- [ ] 2.2 表心重算：`gaugeCy = cy - R - 1 - gaugeR`，使表底 `gaugeCy + gaugeR = cy - R - 1`、表顶 `gaugeCy - gaugeR ≥ 0`（关键约束）
- [ ] 2.3 表座接管长度随间距定尺：由 `gaugeCy + gaugeR` 连到蜗壳顶 `cy - R`（保持与泵体衔接），高度按间距 clamp，避免矮尺寸下出现负高
- [ ] 2.4 指针长度 / 铰点圆 r 定尺（`r="1.4"` → clamp 到 `gaugeR` 比例）
- [ ] 2.5 校验：`node --check` 退出码 0；断言 120×90 / 90×90 / 60×45 / 40×30 下 `gaugeCy - gaugeR ≥ 0` 且 `gaugeCy + gaugeR ≤ cy - R`；默认 120×90 的 `gaugeR` 仍 ≈4.5（观感不变）

### 步骤 3：接管与泵体细节对齐（提升机式画法）
- [ ] 3.1 两根接管统一画法（抽 `makePipe(x0,x1,cy,ph,side)`）：金属管壁（`metalVId`）+ `#12162b`/`#6a7192` 管腔 + 端面把合法兰（`#2a2f45`/`#3f445c`）+ 2 颗 `#8e96b6` 螺栓；**管口端必须与端口锚点重合**（左管口 `x=0`、右管口 `x=w`，`side` 显式传参）
- [ ] 3.2 蜗壳中开把合法兰：沿蜗壳水平中分面（`y=cy`）加 `#2a2f45`/`#3f445c` 法兰带 + 左右四角 `#8e96b6` 螺栓，带宽 / 外伸 / 螺栓半径按蜗壳直径封顶
- [ ] 3.3 泵底座 / 机脚：蜗壳正下方加 `#9aa2bc`/`#5b6280` 件体底板 + 两侧机脚（脚板厚 / 外伸 / 高 clamp），底边收进 `h` 范围；矮尺寸下自动省略或减薄
- [ ] 3.4 校验：端口锚点比例实测不变（`in=0` / `out=1`、`y=0.5`）；浏览器目视左右法兰与管口位置正确、底座不与接管 / 蜗壳错压
- 备注（与参照样板的差异）：本模板默认仅 120×90 且为圆形蜗壳，**不设提升机式观察门**（`desulfTower` / `dustCollector` 的门板 / 铰链 / 把手画法不适用于此）——门色系 `#1a1f33`/`#3a4060`/`#8e96b6` 仅沿用螺栓头 / 指针件体色

### 步骤 4：定尺封顶与自适应
- [ ] 4.1 逐量 clamp：蜗壳半径 `R`、螺栓半径（`r 1.8`）、法兰板厚 `flangeW`、管腔高度（`6`）、表盘半径 `gaugeR`、表座接管高、指针铰点 `r 1.4`、粒子半径 `r 1.8`、底座 / 机脚厚、中开法兰带宽与螺栓
- [ ] 4.2 替换所有固定像素偏移（P4 清单：`±3 / ±5 / 6 / 10 / 2 / r 1.8 / r 1.4 / rx 1` 等）
- [ ] 4.3 叶轮片内外半径（`R*0.34` / `R*0.62`）、蜗壳内圈（`R*0.78`）、轮毂（`R*0.3`）、叶轮心（`R*0.12`）按 `R` 比例，小尺寸下设下限（≥0.4）避免退化为点
- [ ] 4.4 校验：极端尺寸（120×90 / 240×180 / 60×45 / 40×30 / 120×45）冒烟无 NaN、无 `gEquip`、无越界，比例不失真

### 步骤 5：整体校验
- [ ] 5.1 `node --check templates/pump.js` 退出码 0；`GetDiagnostics` 无告警（仓库无 lint / typecheck 配置，无 `package.json`，以 `node --check` + 多尺寸冒烟为准）
- [ ] 5.2 浏览器目视：默认 120×90、自定义色、极端尺寸（临时核对页放大 4x / 8x 复核金属壳、深腔、端面法兰、中开法兰、底座机脚、压力表）
- [ ] 5.3 编辑态 / 运行态：`stripSMIL()` 生效（编辑态 SMIL 计数为 0），运行态动画恢复（叶轮旋转 3s、进/出口各 4 颗粒子 2.4s），`<animate>` + `<animateTransform>` 总计数恒为 **17**
- [ ] 5.4 镜像 `mirrored`、旋转 `rotation`、`namePos` 正常（本模板无文字，`namePos` 仅影响编辑器侧标签）
- [ ] 5.5 临时核对页删除，清理产物

### 步骤 6：文字 / 铭牌决策（不新增）
> 本模板当前无任何 `<text>`，压力表刻意无字（L31 注释「无文字」）。
> 结论：不保留、也不新增铭牌与任何文字；泵体细节只用几何图元（中开法兰 / 机脚 / 端面法兰）表达。

- [ ] 6.1 明确不新增铭牌：与 `desulfTower` / `dustCollector` 中「铭牌保留 / 对齐配色」的分支相反，本模板不做 `nameplate` 板 + 文字（也不引入任何型号 / 压力等级文字）
- [ ] 6.2 步骤 1 / 3 新增的细节全部为几何图元，不含 `<text>`
- [ ] 6.3 校验：`grep -n` 确认 `templates/pump.js` 内无 `<text>`、无 `nameplate`；`node --check` 退出码 0；冒烟 `hasText=false`、动画数恒为 17

## 4. 进度记录

| 步骤 | 状态 | 备注 |
| --- | --- | --- |
| 步骤 1 | 未开始 | 局部 `<defs>`（`pump_metal_` / `pump_metalv_`）+ 深腔 / 法兰配色 + 颜色兜底 + 粒子改 `${c}` |
| 步骤 2 | 未开始 | 压力表按顶部可用间距反推表径与表心，保证 `gaugeCy - gaugeR ≥ 0` |
| 步骤 3 | 未开始 | `makePipe` 统一左右接管（管口对齐 x=0 / x=w）+ 蜗壳中开把合法兰 + 泵底座机脚；不设观察门 |
| 步骤 4 | 未开始 | 逐量 clamp 替换全部固定像素偏移；多档尺寸冒烟无 NaN / 无越界 / 比例不失真 |
| 步骤 5 | 未开始 | `node --check` + 浏览器目视 + 镜像 / 旋转实测 + 动画数 17 不变 + 临时产物清理 |
| 步骤 6 | 未开始 | 明确不新增铭牌 / 文字，全模板保持无 `<text>` |
