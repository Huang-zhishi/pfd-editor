# 固体三通阀（switchValve）风格对齐 · 分步优化计划

> 目标文件：`templates/switchValve.js`（经 grep 确认联动点：`editor.js` L1063-1069 / L1504-1526 / L1617-1629 / L2131-2184，`preview.html` L365-404，均为**按 props 与 `g.dataset.active` 增量刷新**，不依赖模板内部结构，实测**无需改动联动代码**）
> 风格基准：`templates/bucketElevator.js`（斗式提升机）、`templates/reactor.js`（反应釜）
> 参照样板：`templates/screwConveyor.js`（螺旋输送机，已按同一套步骤改造完成，本轮主要实现参照）
> 本文档用于记录每一步的进度，异常中断后从第一个未勾选项继续。

## 0. 风格基准清单（必须照搬）

| 项 | 取值 |
| --- | --- |
| 金属渐变 | `#7d849e → #d9dded → #767d97`（局部 `<defs>`，id 带 uid 后缀，不用外部全局 `gEquip`） |
| 深色内腔 | `#12162b`，描边 `#6a7192` |
| 法兰 / 接管 | `#2a2f45` 填充 + `#3f445c` 描边 |
| 件体（金属件明细） | `#9aa2bc` + `#5b6280` |
| 门板 / 铰链 / 把手 | `#1a1f33` / `#3a4060` / `#8e96b6` |
| 定尺封顶 | `H(hr, wr) = Math.min(h*hr, w*wr)`；或逐量 `Math.max(下限, Math.min(上限, 比例量))` |
| 动画 | SMIL `<animate>` / `<animateTransform>`（编辑态由 `editor.js stripSMIL()` 剥离，故动画元素须写成**闭合标签**） |
| 接管三件套 | 金属渐变管壁 + 深色管腔（`#12162b`/`#6a7192`，内缩 1 个壁厚）+ 端面法兰（`#2a2f45`/`#3f445c`） |
| 指示色 | `const c = (p && p.color) || '#9C99FF';` 仅用于**运动件/状态件**（翻板、执行器活塞、物料粒子），件体一律基准配色 |

## 1. 现状问题清单（读码定位结果）

- [x] P1 依赖外部全局渐变：L32 进料立管、L37 菱形分料区、L41 左斜分支、L45 右斜分支，共 **4 处** `fill="url(#gEquip)"`（`gEquip` 定义在 `editor.html`，同页多实例共用同一 id）→ 步骤 1 改局部 `<defs>` + uid 解决
- [x] P2 颜色无兜底：L24 `const bodyColor = p.color;`，且 L13-16 直接对 `p` 赋值（`p` 为 `null` 抛错）；粒子 L85 亦直用 `bodyColor` → 步骤 1 加 `const c = (p && p.color) || '#9C99FF';` 并改为非变异局部量
- [x] P3 配色不符基准：`#0f0e22`（L33/42/46/50）、`#161534`（L34/49/53/69/95）、`#1a1a3a`（L38）→ 步骤 1~2 全面改基准配色
- [x] P4 无 `<defs>`、无 uid 防冲突 → 步骤 1 新增局部金属渐变 + uid 后缀
- [x] P5 无 `clipPath`：物料粒子（`cx`/`cy` 动画）无裁剪，路径折点多，过渡段会飘到阀体外 → 步骤 5 加内腔裁剪解决
- [x] P6 定尺硬编码：`inW = w*0.22`、`outW = w*0.2`、`pipeThick = w*0.18`、`splitW = w*0.5`、`topY/splitY/midY/outletY` 全按比例直取，无上下限；`flapW`、执行器尺寸、法兰悬出宽度均写死 → 步骤 4 逐量 clamp + 净空守卫
- [x] P7 **端口与画法不对齐（结构性缺陷）**：`inlet` 端口在 `y=0`，模板却把进料口画在 `topY = 0.08h`（悬空 0.08h）；`outletA/B` 端口在 `y=1`，出口法兰却画在 `outletY = 0.88h`（悬空 0.12h），端口圆与管口错位 → 步骤 1 按端口坐标反推几何，使管口顶沿 `y=0`、出口管口底沿 `y=h`
- [x] P8 `autoBadge` 顶部越界：L103 `rect y="-1"`（组件框外 1px）→ 步骤 3 归位到组件内
- [x] P9 动画几何写死：`dur="2.2s"`、`pCount = 4`、`begin` 相位、`targetX` 取 `outAX/outBX`、`cy` 关键帧 `topY+6 → splitY+8 → outletY` 均硬编码 → 步骤 5 改为由端口与内腔推导、周期与粒子数自适应
- [x] P10 外壳由 4 组独立 polygon 拼成（立管 / 菱形 / 左右分支各自描边），接缝处描边互相穿插；出口管与斜分支之间无过渡，出口法兰与管口错位 → 步骤 1 合并为**单条闭合路径**（一次描边、无接缝）

## 2. 必须保持的对外契约（不得改动）

- 模板 key：`TEMPLATES.switchValve`
- `name: '固体三通阀'`、`category: '阀门管件'`、`defaultSize: { w: 120, h: 150 }`
- `ports`（原样保留，三个）：
  - `inlet (0.5, 0, up)` 顶部进料 → 画法：进料立管中心线 `x = 0.5w`，管口**顶沿** `y = 0`
  - `outletA (0.3, 1, down)` 左下出口 → 画法：出口管中心线 `x = 0.3w`，管口**底沿** `y = h`
  - `outletB (0.7, 1, down)` 右下出口 → 画法：出口管中心线 `x = 0.7w`，管口**底沿** `y = h`
- props 语义（不得改含义）：
  - `switchValve`：`0` = 走 A 路（左出口），`1` = 走 B 路（右出口）；非 0/1 时按 `0` 处理
  - `controlMode`：`'manual'`（默认）/ `'auto'`
  - `sensorA` / `sensorB`：A/B 路开关量标签串（可为空串）
  - `color`：指示色，**须有兜底**（缺省 `#9C99FF`）
- 联动依赖（`editor.js` L2131-2184 `refreshSwitchValveAuto`、`preview.html` L365-404 同构）：
  - 依赖 `TEMPLATES['switchValve'].render(w, h, props)` 返回**自包含 markup**，且可被重复注入到 `g.innerHTML`（editor 路径）或 `g.children[0].innerHTML`（preview 路径）
  - 依赖 `g.dataset.active` 与 props 的 `switchValve` 一致；渲染结果须**完全由 props 决定**（不得依赖渲染次数、时间戳等外部状态）
- 属性面板（`editor.js` L1504-1526 / L1617-1629）读取的 `comp.props` 字段：`controlMode` / `switchValve` / `sensorA` / `sensorB` —— 模板**不得写回/变异** props（旧代码在 render 内对 `p.controlMode` 等赋值，本轮改为局部量）
- 动画标签形式：`<animate>`（编辑态由 `editor.js stripSMIL()` 剥离，故动画元素须写成闭合标签）
- 本组件**不接** `runTag` / `_run`，动画恒运行、编辑态被 `stripSMIL()` 剥离
- **模板不处理 `mirrored`**：镜像由宿主统一包一层 `translate(w,0) scale(-1,1)`，端口圆坐标由宿主按 `(1-pt.x)` 计算（`Grep mirrored templates/` 无命中，本轮不改）

## 3. 分步执行

### 步骤 1：阀体主体重构（局部 defs + 基准配色 + 端口与画法对齐）— 已完成
- [x] 1.1 新增局部 `<defs>`：`metalId`（横向 `#7d849e → #d9dded → #767d97`），id 带 uid 后缀 `sv_metal_*`；删除 4 处 `url(#gEquip)`
- [x] 1.2 颜色兜底 `const c = (p && p.color) || '#9C99FF';`；props 归一改为**非变异局部量**（`p` 为 `null` 不再抛错）
- [x] 1.3 几何改为「端口反推」：`cx = 0.5w`、进料管口顶沿 `y = 0`；`outAX = 0.3w`、`outBX = 0.7w`、出口管口底沿 `y = h`
- [x] 1.4 阀体外壳合并为**单条闭合 `<path>`**（15 顶点）：立管左壁下行 → 菱形左侧顶点 → 左斜分支 → 左出口管 → 底部凹口（回菱形下顶点）→ 右出口管 → 右斜分支 → 菱形右侧顶点 → 立管右壁，一次描边无接缝
- [x] 1.5 内腔合并为**单条闭合 `<path>`**（同拓扑法向内缩 1 个壁厚）：菱形为平行四边形，按内切圆半径 `inR = hw·dh/√(hw²+dh²)` 等比缩放 `k = (inR-wall)/inR` 即等价于严格法向内缩，无需逐边求交；填充 `#12162b` + 描边 `#6a7192`
- [x] 1.6 校验：`node --check` 退出码 0；冒烟无 `NaN`/负值/越界；浏览器核对「管口顶沿 `=0`、出口管口底沿 `=h`、中心线 `=0.5w/0.3w/0.7w`」零偏差

### 步骤 2：进料口与出口接管三件套（端面法兰）— 已完成
- [x] 2.1 顶部进料口端面法兰（顶沿严格 `y=0`，`#2a2f45` + `#3f445c`，宽度按管径悬出定尺）
- [x] 2.2 左右出口端面法兰（底沿严格 `y=h`，同配色与定尺）
- [x] 2.3 校验：端口命中断言（顶沿 `0` / 中心 `0.5w`；底沿 `h` / 中心 `0.3w`、`0.7w`）零偏差

### 步骤 3：翻板 / 封堵叉 / 执行器（状态件）+ AUTO 标签归位 — 已完成
- [x] 3.1 翻板：轴心 `pvY = splitY`（菱形腔中心）；角度与长度由「轴心 → 未选中出口管口中心」连线推导（`flapAng` / `flapLen`），尺寸随菱形腔定尺；指示色 `c`
- [x] 3.2 封堵叉（`#FF5555`）：标记**未选中**出口，位置由对应出口管中心线推导（不再写死 `cx±splitW/4`）
- [x] 3.3 执行器：缸体 + 活塞 + 指向箭头，位置由菱形右侧顶点推导（`cx + hw + actGap`），`c` 作指示色
- [x] 3.4 AUTO 标签（P8）：`rect y="-1"` 顶部越界 1px → 归位到 `badgeY = flH + 1`、贴在进料法兰之下立管右侧净空
- [x] 3.5 校验：`switchValve=0/1` 两态互斥断言（选中路无封堵叉、未选中路有封堵叉）、`auto`/`manual` 两态断言

### 步骤 4：定尺封顶与极小尺寸守卫 — 已完成
- [x] 4.1 逐量 clamp：`inW` / `outW` / `hw` / `dh` / `wall` / `flH` / 法兰悬出宽度 `flW` / 翻板长度与宽度 / 执行器尺寸 / AUTO 标签
- [x] 4.2 净空与负值守卫：法兰悬出量 `ovIn` / `ovOut` 受「组件边缘 / 相邻出口管净空」让位约束；出口管顶 `pipeTopY` 低过菱形下顶点（留底部凹口）且给竖直管留可辨识长度；执行器可用宽度 `(avail-actGap) < 5` 整段省略；翻板按 0.82 逐档回退至 `<2.5` 整段省略
- [x] 4.3 校验：**12 档**冒烟（含真实下限 20×20，依据 `editor.js` L1224 `const MIN = 20;`）无 `NaN`/负值/越界；结构断言（净空 ≥ 0）全过

### 步骤 5：clipPath 防溢出与 SMIL 物料流收敛 — 已完成
- [x] 5.1 `clipPath` 取内腔边界（`<path d="${bodyPath(wall)}"/>`），约束物料粒子
- [x] 5.2 物料粒子路径改为「立管 → 菱形 → 选中出口管」三段，共 5 个关键帧端点由端口与内腔推导（`kx/ky/kt`）
- [x] 5.3 周期 `dur` 与粒子数按尺寸/路径长度自适应（`pCount = clamp(3,8, round(路径长/26))`、`pDur = clamp(1.2,3, 路径长/68)`），`begin` 负错相形成连续料流；静态 `cx/cy` 亦沿路径均匀铺开（编辑态剥离动画后仍可见料位分布）
- [x] 5.4 校验：冒烟 `clip=true`、`stripSMIL()` 后 `<animate` 计数为 0、粒子全部落在裁剪组内且静态坐标在内腔内

### 步骤 6：整体校验与产物清理 — 已完成
- [x] 6.1 `node --check templates/switchValve.js` 退出码 0（无告警）
- [x] 6.2 浏览器目视：默认 120×150、极小 20×20、细高 60×300、扁平 200×20、窄高 20×200、自定义色兜底、AUTO、镜像、旋转 90°、编辑态 —— 10 格 55/55 断言 PASS
- [x] 6.3 编辑态 `stripSMIL` 生效（`animate` 计数 0）；`editor.html` 实机载入 `addComponent('switchValve')` 目视正常
- [x] 6.4 镜像 `mirrored` / 旋转 `rotation` 正常；三个 `ports` 命中对应结构（画布实机局部坐标偏差 `0.00`）
- [x] 6.5 自动互斥链路回归：`controlMode='auto'` + `sensorA/B` 取数 → 画布激活路切换、`g.dataset.active` 同步（5 用例全 PASS）
- [x] 6.6 清理临时核对页与冒烟脚本（`_sv_check.html` / `_sv_smoke.js` / `_sv_dbg.js` 已删除）

## 4. 进度记录

| 步骤 | 状态 | 备注 |
| --- | --- | --- |
| 步骤 1 | 已完成 | 单条闭合阀体（15 顶点）+ 内腔同拓扑内缩 + 局部 defs/uid + 端口反推几何；`check=0`、12 档冒烟 `ALL OK`、浏览器核对页端口零偏差、包围盒 0 越界 |
| 步骤 2 | 已完成 | 三个端面法兰（顶沿 `y=0` / 底沿 `y=h`），悬出量受组件边缘与相邻出口管净空让位约束；12 档冒烟 `法兰数=3`、横向 0 越界、左右不压盖 |
| 步骤 3 | 已完成 | 翻板（轴心菱形中心、角度由未选中出口推导）+ 封堵叉（互斥）+ 执行器 + AUTO 归位；5 档状态件冒烟全 OK、两态互斥与 auto/manual 断言全过 |
| 步骤 4 | 已完成 | 逐量 clamp + 悬出/净空/极小尺寸守卫；**12 档**冒烟（含 20×20 真实下限）`ALL OK`、`bad=0`、包围盒 0 越界、`stripSMIL` 后无残留 |
| 步骤 5 | 已完成 | 内腔 `clipPath` + 5 关键帧粒子流（`dur`/粒子数自适应、负错相、静态位也沿路径铺开）；核对页粒子数 3~8、`anim` 运行态 9~24 / 编辑态 0 |
| 步骤 6 | 已完成 | 整体校验与产物清理；`check=0`、12 档 + 5 档冒烟 `ALL OK`、核对页 55/55 PASS、`editor.html` 编辑态 `anim=0` + 端口局部坐标 `0.00` + 镜像 A/B 互换 + `rotate(90 60 75)`、自动互斥链路 a~e 全 PASS，临时产物已删 |

## 5. 校验记录

### 步骤 1（阀体主体重构）

- `node --check templates/switchValve.js` → `check=0`
- `node _sv_smoke.js`（12 档：120×150 / 200×90 / **20×20** / 20×200 / 200×20 / 60×300 / 300×60 / 120×20 / 24×20 / 20×90 / 600×600 / 110×60）→ **ALL OK**
  - 每档 `path=3`、`pts=15`（外壳 / 内腔 / clipPath 各一条路径）；`NaN`/`undefined`/`null` 均为 0；`gEquip=0`
  - 端口命中断言：立管 `y=0` 顶点 2 个、中心线 `=0.5w`；出口 `y=h` 顶点 4 个、中心线 `=0.3w`/`0.7w`，且左右出口管互不压盖
  - 内腔同拓扑断言：内腔 `y=0` 顶点 2 个、`y=h` 顶点 4 个（管口与外壳齐平），内腔全部顶点在 `[0,w]×[0,h]`
  - 配色断言：`sv_metal_*` 局部渐变存在且被 `url(#…)` 引用、`#12162b` + `#6a7192` 内腔配色存在
- **修正记录（法兰互相压盖，w=20 档）**：初版 `flW` 只有下限 1.5，导致左右出口法兰在 `x=10` 处相切 → 新增 `ovIn` / `ovOut` 悬出量让位约束：
  `ovIn = max(0.8, min(flW(inW), cx - inW/2 - 0.2))`、`ovOut = max(0.8, min(flW(outW), (outBX-outAX-outW)/2 - 0.2))`，并新增断言 `fBot[0].x + fBot[0].w < fBot[1].x`。
  （冒烟脚本自身也修正过一处：属性正则 `/\\w+=/` 不匹配 `x1`/`y1` 这类含数字的属性名，导致 X 取到 `NaN` 误报，已改为 `/([a-zA-Z][a-zA-Z0-9-]*)="([^"]*)"/g`。）
- 结论：**P1 / P2 / P3（阀体部分）/ P4 / P7 / P10 已解决**。

### 步骤 2（端面法兰）

- 12 档冒烟断言（从渲染出的 `<rect fill="#2a2f45">` 反查，不重算模板公式）：
  - `y=0` 的进料法兰恰好 1 个、中心 `=0.5w`；`y+h = h` 的出口法兰恰好 2 个、中心 `=0.3w` / `0.7w`
  - 所有法兰 `x ≥ 0` 且 `x+width ≤ w`（横向 0 越界）
- 浏览器核对页：端口命中行同时覆盖了法兰所在端面（`inlet=60.00/60.00 A=36.00/36.00 B=84.00/84.00`，偏差 `0.00`）

### 步骤 3（状态件 + AUTO 归位）

- `node _sv_smoke.js` 状态件段（5 档：120×150 / 200×90 / 20×20 / 20×200 / 600×600）→ **全部 OK**：
  - ① 两态互斥：`switchValve=0` 时红叉中心 `=0.7w`、`=1` 时 `=0.3w`，每态恰好 2 条 `stroke="#FF5555"` 线（X 形）；封堵叉严格落在未选中出口管内腔里（按内腔多边形射线法点内测试）
  - ② 翻板两态反向：`active=0` 翻板自由端伸向右路（`tipX > 0.5w+1`）、`active=1` 伸向左路（`tipX < 0.5w-1`），且四点全部落在内腔内
  - ③ AUTO 标签：`manual` 态不出现；`auto` 态 `w ≥ 60` 时恰好 1 个且完全在组件内
  - ④ 执行器缸体（`fill="#252a44"`）完全在组件内
  - ⑤ 指示色兜底：`props` 缺 `color` → `#9C99FF`；`props = null` → 不抛错、无 `NaN`
- **修正记录 3 则**：
  1. 封堵叉越出出口管内腔（200×90 / 20×200 等窄档）—— 初版 `xr` 下限 1.8 大于实际内腔半宽 `outW/2 - wall` → 改为 `xr = max(0.9, min(4.5, outW*0.28, outW/2 - wall - 0.25))`。
  2. 翻板越出内腔（20×20 喉口最窄）—— 矩形翻板四角外溢 → 改为「轴端宽、自由端收窄」的楔形板，并按 0.82 逐档回退 `flapLen` 直到四点全进内腔，回退到 `< 2.5` 时整段省略（极小尺寸守卫）。
  3. AUTO 标签 `y="-1"` 越界（P8）—— 归位为 `badgeY = flH + 1` 并放到立管右侧净空；`badgeW < 18` 时整段省略（20×20 / 24×20 / 20×90 / 20×200 会省略，已在冒烟脚本中按 `w >= 60` 才强制要求）。

### 步骤 4（定尺封顶与极小尺寸守卫）

- 12 档冒烟 `ALL OK`：`NaN`/`undefined`/`null` = 0、负值 = 0、越界 = 0；每档 `pts=15` 恒定（顶点数不随尺寸变化，说明拓扑稳定）
- 结构断言：
  - `pipeTopY > botY`（出口管顶低过菱形下顶点，底部凹口存在）
  - `midY` 受 `inW*0.5` 下限约束（立管下端不高于立管半径，避免立管"缩进"菱形）
  - `kIn` 被夹在 `[0.18, 0.92]`，极小尺寸下内腔不会退化成负宽
- 真实尺寸下限查证：`editor.js` L1224 `const MIN = 20;`（拖拽缩放手柄）+ L1611-1612 属性面板 `Math.max(20, …)` → 组件最小可达 **20×20**，故冒烟档位由原计划 7 档扩到 **12 档**覆盖下限边界

### 步骤 5（clipPath + SMIL 物料流）

- 12 档冒烟 `ALL OK`：
  - `<clipPath id="sv_clip_*">` 内的路径 `d` 与内腔路径 `d` **完全相等**，且 `clip-path` 引用恰好 1 处
  - 粒子 `<circle>` 全部包在 `<g clip-path="url(#sv_clip_*)">` 内；粒子数 3~8（随尺寸/路径长自适应）
  - 每粒子 3 个 `<animate>`（`cx` / `cy` / `opacity`），故 `animate` 计数 `= pCount*3`；全部写成自闭合标签
  - 逐粒子静态 `cx/cy` 经内腔多边形点内测试，全部落在内腔内
  - `stripSMIL()` 后 `<animate` 计数 = 0 且无 `NaN`
- 浏览器核对页：运行态 `anim` = 18（默认 120×150，6 粒）/ 24（60×300，8 粒）/ 9（200×20，3 粒）；编辑态格 `anim = 0`；`clipPath` 与引用成对；10 格 × 2 = 20 个 `linearGradient`/`clipPath` id **无重复**

### 步骤 6（整体校验与产物清理）

- `node --check templates/switchValve.js` → `check=0`；`node _sv_smoke.js` → 12 档 + 5 档状态件 **ALL OK**
- **浏览器实机核对**（`file://` 被浏览器安全策略拦截，改用仓库自带 `server.js` 的 `http://localhost:8090`；8090 已被现有实例占用，直接复用）
  - `_sv_check.html`（10 格：默认 120×150 / 下限 20×20（编辑态）/ 细高 60×300 / 扁平 200×20 / 窄高 20×200（编辑态）/ AUTO / 自定义色 `#FF7A45` / 镜像 / 旋转 90° / 编辑态 120×150）→ 输出 **合计 55/55 PASS**：
    - `无 NaN/undefined 属性 bad=0`
    - 10 格端口命中全中、偏差 `0.00`（如 200×20：`inlet=100.00/100.00 A=60.00/60.00 B=140.00/140.00`）
    - 10 格可见几何包围盒（排除被裁剪的粒子，取 `getBBox()`）**0 越界**，如 20×20：`box=[2.2, 0.0, 17.8, 20.0] / [0,0,20,20]`
    - 10 格粒子裁剪组 `clip=true g=true particles=3~8`；`stripSMIL` 后编辑态格 `anim=0`
    - 自定义指示色 `#FF7A45` 生效；渐变/裁剪 id `total=20 dup=0`
    - 两态封堵叉 `active=0/1` 各 `lines=2`
  - `editor.html` 实机（`addComponent('switchValve', 120, 120)`）：
    - 编辑态组件内 `animate,animateTransform` 计数 **0**（`stripSMIL()` 生效）
    - 三个端口**组件局部坐标**（用 `.port` 元素 `cx/cy` 与 `portPos()` 双口径）：`inlet (60.00, 0.00)`、`outletA (36.00, 150.00)`、`outletB (84.00, 150.00)`，偏差 **`0.00`**
    - 镜像 `props.mirrored = true` 后 A/B 左右互换（`outletA ↔ outletB` 的 x 互换），与宿主 `(1-pt.x)` 端口映射一致
    - 旋转 `comp.rotation = 90` 后组 `transform = translate(120,120) rotate(90 60 75)`（符合宿主格式）
    - 临时组件已删除、`doc.components` 长度复原、未保存
  - 自动互斥链路 `refreshSwitchValveAuto`（画布实机，临时 `data-id="__t_sv"` 组件）：
    | 用例 | liveMap | 实测 `dataset.active` | 实测红叉中心 x | 期望 | 结果 |
    | --- | --- | --- | --- | --- | --- |
    | a | A=1 B=0 | `0` | 84（=0.7w） | `0` / 84 | PASS |
    | b | A=0 B=1 | `1` | 36（=0.3w） | `1` / 36 | PASS |
    | c | A=1 B=1（异常） | `0` | 84 | 回落手动值 `0` | PASS |
    | d | 两路无数据（待机） | `0` | 84 | 回落手动值 `0` | PASS |
    | e | `controlMode='manual'` | `0` | 84 | 不被自动链路改写 | PASS |
    - console 无新增 error/warning；临时对象已清理
  - `preview.html` 正常加载、`TEMPLATES.switchValve` 可用、无新增 JS 报错（编辑器/预览页各有一条历史 `TypeError: Cannot read properties of undefined (reading 'w')`，与三通阀无关）
- **测量口径修正记录 2 则**：
  1. 核对页 AUTO 格把 `props.color` 也设为 `#00E5FF`，导致格内断言 `svg.querySelector('rect[fill="#00E5FF"]')` 先命中的是**执行器活塞矩形**（实测 `y=67.92`），并非 AUTO 标签；该格读数口径不严。标签归位已由「编辑态 120×150」格（实测 `y=5`，即 `flH+1`）与子代理独立断言（`auto` 有 `AUTO` 文字 / `manual` 无）共同确认。
  2. 画布首次端口读数取了 `getBoundingClientRect`（屏幕像素，含画布 zoom≈1.31 与平移），误判为 FAIL；换算后三个端口关系与期望一致，改用 `portPos()` / `.port` 元素局部坐标复测后偏差 `0.00`。
- 结论：**P5 / P6 / P8 / P9 已解决**，P1~P10 全部关闭；模板无需联动改动 `editor.js` / `preview.html`。

## 6. 追加调整：下部倾斜度（视觉反馈后）

反馈："整体不够倾斜，两个出料口的垂直段太长了"。成因：原 `hw = max(inW*0.7, min(w*0.31, h*0.21))` 偏宽（120×150 时 `hw = 31.5`），菱形左右顶点 `x = 28.5` 已经落到出口管左壁 `26.25` 的**外侧**，斜分支水平位移只剩 `2.25`（斜腿仅 `3.9°`，看上去就是两根直管）；同时 `pipeTopY = min(h - outW/2, max(botY+…, h*0.66))` 把竖直段放到 `51`（占高度 **34%**）。

改动（[switchValve.js](file:///d:/Java/GY/RS/templates/switchValve.js)）：

| 量 | 原式 | 现式 | 120×150 结果 |
| --- | --- | --- | --- |
| 菱形半宽 `hw` | `max(inW*0.7, min(w*0.31, h*0.21))` | `max(inW*0.55, min(w*0.22, h*0.095))` | `31.5 → 14.25` |
| 出口竖直段 | `pipeTopY = min(h - outW/2, max(botY+…, h*0.66))` | `stemH = max(2.5, outW*0.95, h*0.14)`；`pipeTopY = h - stemH` | `51 → 21`（34% → **14%**） |
| 斜腿角度 | — | 由 `(cx-hw) → (outAX-outW/2)` 推导 | `3.9° → **17.2°**` |
| 封堵叉 | `by = h - flH - xr - 1.2` | `by = (pipeTopY + h - flH)/2`，`xr` 追加管腔半高夹取 `stemRoom` | 随竖直段同步居中 |

- `hw` 下限 `inW*0.55` 的理由：菱形内腔须容得下立管（`bodyPts` 的交点参数 `t = xIn/(k*hw)` 要求 `k*hw ≥ 立管内半宽`），再窄会把立管壁挤到菱形横对角线上、形状退化。
- 说明：极细高档（60×300 / 20×200）斜腿角度仍只有 `2.5° / 1.4°` —— 竖直跨度 `126 / 84` 由组件高度决定，属数学必然；这两档视觉主题本就是"细高"，默认档与常规档（120×150 `17.2°`、200×90 `44.6°`、600×600 `22.0°`）倾斜已明显。
- 复检（临时脚本 `_sv_geo.js`，12 档 + 8 档状态件，已删）：
  - 12 档：端口命中偏差 `0.00`、越界 `0`、凹口 `pipeTopY > botY` 成立、内腔管口对齐、封堵叉四点在内腔内 → **ALL OK**
  - 8 档状态件：翻板两态四点在内腔内（扁平 200×20 因菱形腔极扁，按守卫整段省略 —— 预期行为）、执行器与 AUTO 标签在组件内、粒子静态坐标在内腔内、`animate = 粒子数×3`、`stripSMIL` 无残留 → **ALL OK**
  - **测量口径修正记录（第 3 则）**：临时脚本初版用 `polygon[fill="#9C99FF"]` 取翻板，扁平件翻板被省略后该选择器先命中**执行器方向三角**（同为指示色），误报"翻板越出内腔"；改为按 `opacity`（翻板 `0.85` / 执行器三角 `0.75`）区分后通过。

## 7. 追加调整：斜腿再岔开（第二次视觉反馈）

反馈："两个斜腿再岔开一些"。几何关系：斜腿岔开角 `= atan(水平位移 / 竖直跨度)`，其中水平位移 `= 0.2w - hw + outW/2`（受端口位置 `0.3w/0.7w` 锁死，只能靠收窄 `hw` 增大），竖直跨度 `= pipeTopY - splitY`（只能靠菱形下移缩短）。故三处同调：

| 量 | 上一次 | 现在 | 120×150 结果 |
| --- | --- | --- | --- |
| 进料立管 `inW` | `max(5, min(h*0.15, w*0.26))` | `max(5, min(h*0.12, w*0.20))` | `22.5 → 18` |
| 出口管 `outW` | `max(5, min(h*0.13, w*0.22))` | `= inW`（进出口同口径） | `19.5 → 18` |
| 菱形半宽 `hw` | `max(inW*0.55, min(w*0.22, h*0.095))` | `max(inW*0.58, min(w*0.20, h*0.075))` | `14.25 → 11.25` |
| 菱形位置 `midY` | `max(inW*0.5, h*0.44 - dh)` | `max(inW*0.5, h*0.57 - dh)` | `splitY 66 → 85.5` |
| **斜腿岔开角** | `17.2°` | **`26.6°`**（两腿夹角 `34.4° → 53.2°`） | 位移 `19.5 → 21.8` |

- `inW` 必须同步收窄：`hw` 下限是 `inW*0.58`，立管不收窄的话下限会卡住 `hw`（`22.5*0.58 = 13.05`），菱形再窄也无法收。
- 副作用（有意为之）：菱形分料腔整体下移到 `0.57h`，进料立管相应变长；因为"斜腿更岔开"在数学上只能靠缩短斜腿的竖直跨度实现。
- 复检（临时脚本 `_sv_geo.js`，12 档，已删）：端口命中偏差 `0.00`、外壳/执行器/AUTO 不越界、底部凹口 `> 0`、内腔管口对齐、封堵叉与粒子在内腔内、翻板四点在内腔内、`animate = 粒子数×3`、`stripSMIL` 无残留 → **ALL OK**
- 各档岔开角：`120×150 26.6°` / `600×600 32.5°` / `200×90 56.0°` / `110×60 50.5°` / `300×60 73.6°`（扁平件天然更岔）；极细高档 `60×300 3.9°`、`20×90 5.5°`、`20×200 2.5°` —— 竖直跨度由组件高度决定、宽度又窄，属数学必然。

## 8. 追加调整：缩短进料立管（第三次视觉反馈）

反馈："进料口太长了"。症结：上一节为把斜腿岔开，把菱形整体下移到 `0.57h`，立管直段（`0 → midY`）随之变长到 `55%`。但**立管长度与斜腿角度并非同一变量**：

- 斜腿岔开角只由 `splitY`（菱形横对角线 = 斜腿起点）决定；
- 立管直段长度 ≈ `splitY - 0.2·dh`（`midY = splitY - dh`，且需 `midY ≥ inW*0.5` 保住立管入腔段）。

所以**在 `splitY` 不动的前提下加大 `dh`（菱形拉高）**，可让上顶点 `midY` 自动上移、立管变短，而斜腿角度完全不受影响。若单纯把 `midY` 上提则会同时缩短斜腿竖直跨度、把两腿变得更岔（见上节副作用），故不采用。

| 量 | 上两次 | 现在 | 120×150 结果 |
| --- | --- | --- | --- |
| 菱形半高 `dh` | `max(2, h*0.10)` | `max(2, h*0.135)` | `15 → 20.25` |
| 菱形位置 `midY` | `max(inW*0.5, h*0.57 - dh)` | `max(inW*0.5, h*0.47 - dh)` | `70.5 → 50.25` |
| 横对角线 `splitY` | `0.57h = 85.5` | `0.47h = 70.5` | 斜腿起点上移 |
| 下顶点 `botY` | `100.5` | `90.75` | 凹口 `pipeTopY - botY` 仍 `38.25 > 0` |
| **立管直段占比** | `55%` | **`33.5%`**（约 `1/3`） | `83 → 50.25` |
| 斜腿岔开角 | `26.6°` | `20.4°`（位移 `21.8` / 跨度 `58.5`） | 竖直跨度 `43.5 → 58.5` |

- **取舍说明（如实记录）**：立管长度与斜腿岔开角共用一个自由度 —— 立管 `= midY ≈ splitY - dh`，而斜腿竖直跨度 `= pipeTopY - splitY`，`splitY` 越靠下则腿越岔、立管越长。本次选择"立管短"优先，把 `splitY` 从 `0.57h` 提到 `0.47h`（跨度 `43.5 → 58.5`），岔开角相应从 `26.6°` 回落到 `20.4°`；两者不可能同时最优。
- 那为什么还要同时加大 `dh`（`0.10h → 0.135h`）？因为 `midY = 0.47h - dh` ⇒ `splitY = midY + dh = 0.47h`，**`splitY` 与 `dh` 无关**，所以 `dh` 是"纯赚"的：它只让上顶点继续上移、立管进一步缩短（若 `dh` 保持 `0.10h`，`midY` 只能是 `55.5`，立管占 `37%`；加大后为 `50.25`，占 `33.5%`），而斜腿起点与角度纹丝不动。`dh` 的上限由凹口约束 `splitY + dh < pipeTopY` 决定（本档 `dh < 58.5`，取 `20.25` 尚有很大余量）。
- 复检（临时脚本 `_sv_geo.js`，12 档，已删）：

  ```
  120x150  splitY 70.5(47%)  斜腿 21.8/58.5 = 20.4°  竖直段 21.0(14%)  凹口 38.3  OK
  200x90   splitY 42.3(47%)  斜腿 38.6/35.1 = 47.8°  竖直段 12.6(14%)  凹口 23.0  OK
  60x300   splitY 141.0(47%) 斜腿 6.0/117.0 = 2.9°   竖直段 42.0(14%)  凹口 76.5  OK
  200x20   splitY 9.4(47%)   斜腿 39.6/5.8  = 81.6°  竖直段 4.8(24%)   凹口 3.2   OK
  20x20    splitY 9.4(47%)   斜腿 3.6/5.8   = 31.6°  竖直段 4.8(24%)   凹口 3.2   OK
  20x200   splitY 94.0(47%)  斜腿 2.5/78.0  = 1.8°   竖直段 28.0(14%)  凹口 51.0  OK
  600x600  splitY 282.0(47%) 斜腿 111.0/234.0= 25.4°  竖直段 84.0(14%)  凹口 153.0 OK
  24x20    splitY 9.4(47%)   斜腿 4.4/5.8   = 36.9°  竖直段 4.8(24%)   凹口 3.2   OK
  300x60   splitY 28.2(47%)  斜腿 59.1/23.4 = 68.4°  竖直段 8.4(14%)   凹口 15.3  OK
  110x60   splitY 28.2(47%)  斜腿 21.1/23.4 = 42.0°  竖直段 8.4(14%)   凹口 15.3  OK
  120x20   splitY 9.4(47%)   斜腿 23.6/5.8  = 76.1°  竖直段 4.8(24%)   凹口 3.2   OK
  20x90    splitY 42.3(47%)  斜腿 2.5/35.1  = 4.1°   竖直段 12.6(14%)  凹口 23.0  OK
  ALL OK
  ```

  - `node --check templates/switchValve.js` → `check=0`
  - 断言项与上两节一致（端口命中偏差 `0.00`、外壳/执行器/AUTO 不越界、凹口 `pipeTopY > botY`、内腔管口对齐、封堵叉与粒子在内腔内、翻板四点在内腔内、`animate = 粒子数×3`、`stripSMIL` 无残留）全过；`20×20` 等极小档在 `midY ≥ inW*0.5` 兜底下退化为 `midY = 9.4`，仍成立。
  - 注：脚本打印首列标签写的是"立管"，实为 `bodyPts[2][1] = splitY`（横对角线 y）系数；立管直段实为 `midY`（120×150 = `50.25`，即 `33.5%`）。